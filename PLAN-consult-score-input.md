# PLAN-consult-score-input — 실제 성적 입력 모드 (진단 추정 → 실성적 상담)

> **트랙: 컨설팅 · 우선순위 3/5** (선행: PLAN-consult-data-audit — 변환표 감사 통과분만 사용)
> 근거: 진단 12~20문항 추정은 학습 코칭용이지 입시 상담용 정밀도가 아님. 강사(김과외 상위 0.1%)가 실제 상담에서 쓰는 것은 학생의 **실제 모평 성적표** — 이를 입력받으면 리포트가 곧바로 상담 도구가 됨. 변환 재료(score_conversion 413행, 대학 환산식 96행) 이미 확보.
> 라벨: [Confirmed] 변환 데이터 실측. [Inference] 입력 UI의 상담 현장 적합성 — 강사(대표) 파일럿 사용 후 조정.

## 목표

학습기에 "성적 입력" 모드 추가: 최근 모평의 과목별 성적(국·수·영·탐 — 원점수 또는 등급/백분위/표준점수)을 입력하면, 감사 통과 변환표·컷 데이터로 [참고] 위치 정보를 리포트에 결합한다.

## 수정해야 할 정확한 파일

| 파일                                           | 작업                                                     |
| ---------------------------------------------- | -------------------------------------------------------- |
| `평가원_수학영어_확장/08_tool/data_consult.js` | consult_master_v1 발췌 로드 (data-audit 산출)            |
| `평가원_수학영어_확장/08_tool/지니학습기.html` | 성적 입력 폼 + 리포트 결합 (수정 전 `.bak_scorein` 백업) |
| `컨설팅_데모/**`                               | **읽기 전용**                                            |

## 단계별 작업 순서

1. **입력 폼 (리포트 탭 상단 "실제 성적으로 보기" 토글)**: 시험 선택(변환표에 있는 시험만 드롭다운 — scoreTable의 exam 목록에서 생성) → 과목별 입력: 국어/수학(선택과목 포함) = 원점수·표준점수·백분위·등급 중 아는 것 하나(라디오로 어떤 값인지 명시), 영어/한국사 = 등급만, 탐구 2과목 = 백분위 또는 등급. 저장은 localStorage(genie_realscore).
2. **변환 로직**: 원점수 입력 시 scoreTable에서 해당 시험·과목 행으로 std/percentile/grade 조회(**보간 금지** — 표에 없는 원점수는 "±1점 인접값 참고"로 표시). 백분위/등급 직접 입력은 그대로 사용. **모든 표시값에 "[해당 모평 기준]" 시험명 병기**.
3. **리포트 결합**: 실성적 모드가 켜지면 ① 등급 맥락 블록을 실성적으로 대체(진단 추정과 병기, 라벨 구분: [실제 n월 모평] vs [진단 추정]) ② 참고 대학 라인은 실성적 백분위 기준 ±5 범위 ③ english_grade_table 있는 대학은 "이 대학은 영어 n등급 시 감점 x" 팩트 1줄(환산 총점 계산은 금지 — 아래 엣지 1).
4. **자가 검증 (node)**: ① 변환표 왕복 검사(scoreTable 20행 표본: raw→조회→grade가 원 행과 일치) ② 표 밖 원점수(만점+1, 0점) 입력 시 crash 없이 안내 문구 ③ 과목 일부만 입력 시 리포트가 있는 과목만 표시 — 로그 첨부. node --check + 파일 안전 절차.

## 성능 낮은 모델이 놓치기 쉬운 엣지 케이스

1. **대학 환산 총점 계산 금지**: conversion_formulas의 weights로 "당신의 환산점수는 632.1점" 같은 계산을 만들지 말 것 — 반영비·가산점·탐구 변환표준점수 등 미보유 변수로 오차가 크고, 오차 있는 정밀 숫자는 합격 예측처럼 읽힌다(법적 리스크). 허용: 개별 팩트("영어 2등급 감점 0.5" 등 표에 있는 값 그대로).
2. **시험이 다르면 변환 금지**: 6월 모평 원점수를 9월 변환표로 바꾸는 것은 무의미 — 시험 선택과 변환표 시험이 반드시 일치, 없으면 "이 시험 변환표 미보유, 등급/백분위로 직접 입력" 안내.
3. **수학 선택과목**: 같은 원점수라도 선택과목별 표준점수가 다름 — scoreTable의 elective 필드로 분리 조회, 선택과목 미입력 시 원점수 변환 차단(등급 직접 입력만 허용).
4. **영어·한국사는 절대평가**: 원점수 입력란 자체를 만들지 말 것(등급만) — 만들면 혼란.
5. **"진단 추정"과 혼합 금지**: 실성적과 진단 추정을 평균 내는 식의 결합 금지 — 항상 병렬 표기(출처 라벨).
6. **입력값 방어**: 원점수 0~~100(수학 선택 포함), 백분위 0~~100, 등급 1~9 밖은 입력 단계에서 차단. 붙여넣기 문자열("2등급") 파싱 시도 금지 — 숫자만.
7. **개인정보**: 성적은 localStorage에만 — 어떤 네트워크 전송도 추가하지 말 것 (미성년 성적 데이터).

## 직접 검증할 수 있는 완료 기준 (대표용)

1. 리포트 탭 → "실제 성적으로 보기" → 시험 선택 후 수학 원점수+선택과목 입력 → 표준점수·백분위·등급이 뜨고 "[해당 모평 기준]" 병기.
2. 변환표에 없는 시험을 고르면 원점수 입력이 막히고 등급 직접 입력 안내가 뜸.
3. 리포트에 [실제 모평]과 [진단 추정]이 나란히 구분 표기되고, Ctrl+F로 "환산점수·합격" 단어가 없음.
4. 인쇄 미리보기에 실성적 블록 포함.
