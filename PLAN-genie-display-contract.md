# PLAN-genie-display-contract — 학습기 영어 문항 표시 계약 레드팀 + 수리

> **우선순위: 2 / 5**
> 근거: 학습기는 영어 rawText를 "① 위치에서 잘라" 지문만 표시한다. 그런데 **어법(29류)·무관한 문장(35류)·문장 삽입(38/39류)은 지문 안에 ①~⑤가 문제 구성요소**라, 첫 ①에서 자르면 지문 자체가 파괴된다(오학습 = 치명). 1차 레드팀이 이 지점을 지목한 직후 세션 중단됨 — 미확정 상태로 방치 중. 도구를 학생/컨설팅에 쓰기 전 필수 관문.
> 최초 라벨: [Unverified] 파괴 여부 자체가 미확정이었으나, 아래 2026-07-16 전수 검사로 [Confirmed] 전환.

## 2026-07-16 실행 결과

**상태: [Confirmed] 기술 검증 통과 · [Pending] 대표 확인**

현재 `지니학습기.html`에는 첫 `①`이 아니라 마지막 `①→⑤` 선지 구간을 찾는 수리와, 어법·무관한 문장·문장 삽입형의 지문 내부 마커를 보존하는 처리가 이미 반영되어 있다. 브라우저가 실제로 불러온 `GENIE_CORE.cutChoicesFromRaw` 함수를 데이터 868문항에 그대로 적용해 전수 검사했다.

| 검사                                           | 대상 | 실패 |
| ---------------------------------------------- | ---: | ---: |
| 화면 표시 가능 문항                            |  868 |    0 |
| 어법·무관한 문장·문장 삽입형 `①~⑤` 보존        |  124 |    0 |
| 빈칸 31~34번 `____` 보존                       |  124 |    0 |
| 표시 결과가 원문 길이의 30% 미만인 과도한 절단 |  868 |    0 |
| 공통 지문이 없는데 화면 지문이 빈 문항         |  868 |    0 |

추가 안전 점검도 통과했다.

- [Confirmed] 수학 322문항 `problem_latex`의 이스케이프되지 않은 `$` 개수 홀수 문항: 0건.
- [Confirmed] 답 또는 확신도가 없으면 채점 버튼이 비활성화되고, `learnGrade`도 같은 조건에서 즉시 종료한다.
- [Confirmed] 손상된 localStorage JSON은 `load`의 `try/catch`에서 기본값으로 복구한다.
- [Confirmed] 진단 계산은 0문항에서 `ratio: 0`, 만점 표본에서 `ratio: 1`을 반환하며 둘 다 유한값이다.

**판정:** [Confirmed] 표시 로직 추가 수정 불필요. 대표가 본 결과를 확인하면 E1을 종결하고 다음 순서인 M2(`PLAN-math-gate.md`)로 이동한다.

## 목표

영어 868문항 전수에 대해 "화면에 표시되는 지문+선지"가 원문과 동일 정보를 담는지 기계 검증하고, 깨지는 유형을 수리한다.

## 수정해야 할 정확한 파일

| 파일                                                           | 작업                                                                        |
| -------------------------------------------------------------- | --------------------------------------------------------------------------- |
| `평가원_수학영어_확장/08_tool/지니학습기.html`                 | 검증 후 필요 시 수리 (수리 전 `지니학습기.html.bak_display` 백업 생성 필수) |
| `/tmp/display_contract_report.md`                              | 신규 (검증 결과)                                                            |
| `평가원_수학영어_확장/08_tool/data_english.js`, `data_math.js` | **읽기 전용 — 수정 절대 금지**                                              |

## 단계별 작업 순서

1. **표시 로직 위치 파악**: `grep -n "①" 지니학습기.html` 과 `grep -n "rawText" 지니학습기.html` 로 지문 자르기 함수를 찾는다 (빌더 보고 기준: "rawText는 ① 위치에서 잘라 중복 제거" 로직 존재).
2. **전수 시뮬레이션 (node)**: 아래 골격으로 검사 스크립트 작성 → 실행:
   ```js
   global.window = {};
   require("<mount>/평가원_수학영어_확장/08_tool/data_english.js");
   const qs = window.GENIE_ENGLISH.questions;
   // 지니학습기.html에서 자르기 함수를 그대로 복사해 붙여넣고(임의 재작성 금지) 각 문항에 적용
   // 검사 A: type이 어법/무관한 문장/문장 삽입/장문 어휘(42)인 문항 → 잘린 지문에 ①~⑤ 마커가 "선지 개수만큼" 남아있는가?
   // 검사 B: 빈칸(31~34) → 잘린 지문에 "____"(빈칸 표시)가 남아있는가?
   // 검사 C: 밑줄 의미(21)/장문 지칭(44) → 밑줄 어구·(a)~(e) 표기가 남아있는가?
   // 검사 D: 잘린 지문 길이가 rawText 대비 30% 미만이면 과절단 의심 플래그
   ```
   유형별 파괴 문항 수를 집계해 /tmp/display_contract_report.md에 기록.
3. **수리** (파괴 확인된 경우만, 최소 수술):
   - 원칙: 자르기 지점을 "첫 ①"이 아니라 **"마지막 선지 블록의 시작"**으로 바꾼다. 안전한 규칙: rawText 끝에서부터 `①…②…③…④…⑤…` 가 순서대로 모두 등장하는 마지막 구간을 찾아 그 앞까지를 지문으로 간주. 이 규칙으로도 전수 재검사를 통과 못 하는 유형(예: 순서 36/37 선지 "(A)-(C)-(B)" 안의 문자)은 해당 type만 "rawText 전체 표시 + 선지 중복 무시" 분기로 처리.
4. **회귀 검증**: 수정 후 2단계 스크립트 재실행 → 전 유형 파괴 0 확인. script 블록 추출 후 `node --check` 문법 통과.
5. **추가 점검 4건** (같은 세션에서, 각 1개 node 스크립트):
   - 수학 problem_latex의 `$` 개수 홀수 문항 스캔 (0건이어야 함, 발견 시 id 목록 보고만 — 데이터 수정 금지).
   - 확신도 미선택 상태에서 채점 함수 직접 호출 시 차단되는지 (코드 경로 확인).
   - `localStorage.setItem('genie_history','{깨진json')` 주입 시 앱 초기화 코드가 crash 없이 복구하는지 (try/catch 존재 확인, 없으면 추가).
   - 진단 결과 계산에서 0문항/만점 시 NaN이 나오는 수식이 있는지 (분모 0 가드).
6. **파일 안전 절차**: 수리본은 /tmp에 완성 → node --check → `cat`으로 mount 복사 → 재읽기 검증.

## 성능 낮은 모델이 놓치기 쉬운 엣지 케이스

1. **"첫 ①에서 자르기"의 함정이 이 계획의 핵심**: 어법 29번은 지문 중간에 ①associated ②it… 처럼 밑줄 마커가 박혀 있다. 첫 ①에서 자르면 지문의 절반이 사라지는데, 화면상으로는 "짧은 지문"처럼 보여서 눈치채기 어렵다. 반드시 기계 검사로 판정할 것.
2. **검사할 함수는 HTML에서 복사**: 자르기 로직을 "비슷하게 재작성"해서 검사하면 실제 앱과 다른 결과가 나온다. HTML 안의 함수 문자열을 그대로 추출해 검사에 사용.
3. **순서 유형(36/37/43)의 선지에는 ①이 있지만 지문에는 없음** — 이 유형은 첫 ① 자르기가 오히려 정상 작동한다. 유형별로 정상/파괴를 구분해 보고 (일괄 "전부 파괴"로 과장 금지).
4. **도표(25)·안내문(27/28)은 선지가 본문에 내장**된 형태가 있다 — 잘라낸 뒤 choices 배열로 다시 렌더되므로 정보 손실은 아님. "지문에 선지 텍스트가 없음"을 결함으로 오판하지 말 것.
5. **sharedPassage 문항(41~45)**: rawText가 원래 발문+선지뿐이라 자르면 빈 문자열이 될 수 있음 — 빈 지문은 sharedPassage가 대신 표시되는지 확인.
6. **KaTeX 구분자 오탐**: 수학 `$` 홀수 검사에서 `\$`(이스케이프된 달러)는 제외.
7. 데이터 파일(data_*.js)을 "고치면 편한" 유혹 — 금지. 표시 로직만 수리한다.

## 직접 검증할 수 있는 완료 기준 (대표용)

1. `지니학습기.html` 더블클릭 → 기출 학습 → 영어 → **어법** 유형 아무 문항: 지문 안에 ①~⑤ 밑줄 마커가 보이고, 지문이 문단 중간에서 끊기지 않음.
2. **문장 삽입** 유형 아무 문항: 지문 안에 ( ① )~( ⑤ ) 위치 표시가 전부 보임.
3. **빈칸** 유형 아무 문항: 지문 안에 빈칸(밑줄 공백)이 보임.
4. /tmp/display_contract_report.md에 "유형별 검사 결과 표"(파괴 0 또는 수리 전→후 수치)가 있고, `지니학습기.html.bak_display` 백업 파일이 존재.
