# PLAN-consult-teacher-mode — 강사 모드 (다학생 프로필 + 상담 내보내기)

> **트랙: 컨설팅 · 우선순위 5/5** (선행: PLAN-consult-history — 학생별 이력이 있어야 프로필이 의미 있음)
> 근거: 현 학습기는 1인용(localStorage 단일 학생) — 강사가 과외·학원에서 쓰려면 학생 전환이 필수. "김과외 상위 0.1% 강사의 상담 도구"라는 포지셔닝을 실물로 만드는 마지막 조각이자 B2B(학원 납품) 데모의 뼈대.
> 라벨: [Inference] 다학생 요구는 강사 워크플로 추정 — 대표가 실사용 1주 후 우선순위 재평가.

## 목표

강사 모드: 학생 프로필 생성/전환(모든 genie_* 데이터를 프로필별 분리), 학생 목록 대시보드(최근 진단·취약 Top1 한눈), 학생 데이터 JSON 내보내기/가져오기(다른 기기 이동).

## 수정해야 할 정확한 파일

| 파일                                           | 작업                                                            |
| ---------------------------------------------- | --------------------------------------------------------------- |
| `평가원_수학영어_확장/08_tool/지니학습기.html` | 프로필 계층 + 대시보드 + 내보내기 (수정 전 `.bak_teacher` 백업) |
| 데이터 파일들                                  | **수정 금지**                                                   |

## 단계별 작업 순서

1. **저장 계층 설계 (하위호환이 전부)**: 현 키 구조 `genie_*` 를 `genie_p{프로필id}_*` 로 네임스페이스화. **마이그레이션**: 최초 실행 시 기존 `genie_*` 데이터를 "기본 학생" 프로필(p0)로 자동 이관(복사 후 원 키는 `genie_legacy_*` 로 개명 보존 — 삭제 금지). 프로필 목록은 `genie_profiles` = [{id,name,createdAt}].
2. **저장/조회 래퍼**: localStorage 직접 호출을 전부 `store.get(key)/set(key,val)` 래퍼로 치환(현재 프로필 id 자동 prefix). grep으로 localStorage 호출 전수 찾아 치환 — 누락 1곳이 데이터 섞임 사고.
3. **UI**: ① 헤더에 현재 학생명 + 전환 드롭다운 + "학생 추가" ② 강사 대시보드 화면: 학생 카드(이름/최근 진단일/영어 추정 등급/취약 Top1/복습 밀린 수) ③ 학생 카드에서 바로 리포트 열기.
4. **내보내기/가져오기**: 프로필 1개의 전 데이터를 JSON으로 다운로드(Blob+a[download]) / 파일 선택으로 가져오기(신규 프로필로 추가 — **기존 프로필 덮어쓰기 금지**, 이름 뒤 "(가져옴)" 부기).
5. **자가 검증 (node + 수동)**: node로 래퍼 단위 테스트(프로필 A 저장 → B 전환 → A 데이터 안 보임 → A 복귀 → 복원). 수동: 기존 데이터 있는 상태에서 첫 실행 → 기본 학생으로 보이는지. node --check + 파일 안전 절차.

## 성능 낮은 모델이 놓치기 쉬운 엣지 케이스

1. **마이그레이션 원본 삭제 금지**: 이관 실패(도중 새로고침) 시 복구 불가가 되므로 원 키는 개명 보존. 이관 완료 플래그(genie_migrated_v1)로 재실행 방지.
2. **localStorage 호출 누락**: 래퍼 치환에서 한 곳이라도 빠지면 학생 간 데이터 오염(A 학생 오답이 B에게) — 치환 후 `grep -c "localStorage" ` 로 잔여 직접 호출이 래퍼 내부 것만인지 확인.
3. **프로필 삭제**: 삭제는 2단 확인 + "내보내기 먼저" 권장 문구. 삭제해도 legacy 키는 무관.
4. **가져오기 파일 검증**: JSON.parse 실패·필수 키 부재 시 "형식이 다릅니다" 안내(crash 금지). 프로필 id 충돌 시 새 id 발급.
5. **용량**: localStorage ~5MB 한도 — 학생 10명 × 이력이면 근접 가능. 프로필 추가 시 현재 사용량 표시(대략치), 80% 초과 시 경고.
6. **개인정보**: 학생 실명 대신 별칭 권장 문구를 학생 추가 폼에 표시. 내보내기 파일에도 성적이 담김을 안내(강사 책임 고지 1줄). 네트워크 전송 추가 금지.
7. **미성년 데이터 제3자 제공 이슈**: 이 기능은 강사 로컬 사용 전제 — 서버 저장으로 확장할 때는 별도 법률 검토가 선행돼야 함을 파일 상단 주석에 명시(마스터플랜 V13 연동).

## 직접 검증할 수 있는 완료 기준 (대표용)

1. 기존 데이터가 있는 상태에서 열면 "기본 학생"으로 그대로 보이고, 학생 2명 추가 후 전환하면 오답노트·진단 이력이 학생별로 완전히 분리됨.
2. 대시보드에 학생 카드 3장(이름/최근 진단/취약 Top1)이 보이고 카드에서 리포트가 바로 열림.
3. 학생 A 내보내기 → 학생 전부 삭제 → 가져오기 → A의 이력·오답노트가 복원됨.
4. 개발자도구에서 `genie_p0_`, `genie_p1_` prefix 분리가 보이고 `genie_legacy_*` 가 보존돼 있음.
