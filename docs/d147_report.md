# D-147 상신 — 형광펜 실효성 축 신설 + D-146 판정 반영

> 딸린 산출물: [`d147_cs_effect_live.md`](d147_cs_effect_live.md) ·
> [`d146_r2019b_na_dryrun.md`](d146_r2019b_na_dryrun.md)(B안으로 갱신)

---

## ① 🔴 형광펜 실효성 축 — `pipeline/cs_effect_audit.mjs`

### LIVE 267세트 전수 결과 — **형광펜 0개 세트 0건** `[Confirmed]`

| 항목 | LIVE 267 | 전체 396 |
|---|--:|--:|
| 🔴 **형광펜이 한 개도 안 켜지는 세트** | **0** | 2 |
| ⚠ 일부 `cs_id` 만 비-하이라이트 | 9 | 14 |
| 🔴 없는 문장 참조(`DEAD_csid`) | **0** | **0** |

**학생이 지금 보고 있는 화면에서 형광펜이 죽은 자리는 없습니다.** D-146 에서 걱정한 최악은 없었다.

🔴 2세트는 전부 비노출이다.

| 세트 | 건수 | 정황 |
|---|--:|---|
| `2015수능B::l2015bB` | **8** | (나) 유한라산기 산문이 통째로 없는 세트 — 근거가 각주로 몰릴 수밖에 없다 |
| `2020_9월::l20209d` | 1 | Q42#1 `cs_ids` 가 `l20209ds16`(`omission` = 중략) 하나뿐 |

### 검산 — `l20209d` Q42#1 을 잡는다 ✅

```
2020_9월 | l20209d | — | Q42#1(l20209ds16:omission) | — |
```

### S-13 — ⚠ 9세트를 실물로 확인했다

**결함이 아니다. ⚠ 등급이 맞다.** 표본으로 `2022_6월::l20226c` Q29(7건 중 가장 몰린 자리)를 열었다.

- 선지 5개 **전부**가 `l20226cs8`(각주 `* 국궁수명: 존경하는 뜻으로 몸을 굽히며 …`)을 같이 달고 있다
- 그런데 **선지마다 본문 근거가 2~4개씩 따로 있다** → 화면에서 형광펜은 정상으로 켜진다
- 즉 **각주 `cs_id` 는 잉여**일 뿐 학생 피해가 없다

그래서 이 축은 ⚠ 로 내고 **공개를 막지 않는다.** 정리는 배치 작업 때 곁들이면 된다.

### 비-하이라이트 목록은 추측하지 않고 렌더 코드에서 확인했다 (S-15)

`src/PassagePanel.jsx` — `workTag` 는 굵은 제목 `div` 로만 그려지고 하이라이트 style 을 타지 않는다.
`quality_gate` 주석도 같다. 확정 목록: `footnote` · `author` · `omission` · `workTag` · `image` · `figure`.
`stage`·`speech`(극문학)는 **하이라이트된다** — 비-하이라이트로 넣지 않았다.

### 9/2 스프린트 게이트 편입 — 완료

런북 `step 8` 을 **게이트 7종 → 9종**으로 늘렸다.

| 새 단계 | 명령 | 통과 기준 |
|---|---|---|
| **8-6** 형광펜이 진짜 켜지는가 | `node pipeline/cs_effect_audit.mjs --year 2027_9월` | `형광펜 0개 세트 0` |
| **8-7** 참조하는 지문이 실재하는가 | `node pipeline/passage_gap_audit.mjs --year 2027_9월` | `결손 보유 0` |

기존 8-6·8-7 은 8-8·8-9 로 밀었다. 소요는 12분 → 14분.

---

## ② `r2019b` — B안(극문학 분할) 확정 dry-run

**아직 적용하지 않았다.** 발주 ② 마지막 줄대로 dry-run 보고까지다.
전문: [`d146_r2019b_na_dryrun.md`](d146_r2019b_na_dryrun.md)

| 확인 항목 | 결과 |
|---|---|
| 원본 글자 보존 | **1,228 / 1,228자 ✅** (공백 제외 대조) |
| `#68`~`#75` 독립 문장 | **8 / 8 ✅** |
| 각주 2줄 (`◯E` 효과음 · `O･L`) | 포함 ✅ |
| `s1`~`s13` | **무변경** — 새 문장은 `s14`~`s57` 로만 |
| ㉠㉡㉢ | `s7` · `s9` · `s13` — D-145 상신과 일치 ✅ |
| 총 문장 | 13 → **57** (44 추가) |

### `◯E` 7개 문장 — `[확인 필요]` 로 표시했다

**정정**: D-146 에서 「8곳」이라 적었으나 실제는 **문장 7개 · 출현 7회**다.

| 문장 | 추출된 형태 |
|---|---|
| `s19` `s34` `s35` `s36` `s53` `s54` | `철호◯ E :` · `영호◯ E :` (원문자 + 탭 + `E`) |
| `s56` | `* ◯\tE : 효과음(effect). 화면에 삽입된 음향.` |

**원본은 한 글자(ⓔ)로 보인다.** §13⑬ 대로 추출 결과를 원문으로 삼지 않았다.
**적용 시 이 7개 문장은 추출 문자열 그대로 넣지 않는다** — 심사관이 화면에서 글자를 확정한 뒤 일괄 치환한다.

### 적용 대기 — 심사관 판정 후 실행

1. 백업 + MD5 → `s14`~ 추가 → 되읽기 검산(S-02)
2. ㉠㉡㉢ `s7`·`s9`·`s13` 정박 (별도)
3. `[A]` 꺾쇠는 여전히 화면 지목 대기 (⑤)
4. `passage_gap_audit` 재실행 → `#68`~`#75` 부재 0 확인

---

## ③ S-11 조항 추가 — 양쪽 문서 반영 완료

`docs/sent_split_contract.md` 와 `docs/session_20260825_state.md` **양쪽**에 넣었다.

> 🔴 **`resplitProse` 는 독서 전용 — 운문·극문학에 적용 금지**
> `r2019b` (나) 시나리오에서 **62자 유실** 실증(1,228 → 1,166자).
> 종결어미(`다자까가라`)로만 자르는데 시나리오·시는 그렇게 안 끝나는 줄이 많아
> **오류를 내지 않고 조용히 틀린다.**

계약서 쪽에는 실행 규칙 3개를 덧붙였다 — ① `if (sec === "reading")` 가드를 풀지 않는다
② 극문학은 §13⑧ `stage`/`speech`, 새 문장은 **장면 번호·화자 표지·`(중략)`** 에서만 시작
③ **적용 전 공백 제외 글자 수를 원본과 대조**한다.

---

## ④ `l20216d` (가) 표지 추가 — **적용·push 완료** `[Confirmed]`

LIVE 세트라 절차를 전부 밟았다.

| 단계 | 결과 |
|---|---|
| 백업 | `pipeline/backups/all_data_204.before_l20216d_worktag.json` (MD5 `4be640cc…`) |
| 변경 | `l20216ds900` `[workTag]` `(가)` 를 배열 맨 앞에 삽입 · **+54 bytes** |
| 기존 71문장 | **바이트 단위 동일** — id 재부여 없음 |
| 되읽기 검산(S-02) | ✅ 통과 |
| `build_split --verify` | ✅ **LIVE 267세트 · 필드 69,445개 대조 → 누락 0** |
| `passage_gap_audit --live` | ✅ **결손 0** (1 → 0) |

적용 후 all_data MD5 `86711d20feebe3e806c70f39250ef3c7`.

---

## ⑤ 근거·pat 적용 준비 — 판정 대기

`l20199a` Q16#4·#5 를 제외해 **근거 32건 + pat 4건**이 대상이다.

| 세트 | 근거 | pat |
|---|--:|--:|
| `2019_9월::l20199a` | 14 (16 − 2 제외) | 0 |
| `2019_9월::r20199c` | 8 | 1 |
| `2019_9월::l20199e` | 6 | 0 |
| `2020_6월::l20206b` | 3 | 1 |
| `2020_6월::l20206c` | 1 | 2 |
| **합계** | **32** | **4** |

`evidence_assign` 3중 안전장치(S-05)로 넣는다 — 선지별 SPEC · 기존 값 로그 · 되읽기 검산.
**심사관이 후보를 확정해 주면 그때 실행한다.** 지금은 아무것도 안 했다.

> `r20199c` Q36#1 은 `_pat_error` 표지도 함께 있다. **수리(pat 부여)가 먼저, 표지 제거가 나중**(S-10).

---

## 자체 발견 — `release_diag` 에 없는 축이 또 있다

이번 축(`cs_effect_audit`)이 잡는 것을 `release_diag` 13축은 **하나도 못 본다.**
①축은 `cs_ids` **개수**만 세기 때문이다. 개수가 채워져 있으면 ✅ 로 나온다.

지금 배치 B 진단(D-145)을 이 축으로 다시 돌리면 `l20209d` 1건이 추가된다 — 이미 위에 반영했다.
**배치 C/D 진단 때는 `release_diag` 와 이 축을 함께 돌려야 한다.** 런북에는 이미 넣었다.

---

## ⑥ 9/2 중단 규율

유지한다. **① 은 9/2 전에 끝냈다**(오늘 2026-08-29 완료).
