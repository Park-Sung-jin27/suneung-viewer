# D-146 상신 — 본문 결손 축 신설 + r2019b (나) 재추출 dry-run

> **아무 데이터도 고치지 않았다.** 새 도구 3종과 진단 결과만 낸다.
> 딸린 산출물: [`d146_passage_gap_scan.md`](d146_passage_gap_scan.md) ·
> [`d146_r2019b_na_dryrun.md`](d146_r2019b_na_dryrun.md) ·
> [`d146_evidence_candidates.md`](d146_evidence_candidates.md)

---

## ① 본문 결손 축 신설 — `pipeline/passage_gap_audit.mjs`

**문항이 참조하는 것이 본문에 실재하는가**를 본다. 세 갈래다.

| | 무엇을 보나 | 등급 |
|---|---|---|
| ⓐ 구획 참조 | (가)/(나)/(다) 를 묻는데 그 표시가 본문에 없다 | ⚠ 원본 대조 필요 |
| ⓑ 장면 번호 | `#68` 같은 시나리오 장면을 묻는데 본문에도 `<보기>`에도 없다 | 🔴 |
| ⓒ 구간 라벨 | `[A]~[F]` 를 묻는데 `annotations.json` 정박이 없다 | 🔴 |

**원문자 마커(㉠ ⓐ)는 넣지 않았다** — `release_diag` ⑨축·`marker_ref_audit` 이 이미 같은 것을 본다.
발주 ① 「중복되면 통합」 대로, 중복 축을 두지 않고 도구 끝에 어디서 보는지만 적었다.

### 검산 — r2019b 를 잡는다 `[Confirmed]`

```
2019수능::r2019b — 문장 13
  🔴 장면 번호 #68 #71 #69 #70 #75 #72 #73 #74 를 묻는데 본문에도 <보기>에도 없다
  🔴 구간 [A] 미정박
```

### S-13 — 대장에 올리기 전에 오탐 두 번을 잡았다

**초안 그대로 냈으면 LIVE 5세트를 오탐할 뻔했다.** 실물로 확인해서 걸렀다.

| 오탐 | 규모 | 원인 | 고친 방법 |
|---|--:|---|---|
| ⓐ 구획 | 123세트 중 **115세트** | (1) 태그가 `workTag` 가 아니라 `body` 문장 선두에 붙는 세트가 있다<br>(2) 구획 규약을 아예 안 쓰는 세트가 47개다 | 문장 선두 태그도 인정 + 규약 미사용 세트는 **판정 보류**로 분리 |
| ⓑ 장면번호 | 3세트 중 **2세트** | `l20156b`(2015_6월 A·B형)의 `#98`·`#99` 는 Q36 `<보기>` 의 **시나리오 각색본**이다. 본문에 없는 게 정상 | 세트의 `<보기>` 전문을 함께 보게 함 |
| ⓒ 구간라벨 | **LIVE 4세트** | 「`<보기>`의 `[A]`에 들어갈 …」류 **보기 빈칸**을 본문 구간으로 오인 | 발문이 `<보기>`·`들어갈` 과 묶어 쓰면 보기 빈칸으로 판정 |

ⓒ 오탐 4건은 전부 LIVE 였다 — `l20259b` · `r20229c` · `l20196c` · `r2020e`.
그중 **`l20196c` 는 `[A]`가 정상 정박돼 있는데 `<보기>`의 `[B]` 때문에 걸렸다.**
축을 그대로 올렸으면 멀쩡한 LIVE 4세트를 「결함」으로 대장에 박을 뻔했다.

### 전 데이터 396세트 스캔 결과

| 항목 | 수 |
|---|--:|
| 검사한 세트 | 396 |
| **결손 보유** | **28** (LIVE **1**) |
| 구획 규약 미사용 — 이 축으로 판정 불가 | 32 |

### 🔴 LIVE 1건 — 즉시 보고 (발주 ①)

**`2021_6월::l20216d`** — 발문이 「(가)와 (나)」를 묻는데 본문에 **(가) 표시가 없다**.

**원본 대조 결과: 라벨만 없다. 본문은 있다.** `[Confirmed]`

- 원본 시험지 41~45번: `(가)` 다음에 `[앞부분 줄거리] 전우치는 …` 로 시작한다
- 데이터: `s1` 이 바로 `[앞부분 줄거리] 전우치는 …` — **(가) 본문 s1~s17 은 그대로 있다**
- `s18` 에 `(나)` `workTag` 만 있다

즉 **본문 결손이 아니라 구획 라벨 1개 누락**이다. 오학습 위험은 없다.
다만 화면에서 앞 작품에 이름표가 없으니 「(가)와 (나)의 공통점」 문항을 풀 때 학생이 헷갈린다.
`s1` 앞에 `workTag` 문장 하나를 넣으면 끝나는 수리다 — **판정 요청**.

### 비노출 27세트 — 대부분 구간 미정박이다

| 유형 | 세트 | 비고 |
|---|--:|---|
| ⓒ 구간 `[A]`~`[E]` 미정박 | 26 | 2014~2018 에 몰려 있다 (배치 C/D 소관) |
| ⓑ 장면번호 결손 | 1 | `r2019b` |
| ⓐ 구획 표시 없음 | 1 | `l2015bB` — **기지의 (나) 산문 통째 누락**. 축이 알아서 잡았다 |

**배치 B 소관은 `r2019b` · `l20206a` · `l20206d` 3세트뿐**이고 전부 D-145 에서 이미 상신했다.
나머지 24세트는 배치 C/D 다 — 발주 ③ 대로 건드리지 않았다.

### 이 축의 한계 (스코프 명시, S-15)

- **참조되는 것만 본다.** 문항이 언급하지 않는 구간이 통째로 빠져도 못 잡는다.
- **구획 규약 미사용 32세트는 판정 자체가 불가**하다. 「결함 없음」이 아니라 「이 축이 못 본다」다.
- ⓐ 는 **라벨 누락과 본문 결손을 구분하지 못한다.** 그래서 🔴 가 아니라 ⚠ 로 낸다.

---

## ② `r2019b` (나) 재추출 dry-run — `pipeline/r2019b_na_dryrun.mjs`

**아무것도 쓰지 않았다.** 전문은 [`d146_r2019b_na_dryrun.md`](d146_r2019b_na_dryrun.md).

### ㉠㉡㉢ — D-145 상신과 전건 일치 `[Confirmed]`

| 마커 | 원본 문맥 | 재추출 | D-145 상신 | |
|---|---|---|---|---|
| ㉠ | `자동차의 문은 유난히 소리 내어 닫히고,` | `s7` | `s7` | ✅ |
| ㉡ | `한편 개천 하나를 건너 신전 집에서는,` | `s9` | `s9` | ✅ |
| ㉢ | `얼마 있다, 원래의 신전은 술집으로 변하` | `s13` | `s13` | ✅ |

발주 ② 의 「다르면 멈추고 보고」 조건에 걸리지 않았다. **마커는 원본대로 살아난다.**

### 🔴 발주 지정 방식(S-11 산문 분할)이 이 지문에는 안 맞는다

두 안을 다 만들어 나란히 냈다. **고르지 않았다 — 판정 사항이다.**

| | A. S-11 산문 (발주 지정) | B. §13⑧ 극문학 (권고) |
|---|--:|--:|
| 문장 수 | 17 | 44 |
| `sentType` | `body` 단일 | `workTag`·`stage`·`speech`·`author`·`footnote` |
| `#68`~`#75` 독립 문장 | **2 / 8** 🔴 | **8 / 8** ✅ |
| 각주 2줄 | 미포함 🔴 | 포함 |
| **원본 글자 보존** | **1,166 / 1,228자 — 62자 유실** 🔴 | **1,228 / 1,228자** ✅ |

**A 를 쓰면 안 되는 이유 셋** `[Confirmed]`

1. **62자가 사라진다.** `resplitProse` 는 산문을 이어 붙였다가 종결어미로 다시 자르는데,
   시나리오는 종결어미로 안 끝나는 줄(`#70. 산비탈`, `철호 : 용기?`)이 많아 경계를 못 찾는다.
2. **장면 번호가 문장 안에 파묻힌다.** A 의 첫 문장은 `#68 … #69 …` 를 한 덩어리로 삼킨다.
   Q25 가 `#68~#71` 을 **하나씩** 묻는데, 이러면 형광펜(`cs_ids`)을 장면별로 못 건다.
3. **S-11 은 애초에 독서 전용이다.** D-138 구현이 `if (sec === "reading")` 로 막아 놨다.
   (나)는 시나리오라 규약 적용 범위 밖이다.

**B 는 기존 극문학 세트(`l20216d` 전우치 시나리오)와 같은 틀**이고, 글자 유실이 0이다.

### 자체 발견 결함 — `◯E` 추출 artifact

효과음 원문자가 `철호◯\tE :` (원문자 + 탭 + E) 로 추출된다. 원본은 **ⓔ 한 글자**로 보인다.
**§13⑬ 대로 추출 결과를 원문으로 삼지 않는다** — 적용 전에 화면으로 글자를 확인해야 한다.
`(나)` 안에 8곳, 각주 1곳이다.

### 적용 시 안전 절차 (아직 실행 안 함)

1. `pipeline/backups/` 백업 + MD5 기록
2. **`s1`~`s13` 무변경** · 새 문장은 `s14`~ 로만 추가 (`resplitProse` 의 id 재부여를 쓰지 않는다)
3. ㉠㉡㉢ 를 `s7`·`s9`·`s13` 에 정박
4. 되읽기 검산(S-02) — 문장 수 · `s1`~`s13` 동일성 · 마커 실재
5. `passage_gap_audit` 재실행 → `#68`~`#75` 부재 **0** 확인

---

## ③ 결함 0 3세트 — gate0·gate1 결과

### gate0 정답표 전수 대조 — ✅ 통과

```
node pipeline/answer_key_audit.mjs --expect 2020_9월
  🔴 정답 불일치 : 0건 (LIVE 0)
  ✅ 2020_9월 대조됨 — 정답표 45문항이 실제로 쓰였다
```

### gate1 `quality_gate` — 3세트는 ✅, **같은 회차의 다른 세트에서 CRITICAL 1건**

```
node pipeline/quality_gate.mjs 2020_9월
  🔴 CRITICAL 1건 — 2020_9월 l20209d Q42-[1]
     CS_ALL_NONHIGHLIGHTABLE: cs_ids가 전부 비-하이라이트 sentType
     (각주/표지/중략/작가) → 형광펜 미렌더
```

| 세트 | gate0 | gate1 | 판정 |
|---|---|---|---|
| `l20209a` | ✅ | ✅ CRITICAL 0 | **gate3 대기** |
| `l20209b` | ✅ | ✅ CRITICAL 0 | **gate3 대기** |
| `l20209c` | ✅ | ✅ CRITICAL 0 | **gate3 대기** |
| `l20209d` | ✅ | 🔴 CRITICAL 1 | 별건 — 아래 |

### 🔴 자체 발견 — `l20209d` Q42#1 은 D-145 진단이 못 잡았다

`release_diag` 13축에 **「`cs_ids` 가 형광펜 안 되는 문장만 가리킨다」 축이 없다.**
D-145 에서 `l20209d` 를 「인용부호 1건뿐」으로 상신했는데, `quality_gate` 가 CRITICAL 을 냈다.
학생 화면에서 그 선지는 **형광펜이 아예 안 켜진다** — 핵심 차별점이 죽는 자리다.

이번 축(`passage_gap_audit`)에는 후보 문장에서 각주·작가·표지를 빼는 식으로 반영했지만,
**진단 축 자체는 판정 사항이라 만들지 않았다**(§13⑱ 계열). 별도 발주 요청.

---

## ④ 근거·pat 후보 상신 — `pipeline/evidence_candidates.mjs`

전문은 [`d146_evidence_candidates.md`](d146_evidence_candidates.md). **후보까지만이다. 적용 안 했다.**

| 세트 | 근거 누락 | pat 누락 | 비고 |
|---|--:|--:|---|
| **`2019_9월::l20199a`** | **16** | 0 | 발주 ④ 대로 최우선 — 형광펜 36% 의 원인 |
| `2019_9월::r20199c` | 8 | 1 | Q36#1 은 `_pat_error` 도 함께 (S-10 — 수리가 먼저) |
| `2019_9월::l20199e` | 6 | 0 | |
| `2020_6월::l20206b` | 3 | 1 | |
| `2020_6월::l20206c` | 1 | 2 | |
| **합계** | **34** | **4** | D-145 상신과 일치 (나머지 1건은 `l20206a` — 구간 대기 세트) |

점수는 **선지 어절과 본문 문장의 겹침 비율**이다. 조사 꼬리를 떼어 맞춘다.
형광펜이 안 되는 문장(각주·작가·표지·중략)은 후보에서 미리 뺐다 — `l20209d` 선례 반영.

### 후보가 약한 자리 (판정 필요)

`l20199a` **Q16#4 · Q16#5** — 최고 후보가 13~14% 이고 그마저 (다) 평론 문장을 가리킨다.
Q16 은 「(가)와 (나)의 **표현상 공통점**」이라 특정 문장 하나로 정박하기 어려운 유형이다.
**근거를 비워 두는 것이 맞는지, 대표 문장을 하나 고를지 판정을 청한다.**

---

## ⑤ 구간 3세트 — 대기 (발주 ⑤)

`r2019b` `[A]` · `l20206a` `[A][B]` · `l20206d` `[A][B]` — 화면 지목 대기 중. 손대지 않았다.

## ⑥ 9/2 중단 규율

유지한다. 9/2(수) 오후부터 이 트랙을 멈추고 스프린트 대기.
