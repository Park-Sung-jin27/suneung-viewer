# D-104 bracket 정박 수리 — ⓪③④ 완료 · ① 진행 중 (2026-08-25)

## ⓪ 분류 불일치 회신 — **심사관이 맞고 제가 틀렸습니다**

이전 보고의 「신규 21 · 기존 6」은 **오집계**였습니다. 세지 않고 출력 앞부분만 보고
어림한 것이 원인입니다. 정확한 값은 아래와 같고, **LIVE 7건은 심사관 목록과 정확히
일치**합니다.

`node pipeline/bracket_anchor_audit.mjs`

### 분모 정의 (심사관 「14세트」와 대조용)

| 구분 | 전체 | LIVE |
|---|--:|--:|
| A. 문항이 `[A]~[F]` 를 **참조**하는 세트 | 135 | 81 |
| B. workTag 있고 bracket 없음 = **결함** | **27** | **7** |
| C. bracket annotation **보유** | 32 | 29 |
| D. workTag 도 bracket 도 없음 | 76 | — |

제 「32세트」는 **C** 기준입니다. 심사관 14와 다르므로 기준을 밝혀 주시면 맞추겠습니다.

### 결함 27세트 — 노출 × 구간 교차

| | LIVE | 비노출 | 계 |
|---|--:|--:|--:|
| 기존 | **7** | 5 | 12 |
| 재추출 | 0 | 15 | 15 |
| **계** | **7** | 20 | 27 |

**「기존/재추출」과 「LIVE/비노출」은 다른 축입니다.** 재추출 43세트는 RELEASE_KEYS 에
없어 전부 비노출이므로, LIVE 7건은 **전부 기존 세트**입니다. 심사관 지적이 맞습니다.

LIVE 7건: `2015수능A l2015b`·`l2015d` / `2018수능 l2018a`·`l2018c` /
`2019수능 l2019b` / `2020수능 l2020c` / `2021수능 r2021b` — 목록 완전 일치.

## ③ 검증 축 ⑥ 추가 — **오탐 0**

`marker_anchor_check.checkBracketAnchored()` → `delegation_verify` ⑥축.

기존 데이터 396세트 전수:

```
검출 28건 = 전부 누락 27 + 라벨 일부 누락 1 (2023수능 l2023d 의 [F])
그중 LIVE 8
```

발주 기대값 27과 1건 차이가 나는데, `l2023d` 는 workTag `[F]` 가 있으나 bracket 에
`F` 라벨이 없는 경우입니다. 축 정의(「workTag 존재 && 해당 bracket 부재」)상 결함이
맞으므로 **오탐이 아닙니다.**

> ⓪ 집계에서 「라벨 일부 누락 2」(`l2024b [C]`, `l2023d [C][F]`)였던 것과도 차이가
> 납니다. ⑥축은 **workTag 로 존재하는 라벨만** 보는데, `l2024b [C]` 와 `l2023d [C]` 는
> 문항이 참조만 할 뿐 workTag 가 없어 대상이 아닙니다. 그쪽은 ⑤축·D-94 소관입니다.

## ④ pat 오판 3건 정정 — 완료

| 위치 | 전 | 후 | 근거 |
|---|---|---|---|
| `2017수능 r2017c` Q38#1 | R1 | **R2** | 청약·승낙 주체를 뒤바꿈. 해설 결론문도 "주체를 뒤바꾼"이라 씀 |
| `2021수능 r2021b` Q28#5 | R1 | **R4** | 해설 꼬리에 `[개념 혼합]` 이라 명시돼 있었다 — 자기 모순 |
| `2023수능 r2023c` Q12#3 | R1 | **R4** | 위약벌 성질을 손해배상 예정액에 적용 |

`pat` 필드만 변경. `analysis`·`ok`·`cs_ids` 무수정.
분리 게이트: **기존 변동 0** · 신규 CRITICAL 1건(0.1%).

## ① LIVE 7세트 수리 — **1세트 확정 · 6세트 미완**

### 확정: `2019수능 l2019b` (「일동장유가」, 5구간)

원본 지면(300dpi) 판독으로 범위를 확정했습니다.

| 라벨 | 범위 | 행수 | 시작 행 |
|---|---|--:|---|
| [A] | `l2019bs18` ~ `l2019bs18` | 1 | 그중에 전승산이 글 쓰는 양(樣) 바라보고 |
| [B] | `l2019bs19` ~ `l2019bs22` | 4 | 필담(筆談)으로 써서 뵈되… |
| [C] | `l2019bs23` ~ `l2019bs24` | 2 | 내 웃고 써서 뵈되… |
| [D] | `l2019bs25` ~ `l2019bs27` | 3 | 승산이 다시 하되… |
| [E] | `l2019bs33` ~ `l2019bs37` | 5 | 놀랍고 어이없어 종이에 써서 뵈되… |

### 진행 중: `2015수능A l2015b`

`[A]` 시작 = `l2015bs3`(심신이 황홀하여…), 끝 = `하니 그 소리 웅장하여 산천이 울리는지라.`
확정. `[B]` 는 페이지 경계 너머라 미확인.

### 자동화를 두 번 시도했고 두 번 실패했습니다

1. **세로선 좌표 검출** — 페이지 전체 세로선을 훑으니 단 구분선·표 테두리가 섞여 못 씀.
2. **라벨 인접 세로선 필터** — `get_drawings()` 가 구간 괄호를 **하나도 못 잡음**.
   괄호가 벡터가 아니라 폰트 글리프로 조판돼 있습니다.

그래서 **지면 렌더 → 눈으로 판독**(D-102 속미인곡과 같은 방법)으로 갑니다.
남은 6세트 9마커에 이미지 판독 10회 안팎이 필요합니다.

### 중요한 발견 — 「마커 다음부터 다음 마커 직전까지」 규칙은 **성립하지 않습니다**

`l2019b [D]` 가 반례입니다. workTag `[D]` 다음이 `s25`, 다음 마커 `[E]` 직전이 `s32`
인데, **실제 구간은 `s25~s27` 3행뿐**입니다(`s28~s32` 는 어느 구간에도 안 듭니다).

즉 **마커마다 원본에서 끝을 따로 확인해야 합니다.** 자동 추론은 오정박을 만듭니다.
