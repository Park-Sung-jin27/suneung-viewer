# 해설 본문 패턴 코드 잔재 조사 (발주 D-83 · 2026-08-21)

> **조사만 한다. 데이터는 손대지 않았다.** 정리 방침은 별도 발주 사항.

## 세는 방법

- 코드 사전은 `pat` 필드의 실제 값에서 수집했다: `L1` `L2` `L3` `L4` `L5` `R1` `R2` `R3` `R4`
- `V` 와 `0` 은 제외했다 — 한 글자라 본문의 보통 글자와 구분되지 않아 오탐이 된다.
- 앞뒤가 영문·숫자면 코드로 세지 않는다(예: `R10`, `AR1`).
- 유형은 **위치**로 가른다: 앞머리(맨 앞 12자 안) · 꼬리(맨 끝 30자 안) · 중간(그 밖).

## 규모

| 구분 | 검출 건수 | 선지 수 | 세트 수 |
|---|--:|--:|--:|
| 전체 | 0 | 0 | 0 |
| **노출(LIVE)** | **0** | **0** | **0** |
| 비노출 | 0 | 0 | 0 |

## 유형

| 유형 | 전체 | 노출(LIVE) |
|---|--:|--:|
| 앞머리(맨 앞 12자 안) | 0 | 0 |
| 꼬리(맨 끝 30자 안) | 0 | 0 |
| 문장 중간 삽입 | 0 | 0 |

## 서식

| 서식 | 전체 | 노출(LIVE) |
|---|--:|--:|

## pat 필드와의 중복 — 0/0건 (0.0%)

같은 코드가 `pat` 필드에도 있는데 본문에도 박혀 있으면 **본문 쪽이 잔재**다.

## 🔴 확정 방침 (2026-08-21) — 정리 실행 전 반드시 읽을 것

- 꼬리 4서식은 **일괄 제거**로 간다. 단 실행은 **(가) 렌더 정제 배포 뒤**, 별도 발주로 한다.
- 아래 **불일치 6건은 제외 목록**이다. 일괄 제거에 휩쓸리면 판정 근거가 사라진다.
  각 선지 내용을 읽어 수동 판정한 뒤에만 손댄다. (`pat` 쪽 기본값 오채움이 유력하나 미확정)
- `V`(216)·`0`(18)은 정리 발주에 포함하되, **문자열 매칭이 아니라 「꼬리 위치 + 구분 기호 동반」 조건으로만** 잡는다.
- `pat` null 3,515건은 **별도 백로그**. 이번 축이 아니다.

### 제외 목록 — 본문↔pat 불일치 0건

| 회차 | 세트 | 문항 | 선지 | 본문 코드 | pat 필드 | 노출 | 문맥 |
|---|---|--:|--:|---|---|---|---|

## 세트별

| 회차 | 세트 | 노출 | 건수 | 코드 |
|---|---|---|--:|---|

## 전체 목록

| 회차 | 세트 | 문항 | 선지 | 코드 | 유형 | pat | 중복 | 앞뒤 문맥 |
|---|---|--:|--:|---|---|---|---|---|
