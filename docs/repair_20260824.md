# 재추출 43세트 수리 기록 (2026-08-24, 발주 ①~⑥)

> 데이터는 `pipeline/reextract/step3/*/step4_result.json` 만 고쳤다.
> `data-source/all_data_204.json` 은 **무접촉**(세트 353 유지).
> 백업: `pipeline/reextract/_backup_20260824/`

## ① 줄바꿈 정리 — `sents.t`

판별 근거는 **원본 PDF** 다. 추측하지 않았다.

수능 국어 PDF 는 **어절 경계에서 줄이 바뀔 때 공백 문자를 줄 끝에 남기고, 어절 중간에서
강제 절단할 때는 남기지 않는다.** PyMuPDF 로 줄 끝 공백 유무를 읽어 판별했다
(`pipeline/pdf_line_ends.py` → `pipeline/newline_fix.mjs`).

표본 14/14 검증:

```
공백  "…그 북을" ⏎ "통해 자기의…"   →  "그 북을 통해"      ✅
붙임  "…떼밀어 조각" ⏎ "을 내었다."  →  "조각을 내었다"     ✅
붙임  "…며느리는, 차" ⏎ "라리 더…"   →  "차라리"           ✅
공백  "…민익태," ⏎ "한때는…"        →  "민익태, 한때는"    ✅
```

| 항목 | 값 |
|---|--:|
| 줄바꿈 지점 | 1,706 |
| 공백으로 (어절 경계) | 897 (52.6%) |
| 붙임으로 (어절 중간 절단) | 790 (46.3%) |
| **판별 불가 — 손대지 않음** | **19 (1.1%)** |

판별 불가 19건은 `docs/newline_unresolved_20260824.md` 에 전건 기록. **추측으로 메우지 않았다.**

### 검증 3요건

| 검증 | 결과 |
|---|---|
| 공백 외 문자 손실 | **0** — 1965/1965 문장이 공백 제거 후 원본과 완전 일치 ✅ |
| ⏎ 잔존 | 973 → **19문장 (1.0%)** = 기존 353세트와 동일 수준 ✅ |
| 표본 육안 | 10문장 확인, 8건 완전 정리 · 2건 판별 불가로 원형 유지 ✅ |

## ② cs_spans 재생성 — CS_SPAN_UNRESOLVED 695 → 25

`step4_csids.js --extract-spans` 는 **span 을 하나도 되살리지 못했다(+0)**. 이유: step4 가
만든 인용문 자체가 마커를 빼고 따옴표를 바꾼 「읽기 좋은 인용」이라 본문에 그대로 없다.

그래서 **재정박** 단계를 넣었다(`pipeline/respan_reextract.mjs`). 텍스트를 만들지 않는다 —
본문에서 그 구절이 실제로 있는 위치를 찾아 `span.text` 를 **그 자리 원문 그대로**로 맞춘다.

```
실패 695 → (줄바꿈 정리) 276 → (재추출) 267 → (재정박 242건 복구) → 남은 25
```

### 두 번 잡은 자체 결함

1. **인덱스 매핑 버그** — 따옴표를 `out` 에는 안 넣고 `idx` 에는 넣어 경계가 한두 글자씩
   밀렸다. 39건이 어긋난 span 이 될 뻔했다. `out`/`idx` 1:1 강제로 수정.
2. **`--extract-spans` 의 덮어쓰기** — analysis 인용문을 다시 찾다가 **다른 구절**로 덮었다
   (실측 3건: `"일인 주주가 회사의 대표 이사가…"` → `"일인 주식회사"`). 되돌리는 가드 추가.

### 최종 검증 — 오정박 0

| 구분 | 건수 |
|---|--:|
| 손대지 않음 | 1,039 |
| 표기만 달라짐 (정상) | 159 |
| 범위만 조정 (정박 위치 정확) | 6 |
| **내용이 달라짐 (오정박)** | **0** ✅ |

제거된 25건은 `docs/csspan_dropped_20260824.md` 에 전건 기록. `cs_ids` 는 유지되므로
문장 단위 근거는 남고, 사라지는 것은 문장 안 구절 강조뿐이다.

## ③ 옛한글 복원 (`hypua2jamo`)

`pipeline/oldhangul_restore.py`. glyph 육안 판독 없이 매핑표만 근거로 삼았다.

| 세트 | PUA | 제로폭 |
|---|---|---|
| `2020_9월 l20209a`(상춘곡) | 80 → **0** | 272자 제거 |
| `2015_6월B` | 22 → **0** | 0 |

### 잔존 4자 — 옛한글이 아니다 `[확인 필요]`

| 위치 | 문자 | 실체 |
|---|---|---|
| `2016_9월A l20169es41` | U+F675 | 각주 `암질(暗質) :▩어리석은…` 의 콜론 뒤. **PDF 원본에도 있다.** 공백 자리로 보이나 매핑표에 없음 |
| `2017_9월 r20179c` Q35 bogi | U+E06D · U+E000 · U+E001 | `<보기>`의 **분수 수식**(A·B로 열의 일당량). 옛한글이 아니라 수식 기호 |

`hypua2jamo` 대상이 아니므로 손대지 않았다.

## ④ analysis 내부 id 제거 — 35건, 잔존 0

`pipeline/idleak_clean.mjs`. 괄호로 감싼 것만 지운다.

```
"…새로운 감성과 감각이 일깨워진다." (r20199cs26)   → 괄호째 제거
…이야기를 꺼내고(l20146fs9~11), 장돌뱅이도…        → 괄호째 제거
"…맞았지요?" (지문 l20149es30~34)                  → 한글 라벨 포함형도 제거
```

괄호 밖에 박힌 id 는 문장이 깨지므로 손대지 않기로 했으나, **실제로 0건**이었다.
step3 프롬프트 자체의 수정은 발주대로 백로그.

## ⑤-1 극성역전 8건 — answer_key 대조 판정

**8건 전부 `ok=false` 가 맞고 해설 결론 표지 `✅` 가 틀렸다.** 정답키 + 발문 극성으로 기계 판정.

그런데 해설 **본문**까지 보면 두 부류로 갈린다.

### (A) 표지만 틀림 — 본문은 선지를 정확히 부정했다 (4건)

| 세트 | 본문 서술 |
|---|---|
| `2016_6월A l20166e` Q43#1 | "'단절'이 아니라 '연결'이 핵심이다… '단절'은 작품의 정서와 정반대다" |
| `2016_6월B l20166d` Q43#1 | "단절된 공간이 아니라… 세대 간 연속성을 담은 공간이다" |
| `2018_9월 r20189c` Q42#2 | "융합이 차단되어 가치의 일반화가 일어나지 못했다" |
| `2021_9월 l20219d` Q32#5 | "심봉사의 말이 끝난 후에 밝힌 것이다" (선지는 "끝나기 전에") |

→ 마지막 결론 표지만 `✅`→`❌` 로 바꾸면 본문과 일치한다.

### (B) 본문 서술도 선지를 긍정 = 해설 자체가 오답 (4건)

| 세트 | 본문 서술 |
|---|---|
| `2014_9월A l20149d` Q43#5 | "과제가 완수되었음을 확인할 수 있다… 원한이 해소되었다" |
| `2015_9월B l20159d` Q45#3 | "두 작품 모두 '옥 = 순수한 충심'… 공통점이 명확하다" |
| `2016_6월B l20166b` Q35#5 | "성규의 독자적 판단이 드러난다" |
| `2016_9월B l20169b` Q34#2 | "세 작품 모두 색채어를 통해… 부각하고 있다" |

→ 표지만 바꾸면 본문과 모순된다. **해설 재생성이 필요하다.**

수리는 이 구분을 보고한 뒤로 미뤘다.

## ⑤-2 U+FFFD 3건 — 원본 대조 수리 완료

`pipeline/fffd_repair.mjs`. **근거가 확정된 형태만** 고쳤다.

`pdftotext` 가 흘린 자리를 PyMuPDF 로 다시 읽으면 **U+F0854 / U+F0855**(SPUA-A) 쌍이다.
기존 353세트 관례가 근거다 — 책·문집·신문명은 `『』`(80회), 작품명은 `「」`(240회).
`『시경』` 은 기존 데이터에 3건 실재한다(`2016_9월A`·`2017_6월`).

| 위치 | 전 | 후 |
|---|---|---|
| `2014_9월B l20149ds4` | `▯▯▯시경▯▯▯에` | `『시경』에` |
| `2015_6월B l20156c` Q42 bogi | `문집 ▯▯▯효전산고▯▯▯를` | `문집 『효전산고』를` |
| `2018_9월 l20189d` Q43#4 | `펼▯쳐진다` | `펼쳐진다` (끼어든 낱자 제거) |

미해결 0건.

---

## 자체 발견 — 발주 범위 밖 `[확인 필요]`

**줄바꿈이 `sents.t` 에만 있는 게 아니다.** 발주 ①이 `sents.t` 를 지정해 그것만 고쳤으나,
발문·선지·보기에도 같은 PDF 조판 줄바꿈이 남아 있다.

| 필드 | 신규 43세트 | 기존 353세트 |
|---|--:|--:|
| `sents.t` | 19/1965 (1.0%) ✅ | 110/10874 (1.0%) |
| `questions.t` | **47/156 (30.1%)** 🔴 | 9/1368 (0.7%) |
| `choices.t` | **587/780 (75.3%)** 🔴 | 139/6840 (2.0%) |
| `questions.bogi` | **44/44 (100%)** 🔴 | 131/1312 (10.0%) |
| `choices.analysis` | 780/780 (100%) | 6800/6840 (99.4%) — **정상**(해설은 문단 구분에 줄바꿈을 쓴다) |

실례 (`2015_6월B` Q42 bogi):

```
조선 후기 사대부 심노숭의 문집 『효전산고』를 보면, 종로⏎의 담배 가게에서 ｢임경업전｣을
낭독하는데, 김자점이 장군⏎에게 죄를 씌워 죽이는 데 이르자…
```

병합하면 **발문·선지·보기가 화면에서 중간에 끊겨 보인다.** `newline_fix.mjs` 를 이 필드까지
확장하면 바로 처리 가능하나, 발주 범위 밖이라 **실행하지 않았다.**

## 만든 도구

| 파일 | 역할 |
|---|---|
| `pipeline/pdf_line_ends.py` | PDF 줄 끝 공백 정보 추출 (판별의 원본 근거) |
| `pipeline/newline_fix.mjs` | `sents.t` 줄바꿈 정리 |
| `pipeline/respan_reextract.mjs` | cs_spans 재추출 + 재정박 |
| `pipeline/oldhangul_restore.py` | 한양PUA → 첫가끝 자모 |
| `pipeline/idleak_clean.mjs` | analysis 내부 id 제거 |
| `pipeline/fffd_repair.mjs` | U+FFFD 복구 |

---

## ⑥ 분리 게이트 재판정 — **4.7%, 병합 조건 충족**

```
기존만  세트 353 · 선지 6840 → CRITICAL  487
신규만  세트  43 · 선지  780 → CRITICAL   37
병합본  세트 396 · 선지 7620 → CRITICAL  524

기존 변동 = 524 − 487 − 37 = 0  ✅
신규 CRITICAL 위반율 = 37/780 = **4.7%**  (기존 7.1% · 정지선 15%)  ✅
```

수리 전 **798건 · 102.3%** → 수리 후 **37건 · 4.7%**. 기존 평균(7.1%)보다도 낮다.

### 잔여 37건 소명

| 코드 | 건수 | 소명 |
|---|--:|---|
| `C_anchor_exact_fail` | 20 | 아래 |
| `OK_ANALYSIS_CONFLICT` | 8 | ⑤-1 판정 완료 — 수리 승인 대기 |
| `F_content_reversed` | 8 | **위 8건과 동일 선지의 이중 계상.** 실질 8건 |
| `CS_ALL_NONHIGHLIGHTABLE` | 1 | `2020_9월 l20209d` Q42-[1] — cs_ids 가 전부 각주/중략 문장 |

실질 위반 선지 수는 **29개**(37 − 이중계상 8).

### `C_anchor_exact_fail` 20건 — 줄바꿈이 가리고 있던 결함이 드러난 것

수리 전 1건 → 수리 후 20건. **내가 만든 결함이 아니다.** 게이트 코드(1757행)가 근거다:

```js
if (ms.sentType === "verse" || region.includes("\n")) continue;   // 운문 정상으로 보고 통과
```

`sent.t` 에 `\n` 이 있으면 이 축은 **운문일 수 있다고 보고 그냥 넘어간다.** 줄바꿈이 49.5%
있던 동안 축이 눈을 감고 있었고, 정리하자 비로소 작동해 **원래 있던
`sent.t` ↔ analysis 인용문 공백 불일치**를 잡아냈다.

표본 8건 육안 확인:

| 문장 | 정리 결과 | 판정 |
|---|---|---|
| `l20146ds31` | `것이⏎라면야` → `것이라면야` | 본문 맞음 — analysis 인용문이 `것이 라면야`(옛 형태) |
| `l20146fs6` | `이러한⏎생활` → `이러한 생활` | 맞음 |
| `l20159ds43` | `모함으로⏎유배를` → `모함으로 유배를` | 맞음 |
| `l20159bs55` | `지구의⏎절반을` → `지구의절반을` | 🔴 **내 판별 오류 — 띄어야 한다** |
| `r20199cs34` | `때마다⏎변화하는` → `때마다 변화하는` | 맞음 |
| `l20209bs23` | `서대주⏎노비` → `서대주노비` | 🔴 **내 판별 오류 — 띄어야 한다** |
| `l20229cs9` | `대대⏎손손이` → `대대손손이` | 맞음 |
| `l20169cs29` | `묻자⏎아들은` → `묻자 아들은` | 맞음 (앞의 `도도 가` 는 정리 전부터 있던 공백) |

표본 8건 중 **내 판별 오류는 2건**. 나머지는 analysis 인용문 쪽이 정리 전 형태다.
어느 쪽이 옳은지는 건별 PDF 대조가 필요하므로 **수리하지 않고 남겼다.**

> 제2 근거(analysis 인용문)로 자동 교정하는 도구를 만들었으나 **이중 공백을 만드는
> 오작동**을 확인하고 폐기했다(dry-run 단계라 데이터 무피해).

---

# 2차 발주 (①~⑥ + 백로그) — 병합 완료

## ① 발문·선지·보기 줄바꿈 정리

`newline_fix.mjs --fields`. `sents.t` 와 **같은 방법·같은 검증**을 썼다.
`choices.analysis` 는 제외했다 — 해설이 문단을 줄바꿈으로 나누는 것은 정상이고
기존 353세트도 99.4%가 그렇다.

| 필드 | 전 | 후 | 기존 353세트 |
|---|--:|--:|--:|
| `sents.t` | 1.0% | **1.0%** | 1.0% |
| `questions.t` | 30.1% | **0.0%** | 0.7% |
| `choices.t` | 75.3% | **0.0%** | 2.0% |
| `questions.bogi` | 100% | **6.8%** | 10.0% |

전 필드가 기존 수준 이하다. 판별 불가 39건은 `\n` 원형 유지.

## ② 극성역전 8건

**(A) 4건** — `polarity_fix.mjs`. 결론 표지 `✅`→`❌` 만 교체, 본문 무수정.

**(B) 4건** — `polarity_regen.mjs`. 43세트를 만든 바로 그 함수
(`step3_analysis.js` 의 `reanalyzeSingleChoice`)를 호출했다. 프롬프트·모델·시스템
프롬프트·형식 전부 무변경 — `export` 한 단어만 열었다. **4/4 채택**.

```
전: ✅ 선군이 …과제 완수 단계이며, 이로써 낭자의 원한이 해소되었다
후: ❌ ㉤은 진범 단서 발견 단계일 뿐, …과제 완수 단계는 [B] 이후다 [단계 혼동]

전: ✅ 백옥의 상징을 통해 변치 않는 충절을 형상화하는 공통점이 명확하다
후: ❌ [나]의 '옥 같은 얼굴'을 화자의 마음으로 오독한 함정 [비유 대상 혼동]
```

재대조(결론 표지 ↔ `ok`) 통과분만 채택하고, 실패 시 원본을 유지하도록 했다.
후처리로 꼬리 패턴 코드(`[L4]` 등)를 떼어 D-85 재오염을 막았다.

## ③ `C_anchor_exact_fail` 20건 건별

자동 판정 근거가 **셋 다 실패**했다:

- PDF 줄 끝 공백 — 1차 근거지만 줄 매칭이 어긋난 자리가 바로 이 20건이다
- analysis 인용문 — step3 는 줄바꿈 자리에 **항상 공백을 넣는다**. 근거가 못 된다
  (`것이⏎라면야` 를 `것이 라면야` 로 인용)
- 기존 353세트 코퍼스 — 958k자로 표본 9개 중 8개 미출현

그래서 **건별로 판정**했다. 두 방향이다:

**(가) 본문이 틀림 → `sent.t` 정정 (12곳)**

`봉건적성장과`·`지구의절반을`·`있었다고주장하였다`·`집단적으로공유하면서`·
`서대주노비`·`흑공단두루마기`·`그처럼잘난`·`위풍이 헌앙한짐승이라`·`삽니다아 ―부서진`
→ 띄움 / `도도 가`·`근대 도시 는`·`도요새 의` → 붙임

**(나) 본문이 옳고 인용문이 틀림 → 인용 재정박 (8곳)**

`대대손손이`·`것이라면야`·`이렇듯이`·`못내` 는 한 단어다. 마커 뒤 한 칸(`㉮ 정신적`)은
본문 관례다. 이쪽은 analysis 인용을 본문 원문으로 맞췄다.

결과: **20건 → 0건**.

## ④ 잔여 소명 → `docs/residual_soname_20260824.md`

## ⑤ 최종 재판정 — **0.1%** · 병합 실행

```
기존만  353세트 · 6840선지 → CRITICAL 487
신규만   43세트 ·  780선지 → CRITICAL   1
병합본  396세트 · 7620선지 → CRITICAL 488

기존 변동 = 488 − 487 − 1 = 0  ✅
신규 위반율 = 0.1%  (기존 7.1% · 정지선 15%)  ✅
```

수리 전 798건(102.3%) → **1건(0.1%)**. 남은 1건은 `CS_ALL_NONHIGHLIGHTABLE`
(④에 소명 — 근거로 삼을 지문 문장이 존재하지 않는 부재 논증형 선지).

### 병합 후 확인

| 항목 | 결과 |
|---|---|
| 계수 | 세트 353→**396**(+43) · 문항 1368→**1524**(+156) · 선지 6840→**7620**(+780) |
| `build:split` | 43회차 **241세트** — 병합 전과 동일 ✅ |
| `RELEASE_KEYS` | **무접촉** — `git status src/` 변경 0 ✅ |
| free/index 복합키 | RELEASE_KEYS 241 = index 241 · 유출 0 · 누락 0 ✅ |
| LIVE 실측 | `www.jippi.kr` free/index.json **241세트** · 회차 8세트 응답 200 / 141ms ✅ |

## 수리 중 잡은 자체 결함

1. **가드 키 충돌** — 한 선지가 같은 `(sent_id, occurrence)` 로 span 을 3개 가진
   경우(`l20189c` Q36#2), Map 이 셋을 하나로 뭉개 되돌릴 때 서로 다른 근거가 같은
   구절로 덮였다. dedup 이 1개로 줄여 **형광펜 2개가 조용히 사라졌다.**
2. **`--extract-spans` 를 기본에서 제거** — span 을 추가해 배열 인덱스를 밀어내고
   같은 자리를 다른 구절로 덮는다. 실측 이득은 +10건뿐이라 뺐다.
3. **중복 span 366건** — `mergeSpans` 는 추가만 하므로 도구를 두 번 돌리면 쌓인다.
   중복 제거 단계를 넣었다.
4. **제2 근거 자동교정 도구 폐기** — 이중 공백을 만드는 오작동(dry-run 단계, 무피해).

### 최종 데이터 상태

`cs_spans` 1,668 · 중복 **0** · 매칭 실패 **0**
재정박 검증(집합 대조, 순서·제거에 강건): 그대로 1,423 · 표기만 238 · **오정박 0** ·
사라진 span 9

## 재현

```
bash pipeline/repair_all.sh          # 원본에서 9단계
node pipeline/merge_reextract.mjs --apply --probe   # 판정만
```
