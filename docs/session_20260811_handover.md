# 세션 핸드오버 — 2026-08-11 (발주 bo ~ dl)

작성: 데이터 엔지니어(Code B) / 발주 cv[3] → dk[4] 전면 갱신 → dl[4] 최종 확정
기준 커밋: `origin/main` = `68a3431` · **세션 push 45회 전량 fast-forward · 강제 push 0**
운용지침: **§7-1 ~ §7-26** (결번 없음, 387행)

---

## 0. 다음 세션 첫 3개 — 발주 dl[4]

### 1️⃣ 게이트 사각 4종 수리

```
따옴표 짝 어긋남   82건   haesol_v2_gate.mjs:346  /"([^"]{12,})"/g
운문 ' / ' 조각 대조 86건
choice.t 미조회     88건
q.t 미조회           3건
```
**정본 판본 = `codex/gates-20260811-dc`(`aa40151`) — 병합이 선행돼야 한다.** 사각 4개를 따로 고치면 회귀 관리가 4배가 된다. fixture는 `pipeline/fixtures/gate_quote_pairing.json`에 고정돼 있다(양성 회귀 + 음성 대조).

### 2️⃣ 지침 통합

`keep/docs-20260811`(`5a92f5f`) 미병합 + 누락 규칙 16건 복원(원문 `docs/archive/cx-4_legacy_rules_verbatim_20260811.md`).
**대표 결정 1건 — §13⑰ 검수 등급제.**

### 3️⃣ `l2021d` Q44 마커 5개 정박 (LIVE)

**본문은 이상 없음이 확인됐다**(dk[2] — sents 47개 중 45 일치, 나머지 2건은 출전 표기). `r2021c`와 같은 방식(PDF 위치 그대로 삽입)으로 처리하면 된다.

---

## 0-A. 이번 세션 최종 상태

- **해설 본문 붕괴 13선지 전건 해소** — 7세트 13선지. `analysis_token_corruption` 관문(보존 원문 기준) **13/13**, 현행 데이터 후보 **0**. 2026-06-15 P0 시리즈(#1~#6)가 남긴 결손이 두 달 만에 정리됐다.
- `r20199a` Q23 — `<보기>` 표 PDF 복원 + 해설 5선지 재작성 완료.
- `r2021c` Q36 — ㉠ 정박 + 해설 2선지 재작성 완료.
- 운용지침 §7-11 ~ §7-26 신설(16개 조).

---

## 0-B. 나머지 미결

### 0-1. LIVE 미결

| 대상 | 상태 | 다음 조치 |
|---|---|---|
| `l2021d` Q44 | 본문 전건 일치 확인됨(dk[2]). **마커 ㉠~㉤ 5개 미정박** | 위 3️⃣ |

`r2021c` Q36은 dl[3]에서 **완결**됐다(정박 + 재작성).

### 0-2. 비노출 미결

| 대상 | 상태 |
|---|---|
| `l2015a` Q32 · `r20176b` Q24 | 지문 구간에 마커 없음 — **재확인 필요**(PDF 전체엔 있으나 화작 문항 것) |
| `r2019b` Q26 · `r2016fB` Q32 | **추출 결손 확정, 복원 가능** (지문 구간에 각 1회씩 실재) |
| `l2016b` Q31 | 본문 PDF 전건 일치 확인(dk[2]) — 마커 ㉢ 미정박만 남음 |
| dg[3] 비노출 14선지 | 공개 전 처리. 이번 세션 범위 밖 |

### 0-2b. 🔴 환각 인용 (미착수 — 규모 미확정)

`2014_6월A::l20146a` Q40 **c2·c4·c5**에 `[A]` 근거 환각 **5건** 확인(비노출).

```
c2  «어디다 하소연하며 어느 곳에 하직할꼬»
    «원통하고 분한 마음 어찌 말로 하오리까»
c4  «산 너머 산» · «물 건너 물»
c5  «내 몸의 있는 죄를 백골이 진토 되어» / «원통하고 분한 마음»
```

모두 「시집살이 노래」 27줄과 속미인곡 `<보기>` 어디에도 없습니다. `haesol_v2` §2 FAIL 5가 이것을 가리키고 있었습니다.

**★ §7-11에 따라 규모 확정 전 개별 수리에 착수하지 않는다.** 붕괴 축과 같은 실수를 반복하지 않으려면 전수 스캔이 먼저입니다.

### 0-2c. `src/` 소스 주석 붕괴 7곳 (기록만, 사용자 영향 0)

```
Landing.jsx:10 · PassagePanel.jsx:7·228·291 · QuizPanel.jsx:237·479 · WrongNote.jsx:145
```
별도 위생 발주 대상. 이번 세션 무접촉.

### 0-2d. ✅ 인용 대조 축 — **종결** (발주 dr~dw-2)

**확인된 진짜 환각은 4건뿐이며 전부 비노출이다.**
```
2014_6월A::l20146a Q40  «산 너머 산» · «물 건너 물» ·
                        «어디다 하소연하며 어느 곳에 하직할꼬» ·
                        «내 몸의 있는 죄를 백골이 진토 되어»
미확정 1건  2021_9월::l20219b Q39c5
```

L1 표본 8건(dr 5 + du 3) 전수 대조 결과 **진짜 환각 0건** — 전부 검사기 오탐이었다. 오탐 6유형(마커 삽입 · 조사 부착 · sents 미탐색 · bogi 미탐색 · 각주 기호 · 위첨자)을 수리했고, 남은 것은 인용 표기 규약 위반이다.

**규약이 확정됐다** — [docs/haesol_quality_standard.md](docs/haesol_quality_standard.md).
📌 근거 블록만 원문 연속 문자열을 요구하고, 🔍 해설 블록은 검사 대상이 아니다. 게이트 §2 스코프를 📌 블록으로 축소했다.

```
📌 블록 인용 전건        6,076
해소                     5,494
잔여(규약 위반 후보)       582   9.6%
   그중 §2 FAIL 축         292   (나머지는 다문장·말줄임표 강등)
```
잔여를 **"환각"이라 부르지 않는다.** 대다수는 내용이 맞고 표기만 위반이다.

**★ 재개 조건**: 대표 검수에서 "원문에 없는 내용"이 다시 지적될 때만 재개한다.
**검사기 수치가 늘었다는 이유로는 재개하지 않는다.**

### 0-3. 도구·게이트

- 게이트 2파일 `codex/gates-20260811-dc`(`aa40151`) **병합 대기**
- 축5 공개 게이트 **미연결** · 후보 = 붕괴 13선지와 동일 건 (전건 해소됨)
- **게이트 사각 4종 수리 대기** — 따옴표 짝 82 · 운문 `' / '` 86 · `choice.t` 미조회 88 · `q.t` 미조회 3 (fixture `pipeline/fixtures/gate_quote_pairing.json`에 고정)
- `haesol_v2`는 **참고 도구**(§7-23) — PASS 75 / FAIL 166(69%)

### 0-4. 지침

- `keep/docs-20260811`(`5a92f5f`) **미병합** · 80커밋 중 지침 수정 이력 0
- 누락 규칙 16건 원문 → `docs/archive/cx-4_legacy_rules_verbatim_20260811.md`
- 복원 초안 = 심사관 · **대표 결정 1건**(§13⑰ 검수 등급제)
- **§7-26 미수신** — 발주 dk[4]가 "위 전문 그대로"만 적고 조문 블록을 싣지 않아 추가하지 못했다(§7-18 자족성)

---

## 1. 이번 세션에 확정된 것

### 1-1. 해설 생성 결함의 근본 원인 특정 + 3단 방어

대표 검수에서 `r20206d` Q40·Q41·Q42 해설의 실질 오류가 지적됐고, 그중 **Q40-①(`ㄷ`=핵 존재)·Q40-④(`ㄴ`=소포체·`ㄹ`=엽록체)는 환각**이었다. 원인은 추측이 아니라 코드로 특정됐다 — `pipeline/step3_analysis.js:653` 변별 재생성 경로가 **`bogi`를 프롬프트에 넣지 않았다**. Q41이 통과한 이유는 선지 `t`에 내용이 실려 있었기 때문이고, Q40은 선지가 `"ㄱ, ㄷ"` 기호뿐이라 모델이 지어냈다.

방어를 3단으로 세웠다.

| 단 | 무엇 | 위치 | 상태 |
|---|---|---|---|
| ① | 변별 재생성 프롬프트에 `bogi` 전달 | `step3_analysis.js:654` | 커밋 `c3fc411` |
| ② | `checkPromptInputs()` — 프롬프트 입력 결손 시 **문항 단위 skip** | `step3_analysis.js` export | 커밋 `76fd364` |
| ③ | `checkMarkerAnchored()` — 마커 미정박 시 **세트 단위 skip** | `step3_analysis.js` export | **미커밋**(§4 참조) |

검사 3종: `bogi_missing`(문항에 `<보기>`가 있는데 프롬프트에 없음) / `combo_def_missing`(선지가 마커 나열뿐인데 마커 정의가 프롬프트에 없음) / `range_def_missing`(발문이 범위 선언인데 정박 문장이 프롬프트에 없음).

★ **이 3단은 "생성 전 입력이 갖춰졌는가"만 본다. 생성된 내용이 맞는가는 보지 못한다.** 조용한 날조는 자동 탐지 대상이 아니라는 결론을 cu[2]에서 확정했고, 그래서 추가 매처 개발을 중단했다. **대표 검수가 유일한 방어선**이라는 §13⑰ 판단은 그대로 유효하다.

### 1-2. 신설 검사 도구 (전부 `--regress` 양성 회귀 보유)

| 도구 | 무엇을 보는가 | 회귀 | 실측 |
|---|---|---|---|
| `pipeline/marker_audit.mjs` | 마커 4축 — A 커버리지 결손 / B 계열 불일치 / C 번호 충돌 / D 역할 충돌 | 픽스처 4건 | **A=0 B=9 C=0 D=0** |
| `pipeline/extractor_corpus_check.mjs` | 추출기 수정의 전 조판 A/B (픽스처만으론 안전 보장 실패를 2회 실증) | 49종 기준선 | **1560/1643** |
| `pipeline/reference_deficit_scan.mjs` | 마커 나열 선지의 인용·괄호설명 대조(규칙 1) / 오귀속(규칙 2) | 3/3 | 검출률 1.19% (92건/32문항) |
| `pipeline/internal_leak_check.mjs` | 내부 어휘 누출 15종 | fixture 1 | LIVE 2선지 |

★ **`reference_deficit_scan` 표본 20건 중 약 80%가 오탐이다.** 92건 목록(`docs/reference_deficit_candidates_20260811.md`)은 **동결 상태이며 작업 지시가 아니다** — cu[2]에서 ①③ 불채택 결정됨.

### 1-3. 추출기 안전장치

`step2_extract.js`에 커버리지 결손 차단(throw) + fallback 열화 방지(`3f0be14`). `pdf_text_extractor.mjs`는 `MARKER_SETS` 단일 출처화 + `readerSanity()`(임계 50‰) + `--selftest` 5케이스. `LEADING_CIRCLED_RE`는 **①~⑤ 전용으로 롤백된 상태**(ⓐ~ⓔ를 leading 인정했더니 표·짝지음 항목행이 선지로 오인돼 전 코퍼스 47세트 악화 — cc에서 되돌림).

---

## 2. 이번 세션에 **틀렸던** 것 (반복 방지용 — 삭제 금지)

같은 실수를 다음 세션이 되풀이하지 않도록 원문 그대로 남긴다.

1. **`marker_audit` 유니코드 오기** — `㉠`을 `0x3220`(=`㈠`, 다른 문자)으로 적어 전 코퍼스가 `?` 계열로 분류 → 축 B 79건이 통째로 무의미했다. **U+3260~326F가 정답.** 양성 회귀를 먼저 돌렸기에 발견됐다.
2. **`.mjs`만 grep해 `.js` 계열 도구 7개를 통째로 놓침** → 블로커 2건 오보, 심사관 기각. **도구 부재를 보고하기 전에 `.js`/`.mjs`/`.cjs` 전 확장자 확인.**
3. **셸 경유 정규식 파손이 하루 3회** (§13⑮(7)) — `node -e`에 정규식·한글 경로를 넣으면 이스케이프가 깨지고, 그때 나오는 "0건"은 clean이 아니라 **검사기가 죽은 것**이다. Write로 스크립트 파일을 만들어 실행할 것. Windows에서 `^`는 이스케이프 문자라 `<sha>^`가 소실된다 → `~1` 사용.
4. **파이프 뒤 `$?` 오독** — `| tail`의 종료코드를 읽어 exit 1을 0으로 착각. **회귀 결과는 파이프 없이** 측정.
5. **워크트리 게이트 오염** (cq[0]) — `haesol_v2_gate.mjs` +138/-12, `quality_gate.mjs` +45가 미커밋 상태로 얹혀 있었다. **게이트 기준이 바뀐 채로 나온 PASS는 무효**이므로 `origin/main` 판으로 되돌려 재검증(PASS 유지 확인) 후에야 커밋했다.
6. **분모 오판** — "bogi 6/36"이라 보고했으나 `<보기>` 문항은 6개가 전부였다(유실 0). 분모를 전체 문항 수로 잡은 오류.
7. **자체 매처 3회 상반** (0건 → 26건 → 19건) → 최종 **PDF 육안 대조 11건**으로 확정. "실질 오류 0건"을 그대로 올렸다면 `r2020e`·`l20239c`가 방치됐다.

---

## 3. 미완 — 다음 세션이 이어받을 것

### 3-1. 대표 검수 대기 (LLM 필요, push 전제 조건)

| 대상 | 내용 | 상태 |
|---|---|---|
| `l20229a` Q19 | 선지 ②③⑤ 재작성 | 미착수 |
| `r20236b` Q7 | 선지 ④ 재작성 | 미착수 |

★ **순서 고정**: 재작성 → 해설 전문을 보고에 그대로 게재 → **대표 재검수** → 그 후에 push. cn·co에서 이 순서를 어겨 지적받았다.

### 3-2. 데이터 커밋 `2791c26` — 묶여 있음

`2791c26`(데이터)은 **단독 push 금지**. `l20229a` Q19·`r20236b` Q7 재작성 완료 후 동반 출고한다. 이 커밋은 `fix/cs-leak`·`keep/cs-data` **두 브랜치에만 존재**하므로 두 브랜치 **삭제 금지**.

### 3-3. 게이트 편입 보류분

`internal_leak_check`의 게이트 축5/축6 정렬 후 편입(ct[4]에서 보류). `reference_deficit_scan`은 오탐률 때문에 **편입 대상 아님**.

### 3-4. 축 B 잔여 9건

`r20146b` Q23 `Ⓒ` 미확정 3건 포함.

### 3-5. 마커 정박 결손 9세트 (발주 cy[4] — 수정·재생성 금지, 기록만)

발문이나 선지가 마커를 참조하는데 그 마커가 본문·`<보기>` 어디에도 정박돼 있지 않은 세트다. `reanalyze_vocab.mjs`가 이 9건을 skip한다 — **막히는 것이 옳다.** 정박 없이 생성하면 `r20206d` Q40과 같은 환각이 나온다.

| 세트 | 문항 | 미정박 마커 |
|---|---|---|
| `r2015aB` | Q20 | ㉠㉡㉢㉣㉤ |
| `l2015cB` | Q44 | ㉠㉡㉢㉣㉤ |
| `r2015d` | Q30 | ㉠㉡㉢㉣㉤ |
| `l2015c` | Q44 | ㉠㉡㉢㉣㉤ |
| `r2016aB` | Q20 | ⓐⓑⓒⓓⓔ |
| `r2017c` | Q42 | ⓐⓑⓒⓓⓔ |
| `l2017c` | Q44 | ㉠㉡㉢㉣㉤ |
| `l2021d` | Q44 | ㉠㉡㉢㉣㉤ |
| `l20196b` | Q31 | ⓔ — 성격이 다르다. `<보기>` 마지막 글자가 `ⓔ`이고 **그 뒤 정의 내용이 잘려 있다**(bogi 길이 572, `ⓔ` 위치 571). 위 8건은 마커가 통째로 없는 경우이고, 이건 마커는 있는데 내용이 절단된 경우다. |

복원은 PDF 대조가 선행돼야 한다 — 추론으로 마커를 배치하면 안 된다. `l2017a` 본문 누락, `l2015bB` (나) 산문 누락과 같은 성격의 결손이다.

### 3-5b. 발주 cr 「대조불가 2건」 — 종결 (발주 cz[3])

cr 이후 성격을 특정하지 못해 유보돼 있던 2건을 cy[3] 실측으로 종결한다. **수정 없음.**

| 건 | 종결 사유 | 처리 |
|---|---|---|
| `l20196b` Q31 | `<보기>` 꼬리가 `ⓔ` **1자**이고 정의 내용이 없다(bogi 길이 572, `ⓔ` 위치 571). `anchorOf`가 반환하는 정박 문자열이 1자라 유효 최소 4자에 미달한다 — 검사기 한계가 아니라 **데이터 결손 확정**. | §3-5 표에 편입. 막히는 것이 옳다. |
| `r20169c` Q29 | `<보기>` 본문 인라인 정박이었고, cy[3]의 `anchorOf` 확대로 풀린 6건에 포함됐다(`r2023a` Q2 · `r20239a` Q2 · `r20256a` Q3 · `r20266a` Q3 · `r20169c` Q29 · `l20179b` Q23). | 해소. 추가 조치 불요. |

「대조불가」 상태는 이로써 0건이다.

### 3-5c. `l20229a` Q21 근거누락 4건 — 백로그 (발주 db[4] / dd-2[3])

`l20229a` Q21c2 · Q21c3 · Q21c4(2건). **이번 범위 밖 유지, 수정 금지.**

★ 이 4건만 따로 읽으면 안 된다. 전역 맥락은 이렇다:

> `haesol_v2` 근거누락은 **전역 190건 / 71세트**이며 `l20229a` Q21이 그중 4건이다.
> 단, 이 190건에는 게이트 사각(운문 `' / '` 인용 · 공백 차이 · `<보기>` 미조회 계열)에서
> 온 오탐이 섞여 있을 수 있으며, dd-2[2] 측정 전까지 결함으로 확정하지 않는다.

dd-2[2] 실측(2026-08-11, `RELEASE_KEYS` 241키 전수, §2 FAIL 인용 814건 기준)에서
게이트 사각이 최소 **259건(31.8%)** 확인됐다. 근거누락 축은 별도 축이라 이 비율이
그대로 적용되지는 않으나, "검출 = 결함"이 아님은 같다(§7-23).

### 3-5d. 해설 본문 붕괴 13선지 — 규모·유입 (발주 de[1]/de[2] → dn 정정)

한글 해설 안에 치환 토큰(`path` · `사양` · `정합` · `안`)이 조사 없이 박혀 문장이 끊긴 상태다. **7세트 13선지**이며 그중 11선지가 LIVE, 2선지가 비노출이었다.

| 세트 | 선지 | 노출 |
|---|---|---|
| `2023수능::r2023a` | Q2c4 | 🔴 LIVE |
| `2015수능A::l2015b` | Q36c4 · Q36c5 | 🔴 LIVE |
| `2020수능::l2020c` | Q35c2 · Q35c3 | 🔴 LIVE |
| `2021_9월::l20219b` | Q38c1 · Q38c3 | 🔴 LIVE |
| `2021수능::r2021c` | Q36c4 · Q36c5 | 🔴 LIVE |
| `2021수능::l2021c` | Q40c1 · Q40c5 | 🔴 LIVE |
| `2014_6월A::l20146a` | Q40c1 · Q40c3 | 비노출 — dn 에서 추가 확인 |

**규모 — 1차 확정이 무효였던 경위**: de[1]은 "6세트 11선지가 전부"로 확정했으나, dn에서 `l20146a` 2선지가 추가로 나왔다. 분모(6,820)나 스코프 문제가 아니라 **매처 신호 집합이 좁았던 탓**이다 — `사양으로`·`사양은`처럼 **조사가 붙은 형태**와 `사양 정합` 조합이 신호 목록에 없었다. R3(세트 원문 무근거) 축을 추가해 재스캔한 뒤 7세트 13선지로 확정했다.

★ **§7-10의 (a)관문은 이미 알던 케이스만 검증하므로 이런 결함을 잡지 못한다.** 관문을 통과한 매처라도 "안 잡히는 형태"는 남는다.

**유입 시점 — 단일 사고 (결론 유지)**:
- **2026-06-15 `P0 정답 교정` 시리즈 #1~#6** — `#1 l20146a Q40`(`f540407`) · `#2 l2015b Q36`(`2c4fec4`) · `#3 l2020c Q35`(`6c0c6f6`) · `#4 l20219b Q38`(`894ed9f`) · `#5 r2021c Q36`(`3f7884a`) · `#6 l2021c Q40`(`ff3c5fb`). 정답 교정과 함께 해설을 재작성하면서 유입됐다.
- `r2023a` Q2c4는 별개 건 — **2026-05-21 `9886d27`**.
- `P0 #7 l2021d Q44`(`d276d0b`)도 같은 사고였으나 **2026-08-06 `7be1736`에서 이미 해소**됐다.
- **`#1`을 놓쳤던 것이지 다른 시점의 별개 사고가 아니다.** "단일 사고" 결론은 유효하다.

**침투 범위**: 본문 `sents.t`(10,880) · 선지 `choice.t`(6,840) · 발문 `q.t`(1,368) **전수 신호 0**. 해설 `analysis`와 `src/` 소스 주석 두 곳뿐이다(심사관 실측).

실측 원문은 [pipeline/fixtures/analysis_token_corruption.json](pipeline/fixtures/analysis_token_corruption.json)에 **재작성 전 원문**으로 보존. 검사 도구는 [pipeline/analysis_token_check.mjs](pipeline/analysis_token_check.mjs)(참고 도구, §7-23).

★ 게이트 5종 어느 것도 이 붕괴를 잡지 못했다. 형식축만 보기 때문이다(§7-7).

### 3-5e. 해설 본문 붕괴 13선지 — 처리 결과 (발주 df~dh·do)

| 세트·선지 | 상태 |
|---|---|
| `l2015b` Q36 ④⑤ | ✅ 재작성 완료 (df[2] 1차) |
| `l2020c` Q35 ②③ | ✅ 재작성 완료 (dg[1] 2차) |
| `l20219b` Q38 ①③ | ✅ 재작성 완료 (dg[1] 3차) |
| `l2021c` Q40 ①⑤ | ✅ 재작성 완료 (dh[2] 4차) |
| `r2023a` Q2 ④ | ✅ 재작성 완료 (dh[2] 6차) |
| `r2021c` Q36 ④⑤ | ✅ 재작성 완료 (dl[3]) — 5차 생성이 `checkMarkerAnchored=false`로 한 번 차단됐고, dk[3]에서 ㉠ 정박 후 완결 |

| `2014_6월A::l20146a` Q40 ①③ | ✅ 재작성 완료 (do, 커밋 1ed47fa) — 비노출 |

**→ 관문(보존 원문 기준) 13/13 · 현행 데이터 후보 0. 붕괴 13선지 전건 해소.**

### 3-5f. `7be1736` 선례의 결함 (재사용 금지 — 발주 df[1])

붕괴를 고친 선례(`l2021d` Q44)가 **다른 결함을 새로 만들었다**. 방법은 재작성이었으나:

- 📌 인용 3건에 **마커를 추론으로 배치**했다(`㉠수수알이…` — 원문에 마커 없음). dg[2] 층위 1에서 원문 복원.
- 결론줄 2건이 완결 문장(§7-20 위반). dg[2]에서 명사형으로 교정.
- 📌 위치가 세트 관례(1행)를 벗어나 3행에 있었다.

**이 프롬프트·형식을 재사용하면 안 된다.**

### 3-5g. `r20199a` Q23 — `<보기>` 복원 + 해설 재작성 (발주 di~dj)

`bogi`가 빈 문자열인데 해설 5선지가 `<보기>` 표를 인용하고 있었고, **인용한 10개 값이 실제와 하나도 일치하지 않았다**(전량 환각). 유입 커밋 `66af3d3`(2026-04-14, 제목은 "2022~2026수능"인데 실제는 2019_9월 — §7-25 실증).

- di[2] — PDF 추출 텍스트에서 표를 잘라 `bogi` 복원(손타이핑 0). `㉣`·`㉤`은 본문 정박 확인 → 문항 성립.
- dj[2] — 해설 5선지 재작성. 산출 순서 `㉮ > ㉯ > ㉱ > ㉲ > ㉰`, 2위 `㉯` = 정답 표기와 일치.

### 3-6. 백로그

- `2015수능B r2015aB` 16번
- 어휘문항 형광펜 정책
- **문항 단위 비노출 수단 부재** — `RELEASE_KEYS`는 `yearKey::setId` 세트 단위뿐이다. 선지 1개가 잘못돼도 세트 통째로 내리는 것 외에 방법이 없다. cn[0]에서 이 제약 때문에 비노출 판단이 막혔다.

---

## 4. 미커밋 상태 — 인수인계 시점의 워크트리

브랜치 `fix/cv-order`. `origin/main`(`8c80dc8`)에 없는 변경:

```
 M pipeline/reanalyze_format.mjs      cv[2] 가드 삽입
 M pipeline/reanalyze_positive.mjs    cv[2] 가드 삽입
 M pipeline/reanalyze_vocab.mjs       cv[2] 가드 삽입
 M pipeline/step3_analysis.js         cv[1] checkMarkerAnchored + 세트 루프 연결
?? pipeline/fixtures/step3_marker_order.json   cv[1] 합성 fixture
?? pipeline/choice_source_diff.mjs    (세션 중 생성, 미채택)
?? pipeline/marker_range_check.mjs    (bt-1b 발주분, 사양 미수신 상태로 남음)
```

★ `step3_marker_order.json`은 **합성 fixture**다. `l20229a`의 마커 정박 이전 상태가 git 이력에 남아 있지 않아 재현이 불가능해, `l20229a` Q19와 동일 구조(발문이 `ⓐ~ⓔ` 범위 선언 + 선지는 마커 조합 + `bogi` 없음)로 합성했다. 실데이터 음성 대조(현행 정박 완료 `l20229a` → `ok=true`)를 함께 돌려 정상 세트가 막히지 않음을 확인했다.

cv[2] dry-run 결과(LLM 0):

| 스크립트 | 결과 |
|---|---|
| `reanalyze_format.mjs` | 통과 (프롬프트에 `bogi` 이미 포함) |
| `reanalyze_positive.mjs` | 통과 (프롬프트에 `bogi` 이미 포함) |
| `reanalyze_vocab.mjs` | **미통과 34/118 (28.8%)** — `range_def_missing` 25 · `bogi_missing` 18 · `combo_def_missing` 3 |

`reanalyze_vocab.mjs`가 미통과인 이유는 **프롬프트가 `sents`만 넣고 `bogi`·마커 정박 문장을 넣지 않기 때문**이다. 발주 cv[2] 지시에 따라 **수정하지 않고 사유만 기록**한다. 이 스크립트를 그대로 돌리면 대상 118건 중 34건이 skip된다 — 즉 **가드가 없었으면 그 34건은 입력 없이 생성되던 것**이다.

---

## 5. 다음 세션 첫 액션

1. `git log --oneline origin/main -1`로 기준 확인 → `fix/cv-order` 미커밋분 처리 방향을 심사관에게 확인.
2. `reanalyze_vocab.mjs` 프롬프트에 `bogi` + 마커 정박 문장을 넣을지 **발주 받은 뒤** 착수(현재는 보고만 된 상태).
3. `l20229a` Q19 ②③⑤ · `r20236b` Q7 ④ 재작성 → 전문 보고 → 대표 검수 → 그 후 `2791c26` 동반 push.

### 상시 규율 (§7 운용지침 · `CODEX_운용지침_20260724.md`)

- `git add <path>` 한정. **`git add .` 금지.** 커밋 후 `git status` 확인 (§7-8).
- **데이터 수정과 검사도구 수정을 같은 커밋에 넣지 않는다** (§7-9).
- 게이트 확인과 push를 **같은 셸 체인에 넣지 않는다** (§13④).
- LLM 사용은 발주별 예외로만 열리고 **해당 발주 종료와 동시에 닫힌다**.
- 공유 트리 무접촉(읽기만). `.env` 커밋 금지.
- **"0건"은 양성 회귀 통과 후에만 유효하다** (§13⑮(7)).
