# 9월 모평 스프린트 런북 (2026-09-02 시행)

> 이 문서만 보고 순서대로 따라 하면 됩니다. 명령어는 그대로 복사해 쓰십시오.
> 작업 위치는 항상 `C:\Users\downf\suneung-viewer` 입니다.
> 목표: 국어 8세트(독서 4 + 문학 4)를 「검수 중」으로 공개.

---

## step 0 — 파일 두기 (5분)

시험지와 정답표 PDF 를 이 이름 그대로 아래 폴더에 넣습니다.

```
C:\Users\downf\suneung-viewer\_done\2027_9월\2027_9월_시험지.pdf
C:\Users\downf\suneung-viewer\_done\2027_9월\2027_9월_정답.pdf
```

**이름이 다르면 도구가 못 찾습니다.** 폴더 이름과 파일 앞부분이 똑같아야 합니다(`2027_9월`).

> ⚠ **홀수형 단독 PDF** 를 넣으십시오. 여러 형이 합쳐진 합본(28쪽 이상)은 도구가 거부합니다.

---

## step 1 — 원본 검사 (1분 · 비용 0) ★ 가장 먼저

**이 검사를 통과하지 못하면 나머지를 진행할 수 없습니다.**

### 1-1. 원문자가 살아 있는가 ← 30초 만에 판정납니다

```bash
pdftotext -layout -enc UTF-8 "C:/Users/downf/suneung-viewer/_done/2027_9월/2027_9월_시험지.pdf" - | grep -c "㉠\|㉮\|ⓐ"
```

- **숫자가 100 이상** → 정상. 1-2 로 갑니다.
- **숫자가 0 이거나 한 자리** → 🔴 **스캔본입니다. 여기서 멈추십시오.**
  이 파이프라인을 쓸 수 없습니다. 2014수능 A/B 가 실제로 그런 PDF 였습니다.
  대표께 보고하고 다른 판본을 구하거나 코덱스 경유로 전환합니다(사람이 하루 붙어야 합니다).

### 1-2. 세트 구간이 잡히는가

```bash
node pipeline/set_ranges.mjs 2027_9월
```

`세트 [1~3]`, `세트 [4~9]` 같은 줄이 나오면 정상입니다.
아무것도 안 나오면 시험지 파일명·경로를 다시 확인하십시오(step 0).

## step 2 — 정답표 읽기 + 등록 (3분)

### 2-1. 정답표 뽑기

```bash
node pipeline/step1_answer.js "C:/Users/downf/suneung-viewer/_done/2027_9월/2027_9월_정답.pdf" 34 > pipeline/test_data/answer_2027_9월.json
```

끝나면 파일을 열어 **1번과 34번 정답이 실제 정답표와 같은지 눈으로 두 개만** 확인하십시오.

> ⚠ 34 는 「34번까지만」이라는 뜻입니다. 35번 이후는 선택과목이라 쓰지 않습니다.

### 2-2. `answer_key.json` 에 등록 ★ 빠뜨리면 step 8-1 이 헛돕니다

`pipeline/answer_key.json` 을 열어 맨 앞에 이 모양으로 한 덩어리를 넣습니다.

```json
"2027_9월": {
  "src": "_done/2027_9월/2027_9월_정답.pdf",
  "ans": { "1": 3, "2": 5, ... , "34": 1 }
},
```

`ans` 안의 숫자는 **2-1 에서 만든 `answer_2027_9월.json` 의 내용을 그대로** 옮깁니다.

> 🔴 **이 등록을 빠뜨리면 step 8-1 의 정답표 대조가 그 회차를 건너뜁니다.**
> 「불일치 0건」이 떠도 실은 아무것도 안 본 것입니다.
> 그래서 step 8-1 은 `--expect 2027_9월` 을 반드시 붙여 돌립니다 — 빠졌으면 도구가 크게 경고합니다.

## step 3 — 구조 뽑기 (3분 · 보통 비용 0)

```bash
node pipeline/step2_extract.js "C:/Users/downf/suneung-viewer/_done/2027_9월/2027_9월_시험지.pdf" 2027_9월 34 all > pipeline/test_data/step2_2027_9월.json
```

**끝에서 두 줄을 반드시 읽으십시오.**

| 나오는 줄 | 뜻 | 할 일 |
|---|---|---|
| `✅ 폴백 문항 0건` | 전 문항이 원본 그대로 나왔습니다 | 그대로 진행 |
| `🔴 폴백 문항 N건 — reading:Q8 …` | 그 문항만 AI 가 다시 읽었습니다 | **그 문항의 원문자(㉠ ⓐ)를 화면에서 확인**하십시오. 깨졌으면 손으로 고칩니다 |
| `🔴 전체 폴백` | 전 문항이 AI 판독입니다 | 🔴 **멈추고 보고.** 원문자가 대량 손실됩니다 |

`pdf-parse accepted` 가 독서·문학 각각 한 번씩 나오면 가장 좋은 상태입니다.

---

## step 4 — 해설 만들기 (45분 · 약 $6) ★ 가장 오래 걸립니다

```bash
node pipeline/step3_analysis.js pipeline/test_data/step2_2027_9월.json pipeline/test_data/answer_2027_9월.json > pipeline/test_data/step3_2027_9월.json
```

세트당 약 6분, 8세트면 45분쯤입니다. 중간에 멈추면 아래로 그 세트만 다시 돌립니다.

```bash
node pipeline/step3_analysis.js pipeline/test_data/step2_2027_9월.json pipeline/test_data/answer_2027_9월.json --retry r20279a
```

---

## step 5 — 회차 자리 만들기 (10초 · 비용 0) ★ 신규 회차는 이게 먼저

`2027_9월` 은 아직 데이터에 없습니다. **자리를 먼저 만들어야 다음 단계가 돕니다.**

```bash
node pipeline/year_skeleton.mjs 2027_9월
```

미리보기가 맞으면 실제로 만듭니다.

```bash
node pipeline/year_skeleton.mjs 2027_9월 --apply
```

> 이 단계가 없으면 step 6 이 `시험 키를 찾을 수 없음: "2027_9월"` 로 멈춥니다.
> 기존 회차를 다시 돌릴 때는 이 단계를 건너뜁니다(이미 있으면 도구가 알아서 아무것도 안 합니다).

---

## step 6 — 데이터에 합치기 (2분 · 비용 0)

독서와 문학을 따로 넣습니다.

```bash
node pipeline/step6_merge.js pipeline/test_data/step3_2027_9월.json 2027_9월 reading
```

```bash
node pipeline/step6_merge.js pipeline/test_data/step3_2027_9월.json 2027_9월 literature
```

각각 이런 줄이 나오면 정상입니다.

```
✅ 머지 완료: 2027_9월 / reading
   세트: 업데이트 0개, 추가 4개
   analysis 채움: 85/85
```

**`추가 4개`** 가 나와야 합니다. `0개` 면 세트가 안 들어간 것입니다.

> 🔴 **당일 유일한 확인점 — `analysis N/N` 과 `cs_ids N/N` 의 분모와 분자가 같은지 보십시오.**
> `85/85` 처럼 같아야 정상입니다. `70/85` 처럼 다르면 **해설이 덜 붙은 것**이니
> step 4 를 그 세트만 다시 돌립니다.
>
> 리허설은 해설이 이미 채워진 데이터로 돌렸기 때문에, 갓 만든 해설이 들어오는
> 당일에 이 수치가 처음으로 의미를 갖습니다.

---

## step 7 — 형광펜 붙이기 (20분 · 약 $3)

**병합이 끝난 뒤에** 돌립니다. 이 도구는 `all_data_204.json` 을 직접 고칩니다.

```bash
node pipeline/step4_csids.js --retarget 2027_9월
```

끝에 `✅ 2027_9월 완료` 와 `N건 cs_ids 채움` 이 나옵니다.
자동으로 `pipeline/backups/` 에 원본 사본을 남기므로 잘못돼도 되돌릴 수 있습니다.

---

## step 8 — 게이트 10종 (15분 · 비용 0) ★ 공개 전 필수

### 🔴 게이트가 빨간불일 때 — 멈출까 진행할까 (D-152)

> **학생이 문제를 풀 수 없게 되면 멈춘다. 덜 잘 배우게 되면 진행한다.
> 「검수 중」 배너는 해설 품질을 양해받는 장치이지 문항 파손을 덮지 못한다.**

| 게이트 | 🔴 가 뜨면 | 왜 |
|---|---|---|
| **8-1** 정답표 대조 | 🛑 **전체 멈춤** | 틀린 정답은 오답을 정답으로 가르친다. 최악이다 |
| **8-2** 진단 13축 | 축에 따라 다르다 — ④ 구간 미정박 · ⑨ 마커 고아 · ⑩ 발문 마커 소실 → 🟠 **세트 제외**<br>① 근거 누락 · ⑬ 결론줄 비표준 → ✅ **진행** | 앞 셋은 발문이 가리키는 것을 화면에서 못 찾는다. 뒤 둘은 해설 품질이다 |
| **8-3** 품질 게이트 | **CRITICAL 이 하나라도 있으면 🟠 세트 제외** — 종류를 가리지 않는다<br>특히 `MARKER_INTEGRITY_FAIL` · `CS_ALL_NONHIGHLIGHTABLE` · `BOGI_IMAGE_MISSING` · `F_content_reversed` · `OK_ANALYSIS_CONFLICT` 는 화면이 깨지거나 정답을 거꾸로 설명하는 것이라 예외 없다 | CRITICAL 종류가 20가지가 넘어 **당일에 하나씩 판단할 수 없다.** 기본값을 제외로 둔다 |
| **8-4** 마커 감사 2종 | 🟠 **세트 제외** | 발문이 `㉠`을 묻는데 본문에 없으면 그 문항을 풀 수 없다 |
| **8-5** 사전 필터 | ✅ **진행 + 기록** | 이미 알려진 결함 표지다. 해설 품질 사항 |
| **8-6** 형광펜 실효 | `형광펜 0개 세트` → 🟠 **세트 제외**<br>`일부만 켜짐` → ✅ **진행** | 형광펜이 죽으면 핵심 차별점이 없어진다. 다만 문항 자체는 풀린다 |
| **8-7** 지문 실재 | 🟠 **세트 제외** | 지문이 없으면 근거를 볼 수 없다 |
| **8-8** 문항 대장 | 구간 누락 → 🛑 **전체 멈춤**<br>단발 누락 → 원본에서 단독 문항인지 확인 후 ✅ **진행**<br>유령·중복 → 🟠 **세트 제외** | 구간 누락은 추출이 통째로 밀렸다는 뜻이라 다른 세트도 의심스럽다 |
| **8-9** 분리 게이트 | 🛑 **전체 멈춤** | 산출물에 안 들어가면 화면에 아무것도 안 나온다 |
| **8-10** bogi 육안 | 🟠 **세트 제외** | `<보기>`가 깨지면 그 문항을 풀 수 없다 |

**🛑 전체 멈춤** = 공개하지 않고 대표께 보고 · **🟠 세트 제외** = 그 세트만 빼고 나머지 공개 · **✅ 진행** = 공개하고 기록만 남김

> **판단이 애매하면 세트 제외한다.** 잘못 빼면 며칠 늦게 열 뿐이고, 잘못 통과시키면
> 깨진 문항이 학생에게 간다. **9/2 의 목표는 8세트를 다 여는 것이 아니라 깨지지 않은 것을 여는 것이다.**
>
> ✅ **진행**은 위 표에 그렇게 적힌 것만이다. 표에 없는 것은 전부 🟠 **세트 제외**로 본다.

### 🟠 세트 제외 절차 (3분)

1. **`RELEASE_KEYS` 에서 그 세트 키 한 줄만 뺀다.** 나머지 7세트는 그대로 공개한다.
   데이터는 손대지 않는다 — 고치는 것은 9/3 이후다.
2. **프론트 인계 메모(step 9)에 제외 사유를 적는다.** 아래 형식 그대로.

```
제외: 2027_9월::l20279c
사유: 8-6 형광펜 0개 선지 2건 (Q31#1 · Q31#4) — cs_ids 가 각주만 가리킴
재검토: 9/3 이후 배치 작업에서 수리 후 재승격
```

3. **8세트 중 3세트 이상이 제외되면 공개하지 말고 보고한다.** 그 정도면 개별 결함이
   아니라 추출·병합 단계에 문제가 있다는 뜻이다.

> ⚠ **제외는 미루는 것이지 없애는 것이 아니다.** 제외한 세트는 반드시
> 배치 대장에 올려 9/3 이후 수리한다. 그러지 않으면 조용히 사라진다.


### 8-1. 정답표 전수 대조 — **틀리면 여기서 멈춥니다**

```bash
node pipeline/answer_key_audit.mjs --expect 2027_9월
```

읽을 것은 세 줄입니다.

| 줄 | 정상 | 아니면 |
|---|---|---|
| `🔴 정답 불일치` | **0건** | 🔴 **공개 금지.** 그 문항의 `ok` 를 고칩니다 |
| `🟡 ok 배치가 1개가 아님` | 0건 | 그 문항의 정답이 특정되지 않습니다. 고칩니다 |
| `✅ 2027_9월 대조됨` | 이 줄이 **떠야** 합니다 | 🔴 `🔴🔴🔴 … 대조되지 않았다` 가 뜨면 step 2-2 를 안 한 것입니다 |

> 이 도구가 진짜 gate0 입니다. 공식 정답 PDF 에서 뽑은 표와 데이터의 `ok` 를 맞춥니다.
> `--expect` 를 빼면 그 회차를 건너뛰고도 「0건」이 뜨니 **반드시 붙이십시오.**

### 8-2. 진단 13축 — 데이터 내부 정합성

```bash
node pipeline/release_diag.mjs "2027_9월::r20279a" "2027_9월::r20279b" "2027_9월::r20279c" "2027_9월::r20279d" "2027_9월::l20279a" "2027_9월::l20279b" "2027_9월::l20279c" "2027_9월::l20279d"
```

표에 🔴 가 하나도 없어야 합니다.

> ⚠ 이 도구는 **정답표를 보지 않습니다.** ⑦ 문항형식 축은 「선지 5개인가 · 정답이 하나로
> 특정되는가」만 봅니다. 정답이 맞는지는 8-1 이 봅니다. 둘을 헷갈리지 마십시오.

> 세트 id 는 step 3 산출물에서 확인하십시오. 보통 `r20279a`~`d`, `l20279a`~`d` 입니다.

### 8-3. 품질 게이트

```bash
node pipeline/quality_gate.mjs 2027_9월
```

**`CRITICAL … 0건`** 이어야 합니다.

### 8-4. 마커 감사 2종 — 신규 추출 세트 필수 (S-09)

```bash
node pipeline/marker_ref_audit.mjs
```

```bash
node pipeline/stem_head_audit.mjs
```

`2027_9월` 이 목록에 나오면 → 그 세트의 마커가 깨진 것입니다. 원본과 대조해 고칩니다.

### 8-5. 사전 필터 — 8세트 전부

```bash
node pipeline/batch_precheck.mjs "2027_9월::r20279a" "2027_9월::r20279b" "2027_9월::r20279c" "2027_9월::r20279d" "2027_9월::l20279a" "2027_9월::l20279b" "2027_9월::l20279c" "2027_9월::l20279d"
```

`표지·위반 보유 0` 이어야 합니다.

### 8-6. 형광펜이 진짜 켜지는가 ★ D-147 추가 — 핵심 차별점 보호

```bash
node pipeline/cs_effect_audit.mjs --year 2027_9월
```

**`형광펜 0개 세트 0`** 이어야 합니다.

> 🔴 이 검사가 없으면 **근거는 채워졌는데 화면에서 형광펜이 한 개도 안 켜지는** 선지가 그대로 나갑니다.
> `cs_ids` 가 각주·작가명·`(중략)`·구획표지만 가리키면 그렇게 됩니다(`l20209d` Q42#1 실증).
> **형광펜 1:1 연결이 이 제품의 핵심 차별점**이라 이게 죽으면 블로그에서 약속한 것과 어긋납니다.
>
> `⚠ 일부만 켜지는 세트` 는 형광펜이 켜지긴 하므로 공개를 막지 않습니다. 기록만 남기십시오.

### 8-7. 참조하는 지문이 실재하는가 ★ D-147 추가

```bash
node pipeline/passage_gap_audit.mjs --year 2027_9월
```

**`결손 보유 0`** 이어야 합니다.

> 문항이 `(나)`·`#68`·`[A]` 를 묻는데 본문에 그게 없으면 **학생이 근거를 못 봅니다.**
> `2019수능::r2019b` 가 시나리오 지문 1,673자를 통째로 잃은 채로 있었습니다.

### 8-8. 문항이 다 들어왔는가 ★ D-151 추가

```bash
node pipeline/question_roster_audit.mjs --year 2027_9월
```

**`구간 누락·유령·중복 0`** 이어야 합니다.

> 🔴 8-1 은 **정답표에 있는 문항을 데이터에서 찾아** 정답을 맞춥니다.
> 데이터에 그 문항이 아예 없으면 맞출 게 없어 **조용히 건너뜁니다.**
> 그래서 문항이 통째로 빠져도 8-1 은 「불일치 0건」이라 찍습니다. 이 검사가 그 자리를 메웁니다.
> `2017_9월` 이 실제로 독서 세트 하나(26~30번)를 통째로 잃은 채 있었습니다.
>
> `⚠ 단발 누락` 이 뜨면 그 번호가 **지문 없는 단독 문항**(문법·화법)인지 원본에서 확인하십시오.
> 단독 문항이면 안 담는 것이 맞습니다.

### 8-9. 분리 게이트

```bash
node pipeline/build_split.mjs --verify
```

**`누락 0`** 이어야 합니다.

### 8-10. bogi 눈으로 보기

`<보기>` 가 있는 문항 두세 개를 화면에서 열어 봅니다. 그림·표가 깨지지 않았는지만 봅니다.

---

## step 9 — 프론트에 넘기기

아래를 프론트 담당에게 전달합니다.

**`src/dataLoader.js` 의 `RELEASE_KEYS` 에 추가할 8줄:**

```
"2027_9월::r20279a",
"2027_9월::r20279b",
"2027_9월::r20279c",
"2027_9월::r20279d",
"2027_9월::l20279a",
"2027_9월::l20279b",
"2027_9월::l20279c",
"2027_9월::l20279d",
```

- **`RELEASED_KEYS` 에는 넣지 않습니다.** 그래야 「검수 중인 시험입니다」 배너가 뜹니다.
- `FREE_YEARS` 는 **이미 준비돼 있습니다**(F-38 로 `2027_9월` 추가됨). 손대지 않습니다.
- 엔지니어는 이 파일들을 직접 고치지 않습니다 — 프론트 발주 사항입니다.

**제외한 세트가 있으면 그 줄을 빼고, 아래를 메모에 함께 적습니다** (step 8 「세트 제외 절차」).

```
제외: <yearKey>::<setId>
사유: <어느 게이트에서 무엇이>
재검토: 9/3 이후 배치 작업에서 수리 후 재승격
```

제외한 세트는 **배치 대장에도 반드시 올립니다.** 메모에만 적으면 조용히 사라집니다.

---

## 전체 소요 요약

| 단계 | 소요 | 비용 |
|---|---|---|
| step 0~3 (파일·검사·정답표·등록·구조) | ~12분 | $0 |
| step 4 해설 | **~45분** | ~$6 |
| step 5 회차 자리 | ~10초 | $0 |
| step 6 병합 | ~2분 | $0 |
| step 7 형광펜 | ~20분 | ~$3 |
| step 8 게이트 10종 | ~15분 | $0 |
| **합계** | **약 1시간 30분** | **약 $9** |

72시간 안에 충분히 들어갑니다. 시간이 아니라 **중간에 멈추는 지점**을 잘 보는 것이 중요합니다.

---

## 멈춰야 하는 지점 — 요약 (자세한 것은 step 8 머리의 대응표)

1. **step 1-1** — 원문자가 0개면 스캔본. 이 파이프라인으로 못 합니다.
2. **step 3** — `🔴 전체 폴백` 이 뜨면 원문자가 대량 손실됩니다.
3. **step 8-1** — 정답이 하나라도 다르면 공개 금지. **틀린 정답 노출이 최악입니다.**
4. **step 8-8** — 문항 번호가 구간으로 빠지면 추출이 통째로 밀린 것입니다.
5. **step 8-9** — 분리 게이트 누락이 있으면 화면에 아무것도 안 나옵니다.
   `✅ 2027_9월 대조됨` 이 안 뜨면 대조 자체가 안 된 것입니다(step 2-2 누락).

## 공개 후 남는 일

「검수 중」 배너를 떼려면 정식 4관문(gate0~gate3)과 대표 승인이 필요합니다.

> **S-17 — 정식 승격 전에는 문항 발문·선지를 원본 시험지와 최소 1문항 표본 대조합니다.**
> 진단 도구 10종은 전부 **데이터 내부 정합**만 봅니다. 선지가 통째로 다른 문항 것이어도
> 모든 축을 통과합니다(`l20206a` Q24 실증). 이건 사람이 원본을 봐야만 잡힙니다.
> 9/2 당일에는 「검수 중」이라 넘어가지만, **배너를 떼는 시점에는 반드시 합니다.**
승격 기록은 `pipeline/release_approval_records/QG-2027_9월-<setId>-release-approval.json` 으로 만듭니다.
