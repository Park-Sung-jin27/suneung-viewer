# D-155 상신 — `l20206a` `[A]` 분리안 · `l20206d` `[A]` 후보

> `l20206d` `[B]` 는 **적용 완료**. `l20206a` `[A]` 는 dry-run 상신(적용 안 함).
> 전문: [`d155_l20206a_s2_split.md`](d155_l20206a_s2_split.md)

---

## ① `l20206a` `s2` 분리 + `[A]` 정박 — dry-run 통과

### 분리 결과

| | 자수 | 문면 |
|---|--:|---|
| `l20206as2` (꺾쇠 밖) | 59 | 이적에 원수가 여러 날 만에 연주에 도달하여 … **사관에서 쉬고 있었는데,** |
| `l20206as901` (꺾쇠 안) | 106 | **한 나비가 침상에 날아들거늘** … 그 가운데 광활하여 완연한 별세계라. |

**글자 검산 통과** — 두 조각을 공백 하나로 이으면 원문 166자와 **완전히 같다**. 공백 제외 123 → 123자.

### `[A]` 정박안

```json
{"type":"bracket","label":"A","sentFrom":"l20206as901","sentTo":"l20206as3"}
```

발주가 확정한 종료 문구 「…쓰여 있었다.」가 `s3` 의 끝과 **일치**합니다 ✅

### `cs_ids` 재정박 3곳 — 하나는 이동이 없습니다

| 위치 | 선지 요지 | 판정 |
|---|---|---|
| Q23#2 | 「연주로 가는 도중에 **사관에서 쉬려고** 군마를 멈추었다」 | **앞 조각 — `s2` 유지** |
| Q24#1 | 「[A]에서는 **공간의 광활함**」 | `s2` → **`s901`** |
| Q24#5 | 「[A]에서는 **비현실적 공간의 신비로움**」 | `s2` → **`s901`** |

### 되읽기 검산 (메모리 시뮬레이션 · S-02)

- 문장 22 → **23** · 본문 글자 **무변경** ✅
- 끊긴 `cs_id` **0** · 비-하이라이트 `cs_id` **0** ✅
- `s3` 이후 문장 id **재부여 없음** ✅

### S-11 예외에 대한 기록

S-11 은 「기존 세트 재분할 금지」입니다. 이번 건은 **예외로 봅니다** —
규약이 막는 것은 **분할 규칙을 소급 적용**하는 것이지, **원본에 있던 문단 경계를 되살리는** 것이
아닙니다. 경계를 정한 것은 프로그램이 아니라 원본 들여쓰기이고, 심사관 실측으로 확정됐습니다.
**이 판단이 틀렸다면 알려 주십시오 — 적용 전입니다.**

---

## ② `l20206d` — `[B]` 적용 완료 · `[A]` 후보 상신

### `[B]` — 문장 경계까지 일치 ✅ **적용 완료**

```
[B] = l20206ds56 ~ l20206ds58   (annotations.json +8줄)
```

| id | 문면 |
|---|---|
| `s56` | 하다가 별안간 방에서 뛰쳐나간다. |
| `s57` | 맨발로 연못을 향해 몸을 날린다. |
| `s58` | 그는 죽을 생각을 했던 것이다. |

발주가 준 세 문장이 데이터 문장 경계와 **정확히 맞습니다.** 라벨 `[B]` 는 `s55` 에
`workTag` 로 이미 떠 있어 구간 바로 앞을 가리킵니다.

`bracket_anchor_write.mjs` SPEC 에 넣어 적용했습니다(일회성 파일을 만들지 않았습니다).
`visual_marks.json` 에는 이 세트가 없어 동기화 대상이 없었습니다.

### `[A]` 후보 — 1순위 `s25~s32`

라벨 `[A]` 는 `s30` 에 `workTag` 로 있습니다. 그 위아래가 구간입니다.

| id | 문면 | 시제 |
|---|---|---|
| `s25` | 삼수가 왔다 간 다음 날 밤, 자정이 넘었다. | 과거 |
| `s26` | 칠흑의 밤을 타고 덩어리 같은 침묵을 지키며 타작마당에 장정들이 모여들었다. | 과거 |
| `s27` | 마을에서는 개들이 짖는다. | **현재** |
| `s28` | 불은 켜지 않았지만 집집에선 인적기가 난다. | 현재 |
| `s29` | 언덕 위의 최 참판댁은 어둠에 | — |
| **`s30`** | **`[A]` (workTag 라벨)** | — |
| `s31` | 묻혀 위엄에 찬 그 형태는 보이지 않는다. | 현재 |
| `s32` | 타작마당에서는 윤보의 그 우렁우렁한 목소리가 … 그 **횃불은 움직이기 시작한다.** | 현재 |

**결정적 근거는 Q16#4 입니다** `[Inference]`

> ④ `[A]`는 **시제가 과거형에서 현재형으로 바뀌면서** 장면에 긴장감을 더하고 …

`s25`·`s26`(과거) → `s27`~`s32`(현재)에서 시제가 바뀝니다.
**`[A]` 가 과거형 부분을 포함해야 이 선지가 성립합니다.** `s26` 부터 시작해도 과거형이 하나
들어가지만, Q16#1·#5 의 `cs_ids` 가 **`s25` 를 쓰고 있어** `s25` 부터로 봅니다.

| 순위 | 범위 | 근거 |
|---|---|---|
| **1** | `s25` ~ `s32` | 시제 전환 포함 + Q16#1·#5 가 `s25` 를 근거로 씀 |
| 2 | `s26` ~ `s32` | 발주 문구 「타작마당…횃불이 늘어나고」에 더 가까움 |

**심사관 원본 대조로 확정해 주시면 SPEC 에 넣겠습니다.**

### 자체 발견 — `s29`/`s31` 이 한 문장인데 두 동강 나 있습니다 🔴

```
s29  언덕 위의 최 참판댁은 어둠에
s30  [A]            ← 라벨이 문장 한가운데 끼어들었다
s31  묻혀 위엄에 찬 그 형태는 보이지 않는다.
```

원본에서 `[A]` 라벨이 여백에 있었는데 추출이 그 자리에서 줄을 끊었습니다.
**화면에서 한 문장이 두 줄로 갈라져 보이고, 형광펜도 반쪽만 켜집니다.**
`l20206a` `s2` 와 정반대 방향의 같은 원인(라벨↔문단 경계)입니다.

**발주 밖이라 손대지 않았습니다.** `[A]` 정박과 함께 판정을 청합니다.

---

## ③ `l20206a` Q24 해설 5건 — D-154 ③ 상신 그대로 대기

발주가 확인해 주신 대로 **근거는 조웅전 본문**입니다. 상신안의 `cs_ids` 는
`s2 s3 s4 s14 s15` 로 전부 조웅전 문장이고, `l20206d` 문장은 하나도 쓰지 않았습니다.

> ⚠ `s2` 분리를 적용하면 상신안의 `s2` 근거 중 **Q24#1·#5 는 `s901` 로 바뀝니다.**
> **두 건은 함께 적용해야 합니다.**

**오염 원인 확정 기록**: 같은 회차 `l20206d`(박경리 「토지」) Q16 선지가 복사됐습니다.
`choice_contamination_audit` 가 잡은 유일한 쌍이었고, 지금은 **0쌍**입니다.

---

## ④ 발주 항목 ↔ 실행 결과 대조표 (S-19)

| 항목 | 지시 | 실행 | 결과 |
|---|---|---|---|
| **①** | `l20206a` `s2` 분리 + `[A]` 정박 + `cs_ids` 3곳 재정박, 되읽기 검산 후 **상신** | ✅ **상신** (적용 안 함) | 글자 검산·메모리 되읽기 검산 통과. `cs_ids` 는 3곳 중 **1곳이 유지**(Q23#2) |
| **②-1** | `l20206d` `[B]` 정박 | ✅ **적용** | `s56`~`s58` · `annotations.json` +8줄 |
| **②-2** | `l20206d` `[A]` 후보 상신 | ✅ **상신** | 1순위 `s25~s32` · 2순위 `s26~s32` |
| **③** | Q24 해설 재작성안 근거 확인 | ✅ **확인** | 전부 조웅전 본문. `s2` 분리와 **동시 적용 필요** |
| **④** | 대조표 첨부 (S-19) | ✅ | 이 표 + `S-19` 를 규칙 목록에 등재 |

### 미결 목록 (심사관 판정 대기)

| # | 건 | 발주 |
|---|---|---|
| 1 | `l20206a` `s2` 분리 + `[A]` 정박 | D-155 ① |
| 2 | `l20206a` Q24 해설 5건 | D-154 ③ |
| 3 | `l20206d` `[A]` 범위 확정 | D-155 ② |
| 4 | `l20206d` `s29`/`s31` 문장 두 동강 | **자체 발견** |
| 5 | `r2019b` ㉠㉡㉢ 정박 + `[A]` 꺾쇠 (`quality_gate` CRITICAL 4건) | D-145 |
| 6 | 배치 B 근거 32건 + `pat` 4건 | D-150 ⑤ (배치 B 수리로 이월) |
| 7 | `2017_9월` 26~30번 세트 복원 | D-152 ③ (배치 C 최우선) |
| 8 | `2017_9월::l20179a` 선지 오염 4문항 | D-150 ③ (배치 C) |
