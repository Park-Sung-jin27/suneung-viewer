# l20206a s2 문단 경계 복원 + [A] 정박 — DRY-RUN

> **아무것도 쓰지 않았다.** 심사관 판정 후에 적용한다.

## ① s2 분리

원본 1문장 166자 → 2문장

- `l20206as2` (59자) — **꺾쇠 밖**
  "이적에 원수가 여러 날 만에 연주에 도달하여 군마를 다 쉬게 하고 원수도 노곤하여 사관에서 쉬고 있었는데,"
- `l20206as901` (106자) — **꺾쇠 안, [A] 시작**
  "한 나비가 침상에 날아들거늘 원수도 자연스럽게 날개를 얻어 그 나비를 따라 공중에 날아 한 곳에 이르니, 첩첩한 산중에 수목이 빽빽한 곳을 깊이 들어가니 그 가운데 광활하여 완연한 별세계라."

**글자 검산**: 두 조각을 공백 하나로 이으면 원문과 **완전히 같다** ✅
- 공백 제외 123자 → 123자 ✅
- 새 id `l20206as901` 는 기존과 겹치지 않는다: ✅

## ② [A] 정박안

`{"type":"bracket","label":"A","sentFrom":"l20206as901","sentTo":"l20206as3"}`

- 시작 `l20206as901` "한 나비가 침상에 날아들거늘 원수도 자연스럽게 날개를 얻어 그 나비를 따"…
- 끝   `l20206as3` "또 한 곳을 들어가니 아름다운 궁궐이 하늘에 닿았거늘, 나아가 보니 문에"…
- 발주 확정 종료 문구 「…쓰여 있었다.」 가 s3 끝인가: ✅
- 현재 annotations: [B]

## ③ `cs_ids` 재정박 — s2 를 가리키던 3곳

| 위치 | 선지 | 어느 조각인가 | 이동 |
|---|---|---|---|
| Q23#2 | 원수는 함곡에서 연주로 가는 도중에 사관에서 쉬려고 군마를 멈… | 앞 조각(연주 도달·사관) | `l20206as2` → `l20206as2` (유지) |
| Q24#1 | [A]에서는 공간의 광활함을 통해 인물의 진취적인 기상이 드러… | 뒤 조각(나비·별세계) | `l20206as2` → `l20206as901` |
| Q24#5 | [A]에서는 비현실적 공간에서 느껴지는 신비로움이, [B]에서… | 뒤 조각(나비·별세계) | `l20206as2` → `l20206as901` |

## ④ 되읽기 검산 (메모리 시뮬레이션 · S-02)

- 문장 22 → **23** · 본문 글자 **무변경** ✅
- 끊긴 `cs_id` **0** · 비-하이라이트 `cs_id` **0** ✅
- 나머지 문장 id **재부여 없음** — `s3` 이후 전부 그대로 ✅

### 적용 시 함께 볼 것

- `[A]` 정박은 `bracket_anchor_write.mjs` SPEC 에 넣어 쓴다(vm 동기화 포함)
- `passage_gap_audit` → `[A]` 미정박 해소 확인
- `quality_gate 2020_6월` → `MARKER_INTEGRITY_FAIL` 해소 확인
- Q24 해설 재작성안(D-154 ③)이 `s2` 를 근거로 쓰므로 **함께 적용해야 한다**
