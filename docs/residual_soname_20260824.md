# 잔여 위반 소명표 (2026-08-24, 발주 ④)

## 1. `CS_ALL_NONHIGHLIGHTABLE` 1건

**`2020_9월 l20209d` Q42-[1]**

| 항목 | 내용 |
|---|---|
| 발문 | 윗글의 서술상 특징으로 가장 적절한 것은? |
| 선지 | 장면의 빈번한 교차를 통해 인물 간의 갈등을 입체적으로 드러내고 있다. |
| `ok` | `false` |
| `cs_ids` | `["l20209ds16"]` |
| 그 문장 | `sentType: "omission"` — 본문이 **`(중략)`** |

**성격**: 「장면의 빈번한 교차가 없다」를 논증하는 선지라, step3 가 근거로 삼을 만한 지문
문장이 원래 없다. 그래서 중략 표지를 가리켰다. 형광펜은 중략 표지에 칠할 수 없으므로
화면에는 아무것도 안 켜진다.

**판단**: 이 축은 「형광펜이 조용히 꺼지는 것」을 막는 축이고, 여기서는 실제로 꺼진다.
다만 **근거로 삼을 문장이 존재하지 않는 유형**이라 데이터를 고쳐서 해결되지 않는다.
`cs_ids` 를 비우면 `E_ok_true_no_cs_ids` 계열로 옮겨갈 뿐이다. 판정은 하지 않는다 —
「부재 논증형 선지에 cs_ids 를 어떻게 둘 것인가」는 규칙 결정 사항이다. `[확인 필요]`

## 2. 제거된 `cs_spans` — 25건 → 15건

`[A]`·`[B]` 대괄호 마커를 재정박이 무시하도록 고쳐 25 → **15건**으로 줄었다.
(step4 는 `"[A] '낭군이 입신양명하여…'"` 처럼 마커를 인용문 앞에 붙여 쓰는데, 본문에는
없고 `annotations` 로 정박되는 표지다.)

전건은 `docs/csspan_dropped_20260824.md`.

### 성격별

| 성격 | 건수 | 예 |
|---|--:|---|
| 생략 인용 — `...`/`…`로 잘라 원문에 그대로 없음 | 다수 | `어떤 늙은 할미가...` |
| 의역 인용 — 원문을 다듬어 인용 | 일부 | `] 지키는 일이다. 지켜보는 일이다.` |
| 극히 짧은 조각 | 일부 | `푸른 봄`, `사랑한다는 것은.` |

**영향 범위**: `cs_ids` 는 그대로 두었다. 문장 단위 근거는 살아 있고, 사라지는 것은
**문장 안 구절 강조**뿐이다. 남겨 두면 형광펜이 조용히 꺼진 채 화면에 아무 표시도
안 되므로(`CS_SPAN_UNRESOLVED`), 지우는 쪽이 화면에 정직하다.

**복구 가능성**: step4 를 다시 돌려도 같은 인용문이 나오므로 자동 복구는 안 된다.
구절 강조를 되살리려면 해당 선지의 `cs_spans` 를 원문 기준으로 새로 만들어야 한다 —
별도 발주 사항. `[확인 필요]`

## 3. 줄바꿈 판별 불가 — 39건

PDF 줄 끝 공백을 확정하지 못한 지점. **추측으로 메우지 않고 `\n` 원형 그대로 두었다.**
전건은 `docs/newline_unresolved_20260824.md`.

문장 기준으로는 `sents.t` 19개(1.0%) · `questions.bogi` 3개(6.8%) — 둘 다 기존
353세트 수준(1.0% / 10.0%) 이하다.
