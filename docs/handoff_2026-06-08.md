# 인수인계 — 2026-06-08 새 회기 진입

## 첫 단계 (CLAUDE.md §15)

1. `CLAUDE.md` read (v1.6 — §6 글자 표준 신설 주의)
2. `docs/current_state.md` read
3. 본 handoff read

## 직전 회기 (06-06~07) 요약 — LEGACY 모의 구조·정합 전 단계 종결

8 sprint 전부 push·라이브 검증 완료: marker 2차 59 / 중복 22 set dedupe / 구조 재구축 14 set / 분포 14 / bogi 복원 7 / PUA 표준화 225 / para 93 set / **cs_spans 415 재정박 → 전체 350 set 결함 0**.
베타: 2026_9월 Q17 데이터표·어휘 판별 마지막문항 룰(JSX)·set 정렬 12곳·표/그림 구현 7건(bogiTable 양형·인라인 [그림src:] 신설).

## 다음 회기 우선순위 (대표 결정 대기 항목 포함)

1. **[대표 결정] LEGACY 모의 해설·cs_ids 생성** — LLM 비용. r20169b Q21~24 등 해설 공란 문항 존재, 생성 전 LEGACY release 금지
2. **LEGACY 수능 결함 8 set 본문 재구축** (r2014a 0sent·r2014e 거대 병합 등 — 06-07 분할·재구축 엔진 재사용: /tmp 소실, current_state 참조 재작성)
3. para 미검출 28 set (2014수능 A/B 등 들여쓰기 미검출 PDF — fitz 좌표 방식 2차)
4. [대표 결정] 2027_6월 R3/V 22건 근거 보강 여부 + release 판단
5. 후순위: ASCII 따옴표 정리(span 동기 의무) / setId 접두사 오기 4건(l2015aB 등 — 참조 파급 검토 전 보류) / 모바일 옛한글 폰트(함초롬LVT — 베타 피드백 시)

## 영구 지식 (06-07 신규)

- **중복 set 진단**: 같은 yearKey 내 문항 번호 겹침 스캔 → 완전판(각주·물결·괄호 보존본) 승자 판정 → 고유 문항 이관 후 삭제
- **본문 분할 재구축**: 거대 sent(>700자) = 본문 완전·미분할 → in-place 문장 분할 + cs containment remap. 무공백 문단 경계·곧은따옴표(") 패턴 주의
- **cs_spans 재정박 룰**: B형 suffix 보정 / 한자병기 괄호 무시 / 말줄임 strip / 원문자·[A] 라벨 strip / 최장 prefix 정박 (전건 원문 substring 검증 의무)
- **이미지 보기**: PDF 내장 raster는 xref 직접 추출이 crop보다 깨끗. bogi 인라인 이미지 = [그림src:/images/…]
- 도구: para_assign 33 yearKey 일괄은 단일 프로세스로 (개별 spawn 시 타임아웃 → 파일 손상 위험, before_para 백업이 생명줄이었음)

## 환경 주의

- sandbox 캐시 절단: 회기 재시작으로 해소되나 **방금 Write한 JSX는 같은 회기 sandbox에서 절단 보임** → build 검증은 대표 머신
- sandbox git push 불가 / 라이브 검증은 Vercel 빌드 40초+ 대기
- bash 45초 타임아웃 — 장시간 배치는 단일 node 프로세스로 묶을 것

## 새 채팅 첫 메시지 권고

```
"새 회기 시작. 일과 시작.
docs/handoff_2026-06-08.md 읽고 진행해줘."
```
