# 세션 상태 — 2026-08-28 기준

> **다음 세션은 이 파일만 읽고 이어받을 수 있다.**
> 공통 규칙은 루트 `AGENTS.md`, 진행 상태는 이 문서가 최신이다(`docs/current_state.md` 보다 뒤).
> 역할: 데이터 엔지니어(Code B) · worktree `C:\Users\downf\jippi-bo` · 브랜치 `fix/dm-r2021c` → `origin/main`

---

## 1. 지금 어디까지 왔나

| 항목 | 상태 |
|---|---|
| `RELEASE_KEYS` | **267세트** |
| 전체 세트 | 396 |
| 웨이브 1 (2026수능 · 2024_9월) | ✅ 종결 — **10세트** · 241 → 251 |
| 웨이브 2 (2023~2025) | ✅ 종결 — **8세트** · 251 → 259 |
| **웨이브 3 배치 A** (2021~2022) | ✅ **종결** — **8세트** · 259 → 267 |
| 웨이브 3 배치 B~D | ⏸ 대기 — **9/3 이후 재개** |
| 9/2 모평 스프린트 | 🔜 준비 완료 (런북·리허설 끝) |

### 남은 갭 — 현재 129 (배치 A 종결로 137 → 129)

`node pipeline/release_gap.mjs --md` 로 언제든 재산출한다.

| 배치 | 범위 | 갭 | 상태 |
|---|---|--:|---|
| A | 2021~2022 | 0 | ✅ 종결 |
| **B** | 2019~2020 | **13** | **다음 차례** |
| C | 2017~2018 | 32 | 대기 |
| D | 2014~2016 | 84 | 대기 |
| — | 2014수능 A·B | (20) | **영구 보류** — 스캔본, 원본 대조 불가 |

2014수능 A/B 는 원본 PDF 앞 6면 추출 글자가 **211자·193자**(다른 회차는 11,000자대)라
스캔본으로 확정됐다. SBS 아카이브·legendstudy 재소싱도 전부 스캔본이었다(D-130).
OCR 대체 금지(§13⑬). 텍스트층 PDF 확보 시에만 재개.

---

## 2. 9/2 스프린트 — 준비 전말 (D-129~D-142)

**시행 2026-09-02(수). 시험 직후 국어 8세트를 「검수 중」으로 공개하는 것이 목표.**

### 실행 문서

**`docs/sprint_runbook_2609.md` 하나만 보면 된다.** step 0~9, 명령어까지 복사해 쓸 수 있게 적혀 있다.
약 1시간 30분 · 약 $9.

### 준비하며 찾아낸 것들 — 이 맥락을 모르면 런북을 고치다 사고 난다

| 발주 | 무엇을 찾았나 |
|---|---|
| D-129 | 예열 점검. `step2_extract` 만 실측이 없었다(코덱스가 대체해 왔음) |
| D-135 | 첫 리허설 — **원문자 마커 77% 소실**(53→12), `①-⑳` 21개 오치환 유입 |
| D-136 | 원인 규명. **pdf-parse 는 멀쩡했다**(마커 238개 보존). `extraction_validator` 가 Q8 하나를 `S2_choice_count` 로 거부하자 **산출물 전체를 버리고 Gemini 로 폴백**했고, 그 경로가 마커를 죽였다 |
| D-137 | 파서 수정(선지 문두 `<보기>` 제외) + 부분 폴백 + 가시화. 회귀 49회차 2,115문항 **어긋남 0 · 개선 14** — 같은 버그가 14개 회차에 있었다 |
| D-138 | 문장 재분할. 독서만, 운문 보호. 종결어미는 실측 5종(`다자까가라`)으로 좁혔다 |
| D-139 | 8세트 리허설 **8/8 통과** — 마커 124/124, 오치환 0, 문장 수 전건 일치 |
| D-140 | 런북 결함 4건. **`release_diag` ⑦축은 정답표를 보지 않는다** — 진짜 gate0 은 `answer_key_audit.mjs` |
| D-141 | step5~7 리허설. **신규 회차 순서 확정** — 스켈레톤이 병합보다 먼저 |
| D-142 | 런북 최종 정리 |

### 반드시 기억할 세 가지

1. **Gemini 폴백은 마커를 죽인다.** `step2_extract` 출력의 마지막 줄
   `✅ 폴백 문항 0건` 을 반드시 읽는다. `🔴 전체 폴백` 이면 멈춘다.
2. **`release_diag` 는 정답표를 보지 않는다.** gate0 은 `answer_key_audit.mjs --expect <회차>` 다.
   `--expect` 를 빼면 그 회차를 건너뛰고도 「불일치 0건」이 뜬다.
3. **신규 회차는 `year_skeleton.mjs` 로 자리를 먼저 만든다.** 없으면 `step6_merge` 가 거부한다.
   순서: 구조 → 해설 → **스켈레톤** → 병합 → 형광펜.

---

---

## 3. 규칙 체계 — 두 갈래다. 헷갈리지 말 것

### ⚠ 먼저 알아야 할 것

원문자 번호가 붙은 규칙이 **두 체계**로 따로 돌아간다.

| 체계 | 어디에 | 범위 | 예 |
|---|---|---|---|
| **`§13`** | **`CLAUDE.md` 13절** | ①~⑱ (이미 있음) | §13⑪ 데이터 편집 git-object 우회 · §13⑬ LIVE typo 검출 · §13⑱ 출시 후보 판정 |
| **세션 채택 규칙** | **이 문서** | ①~⑯ | 아래 표 |

**번호가 겹친다.** `§13⑪`(데이터 편집)과 세션 규칙 `⑪`(문장 분할)은 전혀 다른 규칙이다.
인용할 때 **반드시 `§13⑪` 또는 「세션 규칙 ⑪」로 구분**해서 쓴다.

> 발주 대화에서 「채택 규칙 ⑪」이라 부른 것이 실제로는 세션 규칙 계열이었다(D-137 ⑥).
> `§13` 과 혼동될 수 있으므로 이 문서에서 표기를 통일한다.

### 세션 채택 규칙 ①~⑯

| 번호 | 규칙 | 출처 |
|---|---|---|
| ① | **시험지 대조 의무 표시 WARNING 은 진단 단계에서 즉시 대조한다** | D-119 ① · D-121 공지 |
| ② | **쓰기 도구는 되읽기 검산 필수** — 쓴 뒤 파일을 다시 읽어 SPEC 대로 반영됐는지 보고, 어긋나면 `exit 1` | D-121 공지 |
| ③~⑧ | **이 세션 기록에서 확인되지 않음** — 이전 구간에서 구두로 채택됐을 수 있으나 `AGENTS.md`·`CLAUDE.md`·`docs/` 어디에도 목록이 없다. 다음 세션이 발주에서 ③~⑧ 을 인용하면 **대표·심사관에게 내용을 물어 이 표에 채워 넣을 것** | — |
| ⑨ | **신규 추출 세트는 노출 전 `marker_ref_audit` · `stem_head_audit` 2종 필수 통과** | D-137 ⑥ |
| ⑩ | **결함 표지 제거는 수리 확인 후에만.** `defect_marker_clear.mjs` 가 지우기 전에 결함 해소를 스스로 확인한다. `_discriminative_validation` 은 일상 산출물(LIVE 포함 526건)이라 지우지 않고 `passed` 만 갱신 | D-133 ② |
| ⑪ | **문장 분할 규약** — 2026-08-28 이후 신규 추출 세트는 새 규약을 따른다. **기존 세트는 재분할 금지**(`cs_ids` 는 세트 내부 참조라 공존해도 정합) | D-138 · `sent_split_contract.md` |
| ⑫ | **근거·`pat` 를 새로 부여한 선지는 gate3 에서 결론줄–`ok` 일치까지 확인한다** — `l20219d` Q32#5 는 `pat` 을 넣은 뒤에도 결론줄이 `❌ 황후가 …` 로 남아 판정이 거꾸로 읽혔다 | D-133 ① |
| ⑬ | **새 진단 축은 실물 표본을 확인한 뒤 대장에 등재한다** — ⑬ 결론줄 축이 「마지막 줄」 기준이라 LIVE 19세트를 오탐했다(76건 → 10건) | D-133 ③ |
| ⑭ | **규칙은 기존 데이터 분포 실측으로 정한다** — 문장 종결어미를 넓게 잡았다가 문장 중간에서 잘렸다. 독서 `body` 4,507문장을 세어 `다자까가라` 5종(99.9%)으로 좁혔다 | D-138 ① |
| ⑮ | **게이트 도구는 실제 검사 내용을 코드로 확인한다 — 이름으로 판단 금지** | D-140 ① |
| ⑯ | **새 가드는 실패 케이스로 뚫어 본 뒤 채택한다** — `--expect` 첫 구현이 `skip` 목록만 봐서, `all_data` 에 없는 신규 회차는 순회 자체를 안 해 「✅ 대조됨」으로 통과했다 | D-140 ① |

### ⑮ 가 왜 규칙이 됐나 (가장 값비쌌던 실수)

런북 gate0 자리에 `release_diag` 를 넣었다. **이름이 「진단」이라 정답표를 볼 줄 알았는데 안 본다.**
⑦ 문항형식 축은 `questionType` 기준 「정답이 하나로 특정되는가」만 본다.
진짜 gate0 은 `answer_key_audit.mjs` 다. 심사관이 코드를 직접 읽어 잡았다(D-140).

---

## 4. 이번 구간에 만든 도구

전부 `pipeline/` 아래. **진단 도구는 아무것도 쓰지 않는다.**

| 도구 | 하는 일 |
|---|---|
| `batch_precheck.mjs` | 배치 착수 전 사전 필터 — 이미 알려진 결함 표지를 먼저 골라낸다 (D-134) |
| `defect_marker_clear.mjs` | 결함 표지 제거(수리 확인 후에만) (D-133) |
| `year_skeleton.mjs` | 신규 회차 빈 자리 생성 (D-141) |

**이번 구간에 수정한 도구** — 아래는 **이전 구간 산물**이며 이번에 축·모드를 더했다.

| 도구 | 이번에 무엇을 더했나 |
|---|---|
| `release_diag.mjs` | 축 ⑫ 결함 표지 잔존 · ⑬ 결론줄 추가 → **13축** (D-133) |
| `release_gap.mjs` | 2014수능 스캔본 판정·제외 기록 (D-130) |
| `stem_head_audit.mjs` | `--data=` 과거 스냅샷 회귀 확인 (D-121) |
| `answer_key_audit.mjs` | `--expect <회차>` 가드 — 조용한 skip 차단 (D-140) |
| `analysis_format_fix.mjs` | `REWRITE` · `MARKER_SWAP` 모드 |
| `evidence_assign.mjs` | `replace` · `pat` 부여 모드 (3중 안전장치) |
| `choice_text_restore` · `sent_text_fix` | 되읽기 검산 (세션 규칙 ②) |
| `pdf_text_extractor.mjs` | 선지 문두 `<보기>` 오인 수정 (D-137) |
| `step2_extract.js` | 부분 폴백 + 가시화 · `logUsage` 계측 (D-137) |
| `step2_postprocess.mjs` | 산문 문장 재분할 (D-138) |
| `step4_csids.js` | `logUsage` 라벨에 `set.id` (D-135) |

---

## 5. 백로그 — 지금 건드리지 않는다

| 항목 | 규모 | 시점 |
|---|---|---|
| **배치 B 진단** 13세트 (`r20199c` `_pat_error` 포함) | 중 | **9/3 이후 — 다음 차례** |
| 배치 C 32세트 · 배치 D 84세트 | 대 | B 종결 후 |
| 결함 표지 9세트 (`_pat_error`·`_ok_analysis_mismatch`) | 소 | 배치 C/D 에서 함께 |
| 결론줄 위반 10건 (`r20169b` Q24#5 는 기호 자체가 없음) | 소 | 배치 C/D |
| 인용부호 소실 12건 (LIVE 2 — `l20246d` Q32 · `l20259b` Q23) | 소 | 별도 클래스, 원본 대조 필요 |
| `cs_spans` 0건 세트 (`r20216a`·`r20216d`) | — | **결함 아님** — 문장 단위 형광펜은 정상 동작 |
| 문장 재분할 인용 안 종결 정교화 | 소 | 9/3 이후 |
| `step5_verify` 의 추가 가치 측정 | 소 | 9/3 이후 — 8-1 정답표 대조와 ⑬ 결론줄 축이 같은 것을 $0 에 잡는지, 다른 것을 잡는지 |
| `lastQuestion` 무시 버그 | 소 | 여유 있을 때 |
| `step2` Gemini 호출 계측 | 소 | 응답 형식이 달라 `api_usage.mjs` 가 못 읽는다 |

---

## 6. 블로그 채널 전략

블로그 글이 특정 세트를 직접 인용한다. **인용 대상 세트는 최우선 처리한다.**

- 실제 사례: `2022_6월::l20226a` 「무사와 악사」(홍성원) — D-128 추가 발주로 배치 A 에서 먼저 빼내
  승격 기록을 만들고 F-39 로 노출했다.
- 블로그가 링크를 걸었는데 세트가 비노출이면 **독자가 눌러도 못 본다.** 인용 목록이 오면
  갭 대장(`release_gap.mjs`)과 대조해 비노출 여부를 먼저 확인한다.

---

## 7. 협업 규율 — 바뀌지 않은 것

- `git add <path>` 한정. **`git add .`/`-A` 금지.** 데이터 수정과 검사도구 수정은 별도 커밋
- 공유 트리 `C:\Users\downf\suneung-viewer` 무접촉(읽기만). 다만 **런북은 그 트리 기준으로 적혀 있다**
  — 심사관·대표가 거기서 작업하기 때문이다(공유 트리에도 `pipeline/` 이 있다)
- `RELEASE_KEYS`·`RELEASED_KEYS`·`FREE_YEARS` 는 **프론트 소관**. 승격 기록의 `frontend_request` 에 키를 적어 넘긴다
- push 전 `git fetch origin` → ahead/behind 확인 → 필요시 **merge**(`all_data_204.json` 이 걸린 브랜치는 rebase 금지)
- 산출물(`free/`·`data-pro/`) 커밋 금지
- 4관문 순서 엄수: gate0(정답표) → gate1(QG) → gate3(화면 실측) → 대표 승인 → 승격 기록 → F-xx 노출
  **승격 기록을 gate3 전에 만들지 않는다**(D-132 에서 되돌린 전례)
