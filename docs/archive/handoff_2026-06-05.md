# 인수인계 — 2026-06-05 새 회기 진입

## 새 채팅 시작 시 첫 단계 (CLAUDE.md §15 정합)

1. `CLAUDE.md` read (v1.4)
2. `docs/current_state.md` read (2026-06-04 일과 종결 상태)
3. 본 handoff read

---

## 직전 회기 (2026-06-04) 완료 요약 — 4 sprint, 4 commit, 전부 push·배포·라이브 검증 종결

1. **2025_6월 4 set 정합**: 본문 marker 10건 + stem/선지 오염 정정 + r20256d Q14 6지선다 결함 (phantom 선지 삭제 + 정답 ④ ok 재배정, 정답표 PDF [Confirmed]) + annotation 14건 (box 에이어 + bracket A)
2. **잔여 yearKey 16건** (2023_6월/2026_9월/2022_6월): 본문 marker 19건 + l20269a Q19 발문 유형 정정 (부정→긍정, 정답 ① — ok 5건 재배정 + 해설 5건 재작성) + r20226d Q17 보기 빈칸 ㉮㉯㉰ 재구축 + r20226c Q10 questionType 정정
3. **cs_spans 무결성 1,107건** 자동 정정 (dead sentId remap 811 + text 재계산 등)
4. **잔존 138건 종결** → **베타 영역 (수능+모의 22~26) cs_spans 결함 0건** (dead 0 / text_miss 0 / 정상 3,764). 본문 누락 0건 확인. l20266c s51 ㉥ 오염 글자 본문 정정

핵심 영구 지식:

- **시험지·정답표 PDF가 repo `_done/{yearKey}/` 안에 있음** → 사용자 인용 의뢰 불필요, 직접 대조 = [Confirmed] (CLAUDE.md v1.4 명문화)
- span 재정박 안전 규칙: 동일 문장 또는 정규화 일치율 0.85+ 한정, 전 건 원문 substring 검증, 적용 전 backup (`pipeline/backups/`)
- 정답표 파싱: pdftotext 후 문항번호-원숫자 쌍 추출 (이번 회기 3회 실증)

---

## 다음 회기 첫 액션

1. 베타 무작위 3 set 형광펜 클릭 육안 검수 (대량 정정 후 안전 확인)
2. **대표 방향 결정 의뢰**: ① viewer target='bogi' annotation 시각화 (코드 변경, 프론트엔드 영역) ② LEGACY 모의 14~21 진입 (데이터 영역, 13개년 비전 path)

## 잔여 작업 (우선순위)

1. 방향 결정 후 해당 트랙 진행
2. LEGACY cs_spans 420건 — LEGACY 진입 시 본문 품질 검증과 동시 처리 (베타용 스크립트 무검토 적용 금지)
3. Phase 2b reject 9건 manual 정정 (PDF cross-check)
4. quality_gate 사전 존재 CRITICAL 21건 (suneung5: bracket 의심 14 + analysis_id_leak 5 + questionType 2) 검토 sprint
5. 토스 페이먼츠 결제 연동 (심사 대기)

---

## 환경 주의 (유지)

- Cowork file 절단 결함 → `cat heredoc` / `cp` path 우선
- sandbox에서 git push 불가 (인증 없음) → commit까지 하고 사용자가 PowerShell에서 `git push origin main`
- `.git/index.lock` 잔류 시: 삭제 권한 도구 허용 후 `rm -f .git/index.lock`
- 응답 형식: 결론 먼저 1줄 / 옵션 나열 X / 검증 sample ≤3건 (CLAUDE.md §1.D)

## 새 채팅 첫 메시지 권고

```
"새 회기 시작. 일과 시작.
docs/handoff_2026-06-05.md 읽고 진행해줘."
```
