# 인수인계 — 2026-06-06 새 회기 진입

## 첫 단계 (CLAUDE.md §15)

1. `CLAUDE.md` read (v1.5 — §7 해설 서술 사양 신설 주의)
2. `docs/current_state.md` read (06-05 일과 종결, 7차까지 변경 이력)
3. 본 handoff read

---

## 직전 회기 (06-05) 요약 — 9 sprint 전부 push·라이브 검증 종결

1. 오염 글자 잔존 9 set / 2027_6월 진입(정답표 PNG 본체 지원)·검수 2회·**해설 품질 개편(ok:true 91건 3단 서술 + 🔎 블록 170 제거)** / step2 자동화(cleanSentStructure·(가) 보존·tail-cutter 푸터 2종)
2. **FREE 공개 영역 CRITICAL 21건 종결** — r2025b Q5는 발문·선지 전체가 원문과 다른 **환각 문항**이었음 (전면 재구축, 정답표 ⑤ 정합). gate에 메타 발문 예외 추가
3. 문단(para) 커버 62/64 + r20249d·r20259d (가)(나) 복원
4. **LEGACY 모의 진입**: 전수 진단 (156 set 중 결함 88: marker 75/분포 14/구조 12/오염 1) → marker batch 1차 198건 (잔존 99건/43 set)

## 다음 회기 1순위 (대표 지시 확정: "Legacy 모의 진입하자")

1. **LEGACY marker 2차 99건** — 1차는 pdftotext anchor 유일 매칭. 2차는 fitz 좌표 방식 (06-05 para 추출에 쓴 기법: words y-그룹 + 컬럼 base indent. /tmp 소실됐으니 current_state 7차 항목 참조해 재작성)
2. **구조 미달 12 set 본문 재구축** (PDF 추출 실패류 — l20259c 재구축 precedent)
3. **분포 이상 14 set** — 메타 발문(§6) 정상분 선별 후 잔여만 정정
4. 이후: LEGACY 해설·cs_ids 생성 여부 = LLM 비용 — 대표 결정 필요

## 영구 지식·도구

- 전수 원문 대조 검수법: 발문·선지·보기 정규화 substring ↔ 시험지 PDF 전문 (Q5 환각 class 검출). 정규화: marker·공백·따옴표·한자병기괄호·『』󰡔󰡕·~～ 제거
- `pipeline/para_assign.mjs` (--bounds 옵션), step2_postprocess.cleanSentStructure, tail-cutter 규칙
- 정답표 파싱: PDF 텍스트형=pdftotext 쌍 추출 / 이미지형=렌더 후 직접 판독 (2025수능 정답 PDF는 이미지형!)

## 환경 주의 (치명 — 반드시 숙지)

- **sandbox에서 data-source/all_data_204.json 파싱 불가** (사용자 머신 node가 쓴 파일의 mount 캐시 절단). 우회 확립: ① 진단 read = 라이브 fetch(브라우저 JS) 또는 정상 백업본 ② 정정 적용 = 사용자 `Copy-Item`으로 사본 생성 → sandbox에서 사본 수정 → 사용자 역복사+commit ③ 2027_6월은 step5 캐시 수정→재머지 path
- Edit 도구로 쓴 파일도 sandbox에서 절단되어 보일 수 있음 → 해당 파일 commit은 대표 머신
- sandbox git push 불가 → commit까지, push는 대표
- 라이브 검증은 Vercel 빌드 1~2분 대기 후 (직후 측정은 옛 캐시)

## 새 채팅 첫 메시지 권고

```
"새 회기 시작. 일과 시작.
docs/handoff_2026-06-06.md 읽고 LEGACY 모의 2차부터 진행해줘."
```
