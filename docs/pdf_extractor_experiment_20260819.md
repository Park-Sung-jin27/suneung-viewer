# PDF 추출기 교체 실험 결과 (발주 D-46 · 2026-08-19)

> 결론: **추출기 교체로는 해결되지 않는다. PDF 안에 이미지로 들어 있다.**

## 1. 원본 PDF 보유 — 47 / 47

`_done/<회차>/<회차>_시험지.pdf`. 없는 회차 0.
★ D-45 에서 「원본 PDF가 없다」고 보고한 것은 틀렸다. `_done` 을 찾지 못했다.

## 2. 🔴 핵심 검증 — 2021_9월 r20219e Q23 의 <보기>·선지 5개

| 추출기 | 선지 5개 검출 | 추출 문자수 |
|---|:-:|--:|
| 기존 pdf_*.txt | **0 / 5** | 27,896 |
| pdftotext | **0 / 5** | (CMap 오류로 한글 깨짐) |
| pdftotext -layout | **0 / 5** | (동상) |
| PyMuPDF (fitz) | **0 / 5** | 27,896 |
| pdfplumber | **0 / 5** | 27,912 |

### 원인 확정 — 이미지다

Q23 이 있는 **9쪽**을 뜯어 보면:

```
텍스트 1,513자 · 이미지 객체 4개 · 도형 605개
  img1: 2551 x 3546 png   ← 페이지 전면 크기
```

발문은 텍스트로 있고, 그 뒤 <보기>(메모)와 선지 5개는 **이미지 안에** 있다.
텍스트 레이어에 존재하지 않으므로 어떤 추출기로도 나오지 않는다.

## 3. 🟢 기존 텍스트 47개는 손실이 없었다

| 항목 | 값 |
|---|--:|
| 대조 회차 | 47 |
| PyMuPDF 총 문자수 | 1,384,553 |
| 기존 대비 증감 | **0자** |
| 2% 넘게 차이난 회차 | **0개** |
| PDF 내 이미지 객체 | 1,625개 |
| 총 쪽수 | 816 |

**전 47회차에서 단 한 글자도 늘지 않았다.** 기존 `pdf_*.txt` 는 PyMuPDF 로 뽑은 것과 동일하며,
텍스트 레이어 기준으로 **완전하다.**

★ D-45 에서 제기한 우려 — 「내가 원문이라 부른 것이 어떤 손실을 안고 만들어졌는지 모른다」 — 는 해소됐다.
  손실은 추출 과정이 아니라 **PDF 자체**에 있다.

## 4. 다음 단계 — OCR

회차당 이미지가 평균 35개다. <보기> 박스·표·그림이 이미지로 들어간 사례가 많다.
이것이 「미대조 139건」과 「원문 확정 불가 10건」의 근본 원인이다.

판정 불가 39건(원문 확정 불가 10 + 기준점 없음 10 + 미확정 19)은 **재판정하지 못했다.**
텍스트가 늘지 않았으므로 재판정할 새 근거가 없다. OCR 이 선행되어야 한다.
