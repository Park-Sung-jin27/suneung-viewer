# PLAN-eng-diagnosis-v2 — 영어 진단 정밀화 (20문항 2단계 + 시간·확신도 분석)

> **트랙: 영어 뷰어 · 우선순위 3/5** (선행: PLAN-genie-display-contract)
> 근거: 현 진단은 12문항 1회 — 유형군 10개에 문항 1~2개꼴이라 "취약 Top3"의 신뢰도가 낮고, 컨설팅 리포트의 원료로 쓰기엔 표본이 얇다. 문항 풀은 868문항으로 충분하므로 로직만 고치면 됨.
> 라벨: [Confirmed] 현 진단 12문항/유형군 커버(빌더 보고+검증). [Inference] 20문항 2단계 설계의 변별 개선 — 파일럿 후 판정.

## 목표

진단을 2단계로 재설계: 1부 기본 12문항(유형군 전수, 중간 난이도) → 2부 맞춤 8문항(1부에서 틀린 유형군에 자동 집중 배정) = 총 20문항. 결과에 문항별 풀이 시간·확신도를 결합한 3분면 분석(맞음+빠름=강점 / 맞음+느림=불안정 / 틀림+확신=오개념 최우선)을 추가.

## 수정해야 할 정확한 파일

| 파일                                           | 작업                                                 |
| ---------------------------------------------- | ---------------------------------------------------- |
| `평가원_수학영어_확장/08_tool/지니학습기.html` | 진단 로직·결과 화면 수정 (수정 전 `.bak_diag2` 백업) |
| 데이터 파일들                                  | **전부 수정 금지** (로직만)                          |

## 단계별 작업 순서

1. **현 진단 함수 파악**: `grep -n "진단" 지니학습기.html` 로 문항 추출 함수·결과 계산 함수 위치 확인. 기존 12문항 추출 규칙(유형군 커버, answer 존재, 2023~2026)을 그대로 1부로 재사용.
2. **2부 배정 로직**: 1부 채점 후 유형군별 오답 수 집계 → 오답 많은 유형군 순으로 8문항 배정(유형군당 최대 3, 1부 출제 문항 제외, 동일 시험 편중 방지 — 시험별 최대 3). 전부 맞았으면 2부는 "고난도 세트"(corePatternTarget 중 31~34·39 위주 8문항).
3. **시간 기록**: 이미 문항별 타이머 표시가 있음 — 문항 진입~채점 시간을 결과 객체에 저장(초). 유형군 평균과 비교해 "느림" 판정(해당 유형 중앙값의 1.5배 초과 = 느림; 중앙값은 진단 세트 내 계산).
4. **결과 화면 v2**: ① 유형군별 정답률 막대(기존) ② **3분면 카드**: 강점(맞+빠) / 불안정(맞+느) / 오개념(틀+확신 '확실') / 학습 필요(틀+비확신) — 각 칸에 해당 유형·문항 수 ③ 취약 Top3는 "2부 재검증 반영" 문구로 신뢰도 표시 ④ 진단 이력 localStorage 저장 형식은 기존 키 하위호환 유지(genie_ prefix, 기존 이력 깨지 말 것).
5. **자가 검증 (node)**: 진단 추출·배정 함수를 노출 가능한 형태로 작성해 node에서 ① 1부 12문항 유형군 10개 커버 ② "1부 전부 오답" 시나리오에서 2부 8문항이 오답 유형에 배정 ③ "전부 정답" 시나리오에서 고난도 세트 ④ 시간 판정이 0초·동일값 등 극단에서 NaN 없음 — 4시나리오 실행 로그 첨부. node --check + 파일 안전 절차.

## 성능 낮은 모델이 놓치기 쉬운 엣지 케이스

1. **중도 이탈**: 1부만 하고 나가면? — 12문항 결과만으로도 리포트가 생성되게(2부는 "미실시" 표기). 진행 상태를 localStorage에 저장해 이어하기.
2. **answer 없는 문항 혼입 금지**: 2부 배정 풀도 answer 존재 필터 유지 (2021_csat 복원분은 answer 있음 — 사용 가능).
3. **유형군 문항 부족**: 특정 유형군(어법=시험당 1문항)은 후보가 적음 — 배정 요구가 후보보다 크면 있는 만큼만 배정하고 다른 취약 유형으로 채움 (무한루프 금지).
4. **타이머의 함정**: 탭을 벗어났다 돌아온 시간도 포함됨 — 판정에는 상한 캡(문항당 600초)을 두고, 캡 도달 문항은 "시간 판정 제외" 처리.
5. **확신도 3단계 매핑**: '찍었어요'는 맞아도 "강점" 아님 — 맞+찍음 = "운" 칸이 아니라 "불안정"으로 분류 (과대평가 방지).
6. **기존 진단 이력 하위호환**: 이력 구조를 바꾸면 기존 저장분 파싱에 try/catch — 구버전 이력은 "v1 진단"으로 표시만.
7. **리포트 연동**: 컨설팅 리포트 탭이 진단 결과 객체를 읽음 — 필드명 바꾸면 리포트가 깨짐. 기존 필드는 유지하고 신규 필드만 추가.

## 직접 검증할 수 있는 완료 기준 (대표용)

1. 진단 시작 → 12문항 후 "맞춤 8문항" 안내가 뜨고, 일부러 특정 유형만 틀리면 2부에 그 유형이 몰림.
2. 결과 화면에 3분면 카드가 보이고, '확실하다며 틀린' 문항이 오개념 칸에 표시됨.
3. 1부만 하고 브라우저를 닫았다 다시 열면 이어하기가 뜨고, 그 상태로도 리포트가 에러 없이 생성됨.
