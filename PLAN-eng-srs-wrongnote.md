# PLAN-eng-srs-wrongnote — 오답노트 간격반복(SRS) 재출제 큐 (영어·수학 공용)

> **트랙: 영어 뷰어 · 우선순위 4/5** (선행: PLAN-genie-display-contract. 수학에도 동일 적용되는 공용 기능이나 영어 트랙에서 발주)
> 근거: 마스터플랜 I57("SRS 오답 재출제")의 실행분. 현재 오답노트는 "쌓이기만" 하고 다시 꺼내 풀게 하는 루프가 없음 — 학습과학상 인출연습·간격반복이 성적 향상의 핵심 부품이고, "인강 없는 학습 루프"의 마지막 구멍.
> 라벨: [Confirmed] 오답노트 적재·재풀이 진입은 기존 구현 존재(빌더 보고). [Inference] 간격 스케줄 효과 — 파일럿.

## 목표

오답 문항에 SM-2 간이형 스케줄(1일→3일→7일→21일)을 붙여, 홈 화면에 "오늘의 복습 N문항" 큐를 만들고, 복습 정답 시 다음 간격으로/오답 시 1일로 리셋한다.

## 수정해야 할 정확한 파일

| 파일                                           | 작업                                                             |
| ---------------------------------------------- | ---------------------------------------------------------------- |
| `평가원_수학영어_확장/08_tool/지니학습기.html` | 오답노트 데이터 구조 확장 + 복습 큐 UI (수정 전 `.bak_srs` 백업) |
| 데이터 파일들                                  | **수정 금지**                                                    |

## 단계별 작업 순서

1. **현 오답노트 구조 파악**: `grep -n "오답" 지니학습기.html` — localStorage 키·저장 객체 필드 확인. 기존 데이터를 깨지 않는 것이 제1원칙.
2. **스키마 확장 (하위호환)**: 오답 항목에 `srs:{stage:0, due:"2026-07-15", history:[{date,correct}]}` 필드 추가. 기존 항목(필드 없음)은 로드 시 stage 0·due 오늘로 마이그레이션(1회, in-memory→저장).
3. **간격 규칙**: stage 0→1일, 1→3일, 2→7일, 3→21일, 4=졸업(큐 제외, 오답노트에는 남김). 복습 정답 시 stage+1, 오답 시 stage=0. 확신도 '찍었어요'로 맞힌 경우 stage 유지(승급 없음).
4. **복습 큐 UI**: ① 홈(수능 알기 탭 상단 또는 학습 탭 헤더)에 "오늘의 복습 N문항" 배지+버튼 (N = due ≤ 오늘) ② 클릭 시 복습 모드: 기존 문항 화면 재사용하되 상단에 "복습 (stage n/4)" 표시 ③ 복습 완료 화면: 오늘 처리 수·졸업 수·내일 예정 수.
5. **자가 검증 (node)**: 스케줄 함수를 순수 함수로 작성해 node에서 ① 오답→1일 후 due ② 정답 연속 4회 → 졸업 ③ 3단계에서 오답 → 1일 리셋 ④ 찍고 맞음 → stage 유지 ⑤ 구버전 항목 마이그레이션 — 5케이스 로그 첨부. node --check + 파일 안전 절차.

## 성능 낮은 모델이 놓치기 쉬운 엣지 케이스

1. **날짜 비교는 로컬 자정 기준**: `new Date().toISOString()` 은 UTC라 한국 저녁에 날짜가 밀림 — `YYYY-MM-DD`를 로컬 기준으로 만들어 문자열 비교할 것.
2. **같은 문항 중복 오답**: 이미 오답노트에 있는 문항을 또 틀리면 항목을 새로 만들지 말고 기존 항목 stage=0 리셋.
3. **정답 없는 수학 문항**: 자동 채점 불가 문항은 복습 큐에 넣지 말 것 (자기 채점 문항은 srs 없이 목록만).
4. **큐 폭주**: 방치 후 복귀하면 due 지난 문항이 수십 개 — 하루 큐 상한 20문항(오래된 순), 나머지는 "밀린 복습 M건" 표기.
5. **localStorage 용량**: history 배열이 무한 성장하지 않게 최근 10회만 유지.
6. **진단 문항과 분리**: 진단 중 오답은 진단 종료 후 일괄 적재(진단 도중 큐 배지가 실시간으로 바뀌면 혼란).
7. **졸업 문항 부활**: 학습 모드에서 같은 문항을 또 틀리면 졸업 취소·stage 0 (졸업은 영구가 아님).

## 직접 검증할 수 있는 완료 기준 (대표용)

1. 문항 2개를 일부러 틀림 → 홈에 "오늘의 복습 2문항" 배지. 복습에서 1개 맞히고 1개 틀림 → 배지가 1로 줄고, 틀린 것은 내일 다시 등장(개발자도구에서 due 날짜 확인 가능).
2. 어제 만든 오답(구버전 데이터)이 있어도 에러 없이 큐에 흡수됨.
3. 개발자도구 Application→localStorage에서 srs 필드(stage/due)가 보이고, history가 10개 이하로 유지됨.
