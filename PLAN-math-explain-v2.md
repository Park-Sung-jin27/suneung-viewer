# PLAN-math-explain-v2 — 수학 해설 정본화 파일럿 (logicFlow 검증 → v2 구조)

> **트랙: 수학 뷰어 · 우선순위 5/5** (선행: PLAN-math-answers-v13 — 정답 없이 해설 검증 불가)
> 근거: 현재 수학 힌트는 ProbDex AI 생성물(미검증)을 "참고용" 배지로 노출 중. 정답 확보 후 이를 **검증·재작성**하면 수학도 국어 v2 해설 수준의 학습 루프가 완성됨. 단, 322문항 전체는 대형 작업이라 파일럿(2026수능 공통 22문항)으로 방식 확정이 먼저.
> 라벨: [Confirmed] meta.logicFlow 322문항 존재. [Unverified] logicFlow 정오율 — 본 파일럿이 측정.

## 목표

2026수능 공통 1~22번의 해설을 "검증된 수학 v2 구조"로 재작성: 🎯 핵심 아이디어 1줄 → 🧭 접근(어느 단원 도구를 꺼내나) → 풀이 단계(각 단계 = 한 동작) → ⚡ 함정(pitfalls 검증분) → ✅ 답 확인(검산법). 산출물은 별도 파일(원본 DB 무수정).

## 수정해야 할 정확한 파일

| 파일                                                                 | 작업                                                      |
| -------------------------------------------------------------------- | --------------------------------------------------------- |
| `평가원_수학영어_확장/08_math_data/explain_v2_pilot_2026common.json` | 신규 (22문항 해설)                                        |
| `평가원_수학영어_확장/08_math_data/logicflow_audit_2026common.md`    | 신규 (logicFlow 정오 감사 결과)                           |
| math DB·data_math.js                                                 | **이 단계에서는 수정 금지** (대표 검수 통과 후 별도 병합) |

## 단계별 작업 순서

1. **logicFlow 감사 (검증이 먼저, 재작성은 나중)**: 2026수능 공통 22문항 각각에 대해 ① 문제를 직접 푼다(sympy 병용: `pip install sympy --break-system-packages`) ② 내 답 = DB answer 인지 확인 (불일치면 그 문항은 STOP하고 감사 파일에 기록 — 정답키 재검 신호) ③ meta.logicFlow의 풀이 경로가 수학적으로 옳은지 판정(옳음/부분오류/틀림) → logicflow_audit에 문항별 판정 기록.
2. **해설 작성**: 감사에서 "옳음"인 logicFlow는 골격으로 재사용(문장은 전면 재작성), "부분오류/틀림"은 내 풀이로 대체. 구조(필드명 고정):
   ```json
   {
     "id": "2026_csat_common_9",
     "hook": "🎯 ...(1문장)",
     "approach": "🧭 ...(어느 개념을 왜 꺼내는지 1~2문장)",
     "steps": ["...", "..."],
     "pitfall": "⚡ ...(감사 통과한 함정만, 없으면 빈 문자열)",
     "check": "✅ ...(답 검산 한 줄: 대입/역산)",
     "verifiedBy": "solve+sympy|solve",
     "logicFlowVerdict": "ok|partial|wrong"
   }
   ```
3. **눈높이 규칙**: 타깃 3~4등급 — 한 step = 한 동작("양변을 3^x로 나눈다"), 건너뛰기 금지, "자명하다" 금지, 계산 중간값 명시. 단답형은 마지막 step에 최종 정수.
4. **자가 검증**: json.load + 22문항 전부 steps ≥ 2 + check 비어있지 않음 + 각 문항 "내 풀이 답 == DB answer" 로그 첨부.
5. **대표 검수 → 승인 시 후속**: 병합(별도 발주)과 확산 순서(2026 전체 → 최신 수능 역순) 제안까지만 문서에 남김.

## 성능 낮은 모델이 놓치기 쉬운 엣지 케이스

1. **logicFlow를 그대로 베끼면 안 됨**: AI 생성물이라 그럴듯한 오류가 섞여 있을 수 있음(미검증) — 반드시 자기 풀이로 재도출한 뒤 "경로가 같은지"만 참고. 검증 없이 문장만 다듬는 것은 이 계획의 실패.
2. **내 답 ≠ DB answer일 때**: 내 풀이를 의심해 2회 재검토 → 그래도 다르면 해설을 쓰지 말고 감사 파일에 "정답키 요재검"으로 기록. 틀린 정답에 맞춘 해설은 최악의 산출물.
3. **sympy 한계**: 기하 증명형·경우의수는 sympy로 못 푼다 — 손풀이로 하되 수치 검산(특수값 대입)이라도 check에 남길 것. "검산 생략"을 verifiedBy에 정직 표기.
4. **[n점]과 난이도**: 4점 문항의 steps가 3개 이하면 건너뛴 것 — 4점은 보통 5~8 step.
5. **LaTeX**: steps 안 수식도 $...$ KaTeX 표준 + JSON \\ 이스케이프 (기존 규칙 동일).
6. **⚡ pitfall 톤**: "출제자가 노렸다" 단정 금지 — "…에서 실수하기 쉽다" 관찰 서술.

## 직접 검증할 수 있는 완료 기준 (대표용)

1. `logicflow_audit_2026common.md` 를 열면 22문항 표(내답=DB답 여부 / logicFlow 판정)가 있고, **불일치 은폐 없이** 집계돼 있음.
2. explain_v2_pilot JSON에서 아무 4점 문항(예: 22번)을 열어 steps를 따라가면 실제로 풀림 (강사 눈 3분 검수).
3. PowerShell: `python -c "import json; d=json.load(open(r'C:\Users\downf\suneung-viewer\평가원_수학영어_확장\08_math_data\explain_v2_pilot_2026common.json',encoding='utf-8')); print(len(d), all(len(x['steps'])>=2 for x in d))"` → `22 True`.
