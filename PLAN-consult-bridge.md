# PLAN-consult-bridge — 진단 리포트 → 입시 컨설팅 연계 (리포트 v2)

> **우선순위: 5 / 5**
> 근거: 학습기 진단 리포트를 "학습 코칭 리포트"에서 "입시 컨설팅 리포트"로 승격 — 회사 비전(진단→플래너→학부모 리포트→입시 컨설팅)의 연결 고리이자 김과외 강사 상품과의 직결점. 단, 데이터(진단 정확도·정답 완결)가 먼저라 마지막 순위. 컨설팅_데모 폴더에 입시 데이터가 이미 존재해 신규 수집 불요.
> 라벨: [Inference] 컨설팅_데모 데이터 스키마는 본 계획 1단계에서 실사 필요 (git 추적 해제된 로컬 폴더 — 존재는 [Confirmed]).
> **선행 조건: PLAN-math-answers-v13, PLAN-genie-display-contract 완료 후.**

## 목표

진단 결과 화면과 인쇄 리포트에 ① 등급 참고 맥락(영어 절대평가 등급컷 + 수학 등급은 상대평가라 단정 불가 고지) ② 등급대별 참고 대학 라인 표(컨설팅_데모 데이터 발췌, [참고용] 라벨) ③ 학부모용 요약 섹션을 추가한다.

## 수정해야 할 정확한 파일

| 파일                                                                               | 작업                                                        |
| ---------------------------------------------------------------------------------- | ----------------------------------------------------------- |
| `컨설팅_데모/data/jeongsi_cutoffs.json`, `clean_unified.json`, `universities.json` | **읽기 전용** — 스키마 실사 후 필요 행만 발췌               |
| `평가원_수학영어_확장/08_tool/data_consult.js`                                     | 신규 (발췌 데이터, `window.GENIE_CONSULT = {...}`)          |
| `평가원_수학영어_확장/08_tool/지니학습기.html`                                     | 리포트 탭 확장 (수정 전 `지니학습기.html.bak_consult` 백업) |

## 단계별 작업 순서

1. **데이터 실사 (가정 금지)**: `python3 -c "import json; d=json.load(open('<mount>/컨설팅_데모/data/jeongsi_cutoffs.json')); print(type(d)); ..."` 로 실제 스키마(키 이름·등급 표현 방식·연도)를 출력해 확인. clean_unified.json, universities.json도 동일. **스키마를 확인하기 전에는 어떤 코드도 쓰지 말 것.**
2. **발췌 기준 설계**: 영어·수학 등급(또는 백분위) 조건이 있는 대학 라인만 추린다. 목표 크기: `data_consult.js` ≤ 200KB (전체 임베드 금지). 발췌 필드: 대학명 / 모집단위(계열 수준으로 묶기) / 요구 등급대 / 연도. 파일 상단 주석에 "출처: 컨설팅_데모 (수집 연도 명기), 참고용 — 합격 예측 아님".
3. **리포트 v2 UI** (리포트 탭에 3블록 추가):
   - 블록 1 "등급 맥락": 영어는 절대평가 등급표(90/80/70…)에 [추정] 예상 등급 위치 하이라이트. 수학은 "상대평가 — 표본 진단만으로 등급 단정 불가"를 명시하고 취약 단원 Top3만 표시.
   - 블록 2 "참고 대학 라인": 학생의 [추정] 등급대 ±1 범위의 발췌 표 (최대 10행). 표 상단에 굵게: "참고용 자료입니다. 실제 지원 전략은 담임/컨설턴트와 상담하세요."
   - 블록 3 "학부모 요약": 3문장 자동 생성 (현재 위치 1문장 / 최우선 보완 1문장 / 4주 계획 1문장) — 전문용어 없이.
   - 인쇄(@media print)에 3블록 포함되는지 확인.
4. **검증**: script 추출 node --check / 발췌 데이터 node require 성공 / 등급 경계값(아래 엣지) 시뮬레이션.
5. **파일 안전 절차**: /tmp 작성 → 검증 → cat 복사 → 재읽기 확인.

## 성능 낮은 모델이 놓치기 쉬운 엣지 케이스

1. **합격 예측 표현 금지**: "합격 가능", "안정권" 같은 단정 문구를 쓰면 안 된다 (12문항 표본 진단 + 참고 컷 데이터로는 근거 불충분 + 법적 리스크). 허용 표현: "이 등급대에서 과거에 지원이 이뤄진 라인(참고)".
2. **수학 등급 추정 금지**: 영어는 절대평가라 점수→등급이 정의되지만, 수학은 상대평가(표준점수·백분위) — 진단 정답률로 등급을 계산하는 코드를 만들지 말 것. 취약 단원만 출력.
3. **등급 경계값**: 예상 점수 90.0(1·2등급 경계), 0점, 100점, 진단 미완료(수학 스킵) 케이스에서 리포트가 crash 없이 합리적 문구를 내는지 각각 시뮬레이션.
4. **컨설팅_데모 데이터의 연도**: 2027학년도 기준 수집분 — 리포트에 연도를 반드시 표기 (연도 없는 컷 표는 오해 유발).
5. **국어 성적 부재**: 이 학습기는 영어·수학만 진단 — 대학 라인 표가 "전 과목 기준 컷"이라면 그 갭을 각주로 고지 ("본 표는 참고이며 국어 등 타 영역 성적에 따라 달라짐").
6. **data_consult.js 크기**: jeongsi_cutoffs가 수천 행이면 전부 임베드하지 말고 등급 조건이 명확한 행만. 임베드 후 학습기 로딩이 눈에 띄게 느려지면 실패로 간주.
7. **컨설팅_데모 폴더는 git 추적 해제 상태** — 원본을 옮기거나 수정하지 말 것 (읽기만).

## 직접 검증할 수 있는 완료 기준 (대표용)

1. `지니학습기.html` → 진단 완료 → 리포트 탭: ① 영어 등급표에 내 위치 하이라이트 ② "참고 대학 라인" 표 (연도 표기 + 참고용 경고 문구) ③ 학부모 요약 3문장이 보임.
2. Ctrl+P 인쇄 미리보기: 리포트만 (탭 메뉴·학습 화면 없이) 3블록 포함 출력.
3. 수학 진단을 건너뛰고 영어만 진단해도 리포트가 에러 없이 생성되고, 수학 섹션에 "진단 미실시" 표기.
4. 리포트 어디에도 "합격"이라는 단어가 없음 (Ctrl+F로 확인 가능).
