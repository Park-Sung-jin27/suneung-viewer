# Codex 운용 지침 (품질 심사관 정본, 2026-07-24)

> 목적: 솔로 창업자(대표)가 Codex를 안전·고ROI로 반복 활용하기 위한 단일 기준.
> 근거: 2026-07-24 세션 실증(추출 3차·v2 감사·answerGuide 수정). 정본 규율은 CLAUDE.md §13.

---

## 0. 한 줄 원칙

**Codex는 "손", 심사관은 "눈", Claude Code는 "수술칼".**
Codex에 시킬지 말지는 딱 하나로 결정한다 — **"이 작업이 의미 판단을 요구하는가?"**
- 아니오(기계적·결정적·사양 명확) → **Codex 적합**
- 예(의미·애매·"이게 맞나/환각인가") → **Codex 금지**, 심사관/Claude Code 몫

---

## 1. 적합 / 부적합

| ✅ Codex 적합 (이번 실증) | ❌ Codex 금지 |
| --- | --- |
| 대규모 **읽기전용 추출·렌더·덤프**(재료 생산) | **의미 판단 확정**(환각/오변환/오학습 "이다") |
| 코드·데이터 **감사**(결함 *후보* 발굴) | **무검증 대량 재생성**(해설 등) |
| **잘 정의된 surgical fix**(사양 결정적 + 양성회귀 가능) | 애매한 사양의 **아키텍처 결정** |
| 정답표·PNG·전역 grep 재료 등 반복 노동 | **발주 밖 자율 확장**(스코프 크리프) |

핵심: 감사에서 Codex는 "후보"까지만. **판정(진짜 결함인가)은 심사관이 독립 검증**한다(이번 722 부정발문처럼).

---

## 2. 표준 발주 4요소 (이번에 작동한 형식)

모든 Codex 발주는 이 4개를 자족적으로 담는다:
1. **대상** — 어떤 파일/범위 (정확 경로)
2. **사양** — 무엇을 어떻게 (결정적으로. 애매하면 발주 금지)
3. **검증** — §13⑮(7) **양성 회귀**: 잡혀야 할 기지 케이스가 실제로 잡히는지 먼저 확인. "0건/통과"는 그 후에만 신뢰
4. **규율** — 금지 3종 고정: **판정 금지 · 데이터 수정 금지 · git(add/commit/push) 금지**

+ 교차검증 의뢰 시 **목적만 전달, 판정식 비공개**(§13⑱ — 식을 주면 상대가 복제해 같은 사각 공유).

---

## 3. 가드레일 (이번 세션이 실증한 위험 기반)

- **"발주 밖 작업 금지, 끝나면 대기"를 매 발주 말미에 명시** — Codex는 유휴 시 자율 착수 경향(3차 추출·201 window-fill이 그 사례. 다행히 정본 무변경).
- **산출물은 `pipeline/output/`(.gitignore)로**, 커밋은 승인 후 **path 지정**(공유 트리에 타 직원 미커밋 변경이 `git add .`로 딸려감).
- **"완료 보고 ≠ 완료"(§13⑯)** — 심사관은 Codex 보고를 액면 수용 X. **배포본을 `git show origin/main:<path>`로 직접 조회**해 확인(이번에 스테이징 사본이 옛 버전이라 하마터면 오판).
- **규모 주장은 독립 카운트**(§13⑮) — "722"·"146 unfilled"을 그대로 믿지 말고 별도 각도로 재확인(146은 위양성 다수였음).

---

## 4. 3자 워크플로우

```
Codex     : 재료 생산 · 감사 후보 · 수정 초안 (판정·데이터수정·git 금지)
   ↓
심사관    : 독립 검증(코드 직접 읽기·독립 카운트·배포본 대조) → 판정(수용/부분/거부)
   ↓
적용      : Claude Code(주) 또는 Codex(승인 후) — push는 한 번에 한 엔진(§4)
   ↓
대표      : 전략·우선순위·출시·인력 = 최종 승인
```

---

## 5. Claude Code와의 경계 (교체가 아니라 병행)

| 엔진 | 역할 | push |
| --- | --- | --- |
| **Claude Code** | 주 데이터 엔지니어 — D엔진·복잡 JSON 수술(§13⑪)·게이트 로직·본문 교정 | 주체 |
| **Codex** | 병렬 보조 — 추출·감사·surgical fix. **Claude 플랜 한도 시 연속성** | 승인 후 |

두 엔진 병행 = §13⑱ 독립 매처 교차검증을 공짜로 얻음. 단 **동시 push 금지**.

---

## 6. Codex 주력 승격 조건 (provisional → confirmed)

지금은 며칠 실적 = provisional(§교훈 15). 아래 3개를 2~3회 통과하면 "주력 승격" 재검토:
1. 실제 데이터 과제(예: haesol_v2_gate 재설계·oldhangul_audit 개선)에서 **§13 규율 위반 0**
2. 심사관 독립 검증 **통과**
3. Claude Code 동급 대비 품질·속도 우위

⚠ 승격(주 데이터 엔지니어) 선결 조건 = **AGENTS.md에 CLAUDE.md §13 전체 이식**. 현재 AGENTS.md에 §13(git-object 우회·검사기 무결성·축 상호억제·첫가끝)은 **0건**. 이식 없이 주력 전환 금지.

---

## 7. 확정 절차 (2026-08-08 세션 실증)

이번 세션(추출기 3회 수정·리허설·마커 5축 정비)에서 **실측으로 확정된** 절차 3건. 근거를 함께 남긴다 — 근거 없는 규칙은 다음 사람이 되돌린다.

### 7-1. 추출기 수정 → `extractor_corpus_check` 통과가 push 전제

```bash
node pipeline/extractor_corpus_check.mjs        # 악화 1건이라도 있으면 exit 1
node pipeline/extractor_corpus_check.mjs --update   # 심사관 승인 후에만
```

- 기준선 `pipeline/extractor_corpus_baseline.json` — 시험지 **49종 · 공통 1~34 · 정상5선지 1,518/1,643**.
- **`--update` 는 심사관 승인 후에만.** 악화가 기준선에 흡수되면 회귀 자체가 무의미해진다.

**근거**: 픽스처만으로는 추출기 안전이 보장되지 않는다. 두 번 연속 실증 —
- ca: 픽스처 3케이스 통과했으나 실전에서 4문항 파손. **전 코퍼스로 재보니 47세트**였다(1종 측정이 규모를 한 자릿수로 과소평가).
- cd 1차: 목표(Q16)는 못 고치고 **Q31을 새로 깨뜨림**. 즉시 롤백.

예시문항 1종이 아니라 **전 조판으로 A/B**를 해야 한다.

### 7-2. minified 대용량 JSON은 rebase·cherry-pick 불가 — 치환 스크립트 재실행이 정본

`public/data/all_data_204.json` 은 minified 단일 라인이라 3-way merge 가 성립하지 않는다. base 가 전진하면:

1. `git cherry-pick` 을 시도하지 말 것 (충돌 확정)
2. **최신 base 에서 브랜치를 다시 따고, 치환 스크립트를 재실행**한다
3. 역치환 바이트 동일 증명 + 전역 grep 잔존 0 을 매번 다시 통과시킨다

**근거**: bu(`bc76228`) cherry-pick 이 충돌 → abort 후 최신 데이터에 4개 치환 재실행으로 해결. 강제 해결하지 않았다. 이 방식이면 base 가 몇 커밋 전진했든 결과가 결정적이다.

### 7-3. `<보기>` 박스를 발문 앵커로 못 찾으면 특징 어절 전문 검색으로 재시도

발문 바로 뒤에 없다고 "추출 불가"로 단정하지 말 것. **박스가 멀리 떨어져 추출될 수 있다.**

**근거 (양방향)**:
- `l20196b` Q31 — 발문 뒤에는 `① ⓐ ② ⓑ …` 만 있고 박스가 없었다. 특징 어절 `겸손한 자연` 으로 전문 검색하니 **오프셋 @25834**(33번 문항 뒤)에 전문이 그대로 있었다. 추론 없이 verbatim 복원 성공.
- `r2020e` Q19 — 같은 기법을 5개 어절(`체육관`·`흰색 수첩`·`공동 발표`·`수첩`·`이름이 적힌`)로 시도했으나 `-raw`·`-layout` 모두 **0회**. 텍스트 레이어 자체가 없는 진짜 추출 불가. 이때는 추론으로 마커를 배치하지 말고 **`[Inference]` 로 표시하고 멈춘다**.

즉 이 절차는 "찾을 때까지 시도"가 아니라 **"못 찾았음을 확정하는 방법"**이기도 하다.

### 7-4. (부수) 텍스트 리더는 단(column) 분리형만

`pdf-parse` · `pdftotext -raw` = 단 단위 분리 → **정상 32/36**
`pdftotext -layout` = 시각적 행 병합 → **붕괴 8/19, 블록 36→19**

2단 조판 시험지에서 `-layout`·`pdfplumber` 계열로 교체 금지. `pdf_text_extractor.readerSanity()` 가 임계 50‰ 초과 시 추출 단계에서 중단한다(실측: `-raw` 38.0‰ vs `-layout` 60.8‰).

### 7-5. 신규 yearKey 도입 시 setId 충돌 점검이 선결

`step2_extract` 는 `r{연도2자리}{a,b,c…}` 규칙으로 setId 를 생성하므로, **연도 숫자가 같으면 다른 시험이라도 setId 가 충돌**한다.

**실증(2022예시 리허설)**: 산출 setId `r2022a`·`r2022b`·`r2022c`·`l2022a`·`l2022b` **5개 전부**가 2022수능 setId 와 동일했다(range 는 서로 다름).

`RELEASE_KEYS` 는 `yearKey::setId` 복합키라 구분되지만, **setId 단독으로 참조하는 코드**(§13 release 안전룰 ③의 2014~2016 A/B형 충돌과 같은 유형)에서 혼선이 난다.

→ 신규 yearKey 진입 시 **step6 병합 전에 setId 전역 충돌 검사**를 의무화하고, 충돌 시 접미사(예: `r2022xa`)로 분리한다.

### 7-6. range 결손 판정 매처의 한계 — 최종 분류는 사람이 한다

`range` 대비 문항 결손 판정 매처는 **PDF 에 그 번호가 실재하는지만** 보고, **그 문항이 해당 지문 소속인지는 판별하지 못한다.**

따라서 (가) 진짜 결손 / (나) range 오표기 의 **최종 분류는 발문 내용을 사람이 읽어 확정**한다.

**오분류 실증 (2026-08)**:
- `2017_9월 l20179c` — 26·27 이 PDF 에 실재해 (가)로 분류됐으나, 실제 발문은 `윗글의 내용에 대한 이해로`·`윗글을 바탕으로 추론한` 으로 **건축 독서 지문 문항**이었다. 이 세트는 윤동주 <병원>·박목월 <나무> 다 → (나).
- `2016수능B r2016aB` — 16 이 실재해 (가)로 분류됐으나 `<보기>를 바탕으로 중세 국어의 특징을 탐구한` = **문법 독립 문항** → (나).

매처 출력은 **후보 목록**이지 판정이 아니다(§1 "감사에서 Codex 는 후보까지만" 과 같은 원칙).

### 7-7. LLM 생성 해설은 게이트 통과가 품질 근거가 되지 않는다

게이트 5종(quality_gate·haesol_v2·answer_fidelity·structure_fidelity·passage_fidelity)은 **형식축**만 본다. 내용 정확성은 게이트 밖이다.

**실증(2026-08 r20206d Q40~42)**: 게이트 전종을 통과한 신규 해설 15선지에서 대표 검수로 실질 오류가 잇달아 나왔다 — `ⓓ` 를 '핵 존재'로, `ㄴ·ㄹ` 을 '소포체·엽록체'로 지어냈고, '밝혀지다'와 '조명되다'가 한자를 공유한다는 틀린 설명까지 있었다. 어느 것도 게이트가 잡지 못했다.

→ **LLM 생성·재작성 해설은 push 전 대표 검수를 전제로 한다.** 게이트 통과는 출고 조건이지 품질 근거가 아니다.

### 7-8. `git add <path>` 한정 — `git add .` 금지

공유 트리에는 타 직원의 미커밋 변경과 이 세션의 미커밋 검사도구가 상시 쌓여 있다. `git add .` 는 그것을 통째로 끌고 들어간다.

- 커밋은 **경로를 명시**해서만 스테이징한다.
- **커밋 후 `git status` 로** 의도한 파일만 들어갔는지, 나머지가 미스테이징으로 남았는지 확인한다.

**실증**: 워크트리에 축5/축6 게이트 수정본(미커밋)이 계속 있었고, `git add .` 였다면 데이터 커밋마다 게이트 변경이 딸려 나갔다.

### 7-9. 데이터 수정과 검사도구 수정은 같은 커밋에 넣지 않는다

섞으면 (a) 롤백 단위가 사라지고 (b) "이 PASS 가 어느 게이트로 나온 것인가"를 나중에 판별할 수 없다.

**실증**: co 검증 시 워크트리 게이트가 `origin/main` 과 달라 PASS 유효성을 다시 증명해야 했다(게이트를 원본으로 되돌려 재실행). 두 축이 한 커밋에 있었다면 이 확인 자체가 불가능했다.

### 7-10. 새 매처는 3관문을 통과하기 전에 수치를 보고하지 않는다

```
(a) 양성 fixture 검출   — 잡혀야 할 기지 케이스가 실제로 잡히는가
(b) 오탐률 실측         — 정상 데이터가 대량으로 걸리지 않는가
(c) 기존 게이트와 중복  — 이미 다른 축이 담당하는 영역인가
```

셋을 통과하기 전에는 **"0건"·"N건"을 결과로 보고하지 않는다.**

**실증(2026-08, 같은 세션에서 반복)**:
- `❌` 를 내부 어휘로 넣었다가 LIVE 2,286건이 전량 오탐 — (b) 미실시.
- 참조 결손 매처 1차가 28.61% 검출 — (b) 미실시. 좁힌 뒤 1.19%.
- 그 매처의 규칙 3이 `haesol_v2` §2 와 중복이라 2,167건이 허수 — (c) 미실시.
- 혼입 패턴 매처 4개 중 3개가 죽은 채 "0건" — (a) 미실시.
- 검사기 정규식을 셸 경유로 고쳐 매칭이 죽은 채 "0건" — (a) 미실시.

"0건"은 (a) 를 통과한 뒤에만 신뢰할 수 있는 신호다.

### 7-11. 같은 유형 결함 3건 = 데이터 수정 중단, 생성 경로 조사

같은 형태의 결함이 3건 나오면 개별 수리를 멈추고 그 산출물을 만든
코드 경로를 먼저 조사한다. 개별 수리는 원인 특정 후 재개한다.
근거: 2026-08-11 세션에서 동일 유형 해설 결함 8건을 개별 수리한 뒤
21번째 발주(cm)에서야 근본원인(step3 프롬프트 bogi 미전달)을 특정했다.

### 7-12. 발주에도 근거 의무

심사관의 발주가 수치·규칙·도구 존재를 전제하면 그 전제의 출처를
발주문에 명시한다. 명시할 수 없으면 "미검증 전제"로 표기하고
수신자에게 실측을 먼저 지시한다. §13⑯(완료 보고 ≠ 완료)의 발주판이다.
근거: 같은 세션에서 실측 없는 발주 오류 7건 — LEADING 회귀 47세트,
`❌` 오탐 2,286, 규칙3 중복 2,167 등.

### 7-13. 새 매처 사전 판정

발주 전에 "이 결함은 문자열 대조만으로 판정 가능한가"를 먼저 답한다.
의미 판정이 필요하면 매처를 만들지 않는다.
근거: reference_deficit_scan 92건 중 오탐 약 80%. 요약 라벨의
진위는 문자열로 갈리지 않는다.

### 7-14. 표본 검수 선행

대표 검수는 전수가 아니라 표본 3건으로 먼저 실행하고, 그 결과로
결함 유형을 특성화한 뒤 자동화 대상을 정한다. 자동화를 먼저 짓지 않는다.
근거: 같은 세션에서 대표 육안 검수(표본 3건 중 2건 적발)가
게이트 5종 전부보다 정확했다.

### 7-15. 1발주 1층위 · 독립 항목 병렬

한 발주에 서로 다른 층위(문자열 수정 / 메타데이터 / LLM 생성)를 섞지
않는다. 반대로 서로 의존하지 않는 조사 항목은 한 발주에 묶어 병렬 처리한다.

### 7-16. 검증 스크립트의 엄격도는 게이트 이상

자체 검증의 비교 엄격도는 게이트와 같거나 더 엄격해야 한다.
게이트가 raw exact로 보는 축에 정규화 비교를 쓰지 않는다.
근거: 발주 cw에서 자체 검증이 공백을 정규화해 통과시킨 인용을
게이트가 CRITICAL로 잡았다("배비장" vs 원문 "배 비장").

### 7-17. 발주문의 [Confirmed]는 직접 측정한 값만

발주 작성 시점에 직접 측정한 값에만 [Confirmed]를 붙인다. 다른 문서에서
옮겨 적은 값은 [Unverified · 출처: 문서명]으로 표기한다. 상태 문서의
기재값은 작성 시점 스냅샷이며 현재 사실을 보증하지 않는다.
근거: 발주 cx에서 current_state.md의 jippi-bo 상태를 복사해
[Confirmed]로 표기했으나 실제와 달랐다.

해시·크기·건수를 옮길 때는 종류와 단위를 함께 적는다.
`blob` / `md5` / `sha256`, 바이트 / 줄 / 건. 실증: 2026-08-11 발주 db 에서
심사관이 md5 값을 종류 표기 없이 옮겨 수신자가 blob 해시로 대조했고
4개 전부 불일치로 보고됐다. 두 값 모두 정확했으나 종류가 달랐다.

### 7-18. 발주 블록은 자족적

발주 블록은 이전 메시지를 "위"로 참조하지 않는다. diff·표·정박 문장·
수치는 블록 안에 다시 싣는다. 수신자는 블록만 받는다.
근거: 발주 cy[1]의 diff가 전달되지 않아 수신자가 의도를 추정해 적용했다.

### 7-19. 심사관은 수정의 효과를 단정하지 않는다

"이 수정으로 X가 해소된다"는 적용 후 실측 보고로만 확정한다.
근거: 발주 cy[2]에서 심사관이 "내부 메모 노출이 사라진다"고 단정했으나
해당 커밋은 결론줄만 고쳤고 본문 노출은 남았다.

### 7-20. 결론줄 규약 — 명사형 마감

해설 결론줄은 명사형으로 마감한다. 완결 문장(~이다 / ~습니다)을 쓰지 않는다.
패턴 라벨 접미(`— 패턴: 사실 바꿔치기(R1)`)는 명사형 계열이며 정상 표기다.

실측 근거 (2026-08-11, RELEASE_KEYS 241키 / 결론줄 4,740선지)
  명사형 마감 계열  4,573  96.5%
  완결문장            130   2.7%
  절단·기타            37   0.8%
  연도 43개 중 38개가 명사형 주류

파생 규칙 — 패턴 라벨 노출의 정상·누출 구분
  정상: 패턴 라벨을 그 선지의 오답 유형으로 제시한다
        예) "— 패턴: 사실 바꿔치기(R1)"
  누출: 패턴 라벨이 잘못 부여됐다는 등 메타데이터 자체를 논한다
        예) "pat=R2로 표시되어 있으나 도메인이 문학이므로 잘못 부여되었다"
  internal_leak_check 는 전자를 탐지 대상에서 제외한다.

해설 생성 발주 시 이 규약을 프롬프트에 명시한다. 명시하지 않으면 문체가 흔들린다.
실증: 2026-08-11 발주 cw[1]에서 심사관 승인분 3선지가 규약을 이탈했고,
같은 문항 안에 두 규약이 공존하게 되었다.

### 7-21. `node -e` 에 정규식을 담지 않는다 — 예외 없음

스크립트 패치·일회성 검사 모두 파일로 작성한 뒤 실행한다.
셸을 거치면 백슬래시·중괄호·대괄호가 소실된다.
실증: 2026-08-11 하루 4회 파손 (LEADING 정규식·표 파싱·다중파일 삽입·RELEASE_KEYS 추출).
구문 오류로 드러난 경우도 있으나, 조용히 0건을 반환한 사례가 이전 세션에 4회 있었다.

### 7-22. 검사 도구는 커밋하거나 폐기한다

검사 도구를 미커밋 상태로 워크트리에 남기지 않는다. 남기면 (a) 존재를 몰라
중복 개발이 발생하고 (b) 그 도구로 잰 수치가 배포본 기준인지 증명할 수 없다.

실증: 2026-08-11 기준 quality_gate.mjs · haesol_v2_gate.mjs 의 축5·축6 판본이
8개 워크트리에 미커밋으로만 존재했다. 심사관이 이를 모른 채 발주 cs[2]에서
internal_leak_check 를 중복 개발했고, 같은 기간의 모든 CRITICAL 0 이
배포 게이트 기준인지 증명되지 않았다.

파생: 도구 파일을 커밋했다는 사실이 그 검사가 활성화됐다는 뜻은 아니다.
연결 여부는 별도로 확인하고 별도로 보고한다.

### 7-23. 관문과 참고 도구를 구분한다

**관문 (release gate) — 출시를 차단할 권한을 가진 검사**

  node pipeline/quality_gate.mjs --scope=release   → CRITICAL 0

  이것만이 관문이다. 관문 통과는 출시의 필요조건이며 충분조건이 아니다
  (해설 내용 정확성은 대표 표본 검수가 유일한 방어선 — §7-7·§7-14).

**참고 도구 (advisory) — 결함 후보를 제시하는 검사**

  haesol_v2_gate · reference_deficit_scan · marker_audit
  internal_leak_check · extractor_corpus_check · marker_range_check

  이들의 산출은 후보이지 결함 확정이 아니다.

**보고 규율**

  참고 도구의 결과를 "FAIL" "결함 N건"으로 보고하지 않는다.
  "후보 N건"으로 표기하고, 확정은 원문 대조 또는 사람 판정을 거친 뒤에만 한다.
  참고 도구의 PASS/FAIL 을 세트 품질의 판정으로 인용하지 않는다.

**주의 — 강등은 무시해도 된다는 뜻이 아니다**

  참고 도구는 실제 결함을 여러 건 찾아냈다(internal_leak_check → l20229a Q19
  내부 메모 노출, marker_audit → 마커 계열 불일치). 강등의 의미는
  "검출 = 결함"이 아니라 "검출 = 확정 전 단계"라는 것뿐이다.

**관문 승격 조건**

  (a) 오탐률 실측 5% 이하  (b) 양성 fixture 검출  (c) 기존 관문과 중복 확인
  세 관문을 통과한 뒤 심사관 판정과 대표 승인을 거쳐 승격한다 (§7-10·§7-13).

실증: 2026-08-11 측정. 공개 세트 241개 중 haesol_v2 PASS 75 / FAIL 166(69%).
게이트 두 판본에서 동일하므로 축5·축6과 무관한 기존 상태다.
그동안 개별 세트의 FAIL 이 결함 신호로 읽혀 왔으나 대다수가 그 상태였고,
따옴표 오인 등 오탐이 섞여 있어 어떤 수정으로도 PASS 가 되지 않는 세트가 있다.

**선행 규정과의 관계**

본 조는 CLAUDE.md §13⑰의 "게이트 3종(haesol_v2·quality_gate release·
answer_fidelity) 전 구간 동일 적용, 생략 금지"를 대체한다. §13⑰ 및
docs/handover_data_engineer_2026-07-15.md 의 "haesol_v2 6축 100% PASS" 서술,
current_state.md 의 "5게이트" 표기는 본 조 기준으로 갱신 대상이다.
갱신은 지침 통합 시 일괄 처리하며, 그 전까지 본 조가 우선한다.

### 7-24. bogi·본문 결손은 해설 내용으로 채우지 않는다

<보기>·본문이 데이터에 없을 때, 그 자리를 기존 해설의 서술로 복원하지 않는다.
복원 원본은 PDF 원문뿐이다. 해설을 근거로 데이터를 채우면 환각이 정본으로
승격되고, 이후 모든 검사가 그 환각을 기준으로 통과한다.

실증: 2026-08-11. 2019_9월 r20199a Q23 은 bogi 가 빈 문자열인데 해설 5선지가
<보기> 표를 인용하고 있었다. PDF 대조 결과 표는 실재했고, 해설이 인용한
5행 10개 값이 실제 값과 하나도 일치하지 않았다. 해설 기준으로 채웠다면
학습자가 시험지와 다른 표로 문항을 이해하게 된다.

### 7-25. 커밋 메시지의 범위는 실제 변경 범위와 일치해야 한다

커밋 제목에 적은 대상 범위가 실제 변경 대상과 다르면, 이후 이력 추적이
불가능해진다. 범위를 특정할 수 없으면 대상을 적지 않는다.

실증: 66af3d3 "fix: 2022~2026수능 해설 품질 개선" 이 실제로는 2019_9월
r20199a 를 수정했고, 그 커밋이 Q23 해설에 환각 표를 유입시켰다.
제목만으로는 2019_9월 이 영향 범위임을 알 수 없었다.

### 7-26. 카운트는 스코프를 한정한 뒤 보고한다

PDF 전체·파일 전체·예시 집합에서 센 값을 세트 단위 결론의 근거로 쓰지 않는다.
동일 마커 계열이 다른 영역(화법과 작문 등)에서 재사용되고, 게이트가 출력하는
예시는 상한·절단이 걸려 있어 전수가 아니다. 대상 구간을 먼저 확정한 뒤 센다.

문자열 대조 시 정규화는 양쪽에 동일하게 적용한다. 한쪽만 정규화하면
일치하는 문장이 불일치로 뒤집힌다.

실증: 2026-08-11 하루에 4회 발생.
  452 → 32    "양 끝이 따옴표" 판정식이 정상 인용도 참으로 만듦
  예시 → 전수  게이트 s2failEx 는 세트당 6건 상한 + 34자 절단
  PDF 전체 → 지문 구간   정박 결손 "8건 전건 복원 가능" → 확정 3세트
  정규화 비대칭   l2016b 본문 대사 10건을 "PDF에 없음"으로 오분류.
                 그대로 보고됐으면 "본문 창작"이라는 오보가 될 뻔했다.
