# PLAN-consult-history — 진단·성적 이력 추적 + 성장 리포트

> **트랙: 컨설팅 · 우선순위 4/5** (선행: PLAN-eng-diagnosis-v2, PLAN-consult-score-input — 이력의 원천이 이 둘)
> 근거: 상담의 핵심 질문은 "지난달보다 나아졌는가"인데 현재는 단발 진단만 — 이력이 쌓여야 재상담(재방문)이 생기고, 학부모 리포트가 "성장 스토리"가 됨. 데이터는 이미 localStorage에 쌓이는 구조라 표시층만 만들면 됨.
> 라벨: [Confirmed] 진단 이력 localStorage 저장 존재. [Inference] 성장 지표 선택의 적정성 — 강사 사용 후 조정.

## 목표

진단·실성적 이력을 시계열로 보여주는 "성장" 섹션: ① 회차별 유형 정답률 추이(간단 꺾은선 — 외부 라이브러리 없이 SVG) ② 취약 Top3의 변화(전회 대비 개선/유지/악화 화살표) ③ 리포트에 "지난 진단 대비" 문단 자동 삽입.

## 수정해야 할 정확한 파일

| 파일                                           | 작업                                                  |
| ---------------------------------------------- | ----------------------------------------------------- |
| `평가원_수학영어_확장/08_tool/지니학습기.html` | 이력 표시 + 리포트 결합 (수정 전 `.bak_history` 백업) |
| 데이터 파일들                                  | **수정 금지**                                         |

## 단계별 작업 순서

1. **이력 스키마 확인**: 기존 진단 이력 localStorage 구조 grep으로 확인 — 필드 부족분(진단 버전 v1/v2, 날짜, 유형별 정답률 스냅샷)이 있으면 **신규 저장분부터** 추가(기존 이력은 읽기 전용으로 표시만).
2. **성장 섹션 UI** (진단 결과 화면 하단 + 리포트 블록): ① 회차 선택 칩(최근 5회) ② 유형군별 정답률 추이 미니 꺾은선(inline SVG, 300×120px, 축·점·값 라벨 — CDN 금지) ③ Top3 변화 카드: "순서·삽입 40%→70% ↑" 식.
3. **비교 로직**: 직전 회차와 유형별 정답률 차이 ≥ +15%p = 개선, ≤ -15%p = 악화, 그 외 유지. **문항 수가 2 미만인 유형은 비교 제외**(표본 부족 — "판단 보류" 표기).
4. **리포트 결합**: 이력 2회 이상일 때만 "지난 진단 대비" 문단(개선 1문장·과제 1문장) 자동 생성 — 1회뿐이면 섹션 자체를 숨김(빈 비교 금지).
5. **자가 검증 (node)**: ① 이력 0회/1회/5회 시나리오에서 crash·NaN 없음 ② 유형 문항 1개짜리 회차가 "판단 보류" 처리 ③ 구버전 이력(필드 없음) 혼재 시 정상 렌더 — 로그 첨부. node --check + 파일 안전 절차.

## 성능 낮은 모델이 놓치기 쉬운 엣지 케이스

1. **표본 크기 무시가 최대 함정**: 유형당 1~2문항의 정답률 변화(0%→100%)를 "급성장"으로 그리면 오정보 — 문항 수를 항상 병기하고("2문항 기준") 2문항 미만은 추이 선에서 제외.
2. **v1/v2 진단 혼재**: 12문항 v1과 20문항 v2 이력을 같은 선에 그리면 왜곡 — 회차 점에 버전 뱃지, 비교 문단은 같은 버전끼리만.
3. **같은 날 2회 진단**: x축이 날짜면 겹침 — x축은 회차 번호, 날짜는 라벨로.
4. **SVG 직접 작성**: Chart.js 등 CDN 추가 금지(오프라인 file:// 원칙 + 기존 KaTeX 가드 패턴과 일관) — path d 좌표 계산 시 정답률 0%와 결측을 구분(결측은 선 끊기).
5. **localStorage 이력 상한**: 최근 20회만 보관(초과 시 오래된 것 삭제) — 삭제 전 확인창 불필요(자동), 단 리포트에 "보관 20회" 명시.
6. **실성적 이력과 진단 이력 분리**: 두 시계열을 한 그래프에 합치지 말 것 — 별도 카드.

## 직접 검증할 수 있는 완료 기준 (대표용)

1. 진단을 2회 수행(2회차는 일부러 더 맞힘) → 결과 화면에 추이 선과 "개선 ↑" 카드가 뜨고, 리포트에 "지난 진단 대비" 문단이 생김.
2. 이력 1회 상태에서는 성장 섹션이 아예 없음(빈 껍데기 아님).
3. 유형당 1문항짜리 항목에 "판단 보류(표본 부족)"가 표기됨.
4. 인쇄 미리보기에 성장 블록이 포함되고 SVG가 잘리지 않음.
